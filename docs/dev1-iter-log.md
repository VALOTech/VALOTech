# dev1 iteration log

> **Not for `main`.** This file lives on `development` only (`.claude/CLAUDE.md` §1.1).

Working state, not an archive. Each iteration of `/dev1` appends one entry and
prunes the oldest until **twenty** remain — the bound is held by
`scripts/check-log-retention.py` and the reason it is safe is §13.1: an entry
is deleted only once every durable fact in it already lives in its permanent
home, which is `docs/tasks.md` for task state, `docs/decisions-log.md` for a
choice, and `docs/operator-checklist.md` for something waiting on a person.
`git log -p docs/dev1-iter-log.md` is the archive, so a pruned entry is
recoverable verbatim and no second copy is kept here.

A cold start reads the tail of this file to find out where the last one
stopped. `OUTCOME:` is fixed vocabulary so that reading can be mechanical:
`CLOSED` when at least one task moved to `[x]`, `IDLE` when the queue was
stale and the loop re-fires, `STOP` when it ended and the owner is needed.
`REASON:` on a non-`CLOSED` entry is one stable phrase, reused verbatim when
the same condition recurs, so three of them in a row can be detected rather
than noticed.

---

## 2026-09-15 · ADMIN-001/T11 + LEGAL-SG-001/T2 (2 tasks) · iter 104
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: `investor_type` on the account, and the hall's footer. The column is nullable with no default and **folded into the accounts CREATE, not added by an ALTER** — apps/web/src/db/db.test.ts:parseColumns reads that file's CREATE body and holds it against SCHEMA line for line, so a column arriving by a later migration fails the drift guard rather than the reviewer; the brief for this iteration assumed an ALTER and was wrong, which is the correction worth keeping. Null means nobody has said and is enforced three ways rather than asserted: no default, a read that hands null through, and a route that refuses an omitted field instead of treating the omission as a request to clear. It orders the landing and gates nothing — apps/web/src/content/investor-type-access.test.ts pins that behaviourally in both directions and structurally, the `Actor` the predicate receives carrying an id and a role and nothing else. The trail holds the act and neither value (`account.investor_type_change` with an empty recordable list), which the browser drive confirmed by string containment on the written row rather than by reading the columns it was expected to fill. Driven against a **rebuilt** artifact: the implementing pass declined the drive because the running server predated its edits by twenty-six minutes, which was the right call and left it owed. LEGAL-SG-001/T2's remaining half was blocked only on there being no hall to put a footer in; the shell now carries a `footer` landmark in the segment layout, so every future `/hall` surface inherits it. Also written: the `INV-001 ⇄ ADMIN-001` dependency edge, which the design cited in prose and its frontmatter did not record. Nine mutants killed on the column, four on the footer's iteration-mates earlier in the day, every file restored byte-identical. 85 files / 1388 tests green; `make precommit` 0.
NEXT: `INV-001/T6` is now unblocked and is the natural next anchor — the landing's blocks ordered by the type this iteration added, with an unclassified reader getting an order of their own. Two things are owed and recorded rather than silent: `/account/mail` and `/account/sessions` sit outside `/hall`, so an expired session there returns the reader to the landing instead of the page, which `INV-001/T3` fixes by moving them to `/hall/account`; and the drive investor in the development database is left reading `current`, which is useful for building T6 and is dev data, not product state.

## 2026-09-14 · INV-001/T4 (1 task) · iter 103
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: **The chrome is finished and the search moved onto it.** apps/web/src/app/hall/search-form.tsx is now a magnifier in the toolbar that opens a `details` disclosure — looking something up is a thing an investor does from wherever they are, and a field that sits on one page is one they navigate back to before they can use it. It opens already holding what the reader last asked for. **The language control is twenty rows, each its own submit**, so the click that chooses a language is the click that applies it: no confirm step, and it works with no script, which a `select` that navigates on change cannot. Each language is named in itself and carries the gateway's own flag mapping, decorative with `alt=""` because a flag is a country and not a language. apps/web/src/app/api/locale/route.ts is the write, Origin-checked like the door, and **its redirect target is validated rather than trusted** — a form field that becomes a Location is an open redirect. Search and sign-out are icons carrying a visually-hidden name, so neither is a guess for somebody who does not know the glyph. **The owner asked for a light/dark toggle and it was not built**: brand/GUIDELINES.md records that the three-way toggle was removed deliberately because this system is lit from inside, so the conflict went back to the owner rather than being resolved quietly, and they chose the brand. The hall is called a hall in all twenty locales at the owner's instruction. Nine tests pin the route's two security properties, which had been checked by hand once and would otherwise have regressed silently. Suite 82 files and 1349 tests; gate exit 0.
NEXT: **The visible strings say hall; the route, the code and the vocabulary still say room**, and that is a question for the owner rather than a rename to start unasked: /hall is in invitation and reset mail already sent, §7.3 of the repository's own guide defines `investor hall` as its ubiquitous term, and changing it touches the route, the i18n namespace, three designs and the PRD. The agreed build order is unchanged and waiting: ADMIN-001/T11 (the investor-type column, its admin control, the privacy notice and the record of processing) → INV-001/T6 (the landing's order following it) → the visual system → the four tools, of which INV-003/T6 (one product's own page) is the first. INV-001/T2 and T5 remain the hall's own remainder: an error state that is a different rendering from the empty one with a retry, and an expired session returning the reader where they were going.

## 2026-09-14 · CMS-007/T3,T5,T6 (3 tasks) · iter 102
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: **The hall can be searched and narrowed, and CMS-007 is live.** apps/web/src/content/search.ts takes kind, product, period and type and composes each as a clause on the one statement — never a pass over the rows it returned, because a filter applied after the fetch pages wrongly and reads to the person scrolling as content that vanished. A filter narrows on its own: choosing a product and typing nothing is a question, and answering it with the empty result an absent query gives would be a control that does nothing until it is accompanied. The form is a plain GET, so the narrowing lives in the URL, can be sent to a colleague, is undone by the back button, and needs no script. apps/web/src/app/hall/query.ts reads anything outside a closed vocabulary as absent rather than handing a column a value it cannot hold, and picks which narrowing an empty result should offer to drop — fixed precedence rather than four extra statements to find out which excluded the most. Twenty-four keys authored in all twenty locales, Arabic carrying its six plural branches. **A design disagreement was fixed rather than worked around**: INV-001 §3 said “the two filters”, CMS-007 §2 said three and its table four — INV-001 now defers the set to the feature that owns it instead of restating a number that disagrees the first time one is added. Mutation-proved with the file restored byte-identical: the product clause, the type clause and the narrowing guard each KILLED by assertion. **One environmental failure, attributed rather than absorbed**: a full run showed two files failing in beforeAll hooks, and the cause was my own application and gateway servers competing for the box — stopped, the same suite is 81 files and 1340 tests green in 144s against 511s loaded. Also filed INV-DEC-02 and its three rows for the next stretch.
NEXT: **The hall's presentation is the work the owner has asked for, and it is now specified rather than guessed.** INV-DEC-02 settles that the hall knows an investor from a prospect through a nullable accounts.investor_type that orders the landing and never gates it; INV-001 §3 carries the order table and the rule that null is not prospect. The agreed sequence is ADMIN-001/T11 (the column, its admin control, the privacy notice and the record of processing) → INV-001/T6 (the ordering) → the visual system → the four tools the owner chose: one product's own page (INV-003/T6), the report archive and reading view, the deck reading view, and a since-your-last-visit digest. The visual direction is settled too: the six product hues that already exist on the gateway carry product identity while the one periwinkle accent keeps its single job, and motion happens only when a reader touches something — the brand forbids idle animation and the owner chose to keep it that way.

## 2026-09-14 · INV-001/T1 + INV-003/T4,T5 (3 tasks) · iter 101
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: **The investor hall exists.** apps/web/src/app/hall/ is the surface, its segment layout the gate and the chrome, and the four things INV-001 §3 asks for render in the order of somebody catching up. INV-003/T4 and T5 close on the render they had been waiting for: all six products always appear, and an unset row is drawn as an absence — dashed pill, no fill, and the words underneath — rather than as this module's assumption stated as fact. **The first version was rejected by the owner and rebuilt, and the rejection was right**: it was a light-grey card stack, and brand/GUIDELINES.md says the theme has no light variant. The rebuild is on the designer's own system, read from her repository rather than inferred — an eyebrow pill naming each section, the count as the one accented claim, mono for asides, six cards a reader compares instead of six rows they scan past. Her later rule wins over her earlier one where they disagree: the glass sheen is disabled in her shipping CSS, so it is not here either. **The application serves the brand's faces for the first time** (apps/web/public/fonts, apps/web/src/app/hall/fonts.css scoped to the hall's layout so no surface outside it is restyled): document.fonts reported nothing loaded before and now reports Roboto Condensed at four weights and DM Mono. Twenty locales authored, including four stage words and three chrome strings; every plural branch begins with # because apps/web/src/i18n/dictionary.test.ts reads the first word inside an ICU branch as a placeholder, so English-only could never have landed. **Three of my own errors, each caught by a gate or a measurement rather than by me**: a new test published an update and broke a sibling's counts from behind; the same test omitted the 120s hook budget every database-backed file here declares, and died on Vitest's ten-second default once the box was busier; and closing T1 stranded five rows that named it as their blocker — re-derived one at a time, three genuinely free and two re-blocked on CMS-007/T3, the control, rather than on the hall. Suite 81 files and 1334 tests; gate exit 0.
NEXT: **INV-001/T2 and T5 are the hall's own remainder**, and both are small next to what just landed: T2 wants an error state that is a different rendering from the empty one, with a retry — the empty states ship, the error states do not, and today a failed stream read is indistinguishable from a company that has not posted; T5 wants an expired session to return the reader to where they were going. T3 and T4 are honestly partial rather than closed: two of four destinations exist, because reports and decks are RPT-003 and DECK-003's pages and an entry that answers the not-found page is one somebody tries twice, and the locale control is unbuilt — it wants a route that sets NEXT_LOCALE and a control that posts to it. **Three rows the hall unblocked are now the cheapest work in the tree**: POST-002/T6 (unread marking and paging, on a stream that now renders), CMS-007/T3 (the field and its filters, on the surface CMS-007 §4 names), RPT-002/T3 (the archive, a page added under a segment that now carries the gate and the chrome).

## 2026-09-14 · LEGAL-GLOBAL-001/T5 (1 task) · iter 100
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: LEGAL-GLOBAL-001/T5 — docs/compliance/record-of-processing.md, one page because there is one processing activity. It states who holds the data, what is held and on what basis, who it reaches, where it rests, how long it is kept, how it is protected, and what is deliberately not claimed. **Written from the designs and naming no article or section number**, because the statute text is not in this tree and LEGAL-GLOBAL-001/T6 is the row that lands it — a record citing a section nobody here has read is the paraphrase docs/compliance/README.md exists to forbid, and it reads as authoritative exactly where it is wrong. The basis table is LEGAL-SG-001 §3's, reused rather than restated, so two documents cannot drift into two answers about one system. **Three things are recorded as absent rather than implied**, which is the whole reason the page is worth having: no contact is named, because that is the owner's at DPO-CONTACT; where the data rests is open at OPS-DEC-03, so the page states the question rather than an arrangement and claims no transfer mechanism; and the retention sweep exists and is run by hand with make retention, while the schedule that would run it continuously arrives with OPS-001 — so the page says a window is enforced when somebody runs it, instead of implying a cron nobody wrote. Gate green: evidence rows 248 to 249, doc paths 174 to 177, so the checks read the new page rather than passing over it.
NEXT: **INV-001/T1 — the hall's landing surface — and the descent to W4 is now forced rather than chosen.** W3's only remaining open row is LEGAL-GLOBAL-001/T1, the notice's additional GDPR statements in twenty locales, which §15 makes Medium with owner review because it changes what a public page says; everything else in the wave is external residue, an owner decision, or waits on the hall. Eight rows name INV-001/T1 and W4 holds fifty-four buildable tasks behind it. INV-001 is design-ready, depends on AUTH-002, CMS-006, CMS-007, POST-002 and RPT-002 — all shipped — and touches api, frontend and ui with no new storage, so the hall is a composition of reads that already exist and are proven. Read INV-001 §2 before starting: one route resolves the reader, asks for the stream's first page, the current report, the progress board and the unread count, and renders; apps/web/src/portfolio/board.ts:standing already answers the board half and is waiting for its first surface.

## 2026-09-13 · POST-002/T6 + RPT-002/T3 + DECK-002/T3 (3 tasks) · iter 99
STATUS: green · TIER: S · OUTCOME: IDLE
REASON: the queue offered work that cannot be built; the index was corrected rather than a task closed
WHAT CHANGED: docs/tasks.md — three rows that read as ordinary open work each name a page that does not exist, and none carried a Blocked by, so a lane picking one up would have re-derived the blocker and put it back. apps/web/src/app carries account, admin, api, content, forgot, health, invite, media, privacy, reset, set-password, sign-in and unsubscribe, and **no hall segment**. POST-002 §3 serves the stream at GET /hall/stream and RPT-002 §3 the archive at GET /hall/reports, so both wait on INV-001/T1; DECK-002 §3 puts the change notice in the deck reading view, which is DECK-003/T1's page and is open, so that row waits there instead — the three do not share one blocker and saying they did would have been the same error in the other direction. docs/roadmap.md regenerated: **W3 reports three buildable rather than six**, which is what the ledger actually holds. Two infrastructure defects were also found and fixed this session, both pushed: the evidence gate read a citation's closing full stop as part of the path, which Win32 normalises away and macOS does not, so sixteen rows failed over ten paths that all exist; and the pre-push hook probed for `python` alone, found none on a machine carrying `python3`, and took its fail-open branch — checking the served copy, skipping every gate that reads the planning documents, and saying so in one line a successful push scrolls past.
NEXT: **W3 is nearly exhausted and the descent to INV-001/T1 is now backed by the ledger rather than by a hunch.** What remains buildable in W3 is LEGAL-GLOBAL-001/T5 (a one-page record of processing, whose "what is not claimed" half is already written in the design §3) and LEGAL-GLOBAL-001/T1 (the notice's additional GDPR statements in twenty locales, which §15 makes Medium with owner review because it changes what a public page says); LEGAL-SG-001/T2's remaining half is the hall's footer and waits on INV-001/T1 like the rest. **INV-001/T1 is the row the tree is bent around**: six rows name it and W4 holds fifty-four buildable tasks behind it. The Turbopack question the handover carried is answered and needs no issue — a clean build passes in 32s, and the shared-.next contention the handover blamed was reproduced deliberately and did not panic, so the constraint that blocked roughly five iterations of browser drives does not apply on this machine; those owed drives are now reachable.

## 2026-09-13 · MAIL-002/T2,T3,T4,T6 (4 tasks) · iter 98
STATUS: yellow · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: MAIL-002/T2, T3, T4 and T6, at Critical tier through critical-impl. **The unsubscribes table has had a reader since MAIL-001 and no writer at all**, so an investor could be excluded from a send only by a row nothing could create. **T2** is the unsubscribe: a bulk message carries a per-recipient link, the page says what stops and what does not, and one button posts the change, with no sign-in. **The verb was the load-bearing decision and it is not the obvious one** — a bare GET that writes is fetched by mail scanners and link-protection rewriters, which unsubscribes people who never pressed anything and leaves rows indistinguishable from real ones, so GET renders and POST writes and the route exports POST alone (MAIL-DEC-05, loop-settled, with an overturning signal). The token is an account id beside an HMAC over it under a purpose label, timingSafeEqual, the row keeping its SHA-256; derived rather than minted because the schema has nowhere for a pre-minted token and this ships no migration, so it is stable for the life of SESSION_SECRET — the trade the design already makes when it says the token identifies without authenticating. **T3 was already true and unproved**: transactional mail does not pass through resolveRecipients and kind is set at the send, so a test now pins it and making deliverTransactional consult the suppression list turns that test red. **T4** gives the manual stop with its reason and names the MAIL_FROM mailbox as where bounces arrive, because SMTP reports nothing after hand-off and a person does what a webhook would. **T6** is the log by recipient and date; state is never delivered, and a row still queued after a send ended is presented as an unreported outcome rather than a failure. **The review found a defect of mine from iter 96 that my own iter-97 memory describes exactly**: apps/web/src/app/admin/updates/composer.tsx imported standingOf as a VALUE from portfolio/board.ts, which imports getDb — a client component pulling the PostgreSQL driver into the browser bundle, failing next build on "Can't resolve util/types". I wrote the lesson down last iteration and did not sweep for the existing violation. Pure half split to portfolio/standing.ts, five import sites repointed. Also fixed: the breach runbook still said MAIL-002/T3 was open, and five French strings used a plain space before a colon where I18N-R05 wants U+00A0. Eight mutants, eight killed — and the agent reported that two of them first read as kills off a vitest filter matching NO files, since vitest exits 1 on "no test files found"; re-run against the literal path with an unmutated control first, both are genuine. Full suite green alone at 80 files and 1327 tests, verified independently; make check 0 FAILs.
NEXT: **Handover iteration — the owner is moving this work to a macOS machine.** Everything is committed and pushed; the tree is clean. **One open question travels with it**: `npx next build` under the default Turbopack panics with TurbopackInternalError "AssetContent::file was canceled", while `npx next build --webpack` succeeds and emits all six new routes. Not attributed. The strongest hypothesis is environmental and cheap to settle on a fresh machine: a peer dev server holds :3100 here (PID 23556) and shares apps/web/.next, 504MB, so two processes write one build directory. On a machine with no peer server, prune .next and re-run — if it still panics it is a toolchain defect worth filing, and if it passes it was the shared directory. **W3 after this**: LEGAL-GLOBAL-001/T1 and T5 (public legal words in twenty locales, Medium with owner review), and DECK-002/T3, POST-002/T6, RPT-002/T3 which all render into the hall INV-001/T1 has not built and which still do not say so in the ledger. **INV-001/T1 is the highest-leverage row in the tree**: it unblocks seven. Two owner gates are named and unmet: docs/operator-checklist.md#SMTP-MAILBOX (no investor receives anything, and a password reset is impossible by any path) and #COMPLIANCE-SOURCES (no statute text, so neither legal design may be called complete). Thirteen owner decisions open.

## 2026-09-13 · RPT-001/T4, DECK-002/T5, LEGAL-GLOBAL-001/T4 (3 tasks) · iter 97
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: RPT-001/T4, DECK-002/T5 and LEGAL-GLOBAL-001/T4, plus two rows filed that nobody had. **T4 puts the progress board beside the report section that narrates it**, from apps/web/src/portfolio/board.ts:standing, located by apps/web/src/content/report-structure.ts:standsSectionAt. Two things it deliberately does not do. It **shows and does not check**: my first design computed whether the section named each product and called that agreement, which a substring match on prose cannot establish — RPT-001 section 4 asks for the values beside the section so the author can compare, and that is what ships. And the locator asks apps/web/src/content/sections.ts:deriveSections where a section opens rather than scanning for a heading itself, so this file holds no second opinion about what a section is. **The pure half had to move out of reports.ts**: the editor is a client component and reads the locator on every keystroke, so importing it from a module that also opens a database connection would have pulled the connection into the browser bundle. **DECK-002/T5 names who a deck publish reaches** — the one publish in this product with a named audience. apps/web/src/content/decks.ts:deckAudience answers by name in name order; a pinned grant is counted rather than named because it is exactly who is not reached, an invited grantee is named and marked, a suspended one is in neither number. **The zero case is two situations and the sentence separates them**: nobody granted, against everybody pinned — the second is the trap, a new version published into a diligence process where every reader stays where their pin holds them. I checked that the suspended filter is reachable before writing it, because suspension deletes sessions and invitations and could plausibly have deleted grants too; it does not, so the guard is live rather than the unreachable kind. **T4 of LEGAL-GLOBAL-001 gives the breach runbook one clock**, 72 hours from awareness rather than from the assessment, for everyone. **I refused to write which regime is stricter and that turned into the iteration’s real finding.** docs/compliance/ holds a README and no source text, and its own rule is that a claim about the law cites a section and the file it was read from. So the runbook cites the design. The axis review then caught that LEGAL-GLOBAL-001 section 3 still called 72 hours "stricter than the PDPA’s" — a comparison of two statutes nobody here can source — so the design now states the number as its own decision, and docs/operator-checklist.md#COMPLIANCE-SOURCES plus LEGAL-SG-001/T8 and LEGAL-GLOBAL-001/T6 make the missing text visible in the ledger instead of only in a README. **Two ledger corrections.** LEGAL-SG-001/T4 sat open with no blocker while operator-checklist.md#DPO-CONTACT said it cannot close without the owner naming a person, so the roadmap counted it buildable; it is [!] now with the signal that ends it. And RPT-001 is all-rows-closed but stays in-progress with inert_until: an admin composes and publishes a report end to end and no investor reads any of it, because the hall’s reading surfaces are unbuilt. **PRODUCT_LABEL was in two files and this needed a third**, so it and STAGE_LABEL moved to apps/web/src/portfolio/labels.ts — one copy of what the company calls its own products. Mutation-proved eight ways; one SURVIVED on the first run — widening the heading match to a prefix, which no fixture could tell from equality — so the prefix and containment cases were added and it dies now. Full suite green alone at 75 files and 1275 tests; make check 0 FAILs.
NEXT: W3 has four Standard rows left and they are the same shape: **DECK-002/T3, POST-002/T6 and RPT-002/T3 all render into the hall, which does not exist** — apps/web/src/app carries no room directory, so each is parked behind INV-001/T1 exactly as CMS-007’s three are, and none of them says so in the ledger yet. **That is the first thing to do next iteration**: either mark them or, better, build the read halves the way POST-001/T6 built the board read before the board existed. MAIL-002’s four are Critical under section 15 (sending mail) and want critical-impl. LEGAL-GLOBAL-001/T1 and T5 are public legal words in twenty locales, Medium with owner review. **Still owed**: no browser drive of any admin surface, now fifteen — this iteration adds the standing panel in the report editor and the audience line in the publish confirmation, neither of which any test renders, because this repo runs vitest under environment: node. **Standing rules**: one test run at a time; read a section from the TARGET repo’s CLAUDE.md before citing it; re-triage after context rather than at PICK BATCH; and check a guard is reachable before writing it. Twelve owner decisions open; two block a task.

## 2026-09-13 · POST-001/T5,T6 (2 tasks) · iter 96
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: POST-001/T5 and T6, which completes the feature. **T5**: a marker at two hundred words that offers to move the text into the report being drafted, because the honest answer to "this got long" is rarely "make it shorter" but that the text is in the wrong surface. apps/web/src/content/reports.ts:draftReport finds the destination (greatest period with an unpublished revision, a staff read composing no audience predicate because a draft reaches no reader at all); apps/web/src/content/items.ts:appendToDraft adds and never replaces, under the item’s own lock, which is the whole difference from saveDraft — that one is handed the document by an editor holding it, this one a fragment by a surface that has never seen the rest. **The destination is the server’s choice and that is a security property**: the request carries blocks and nothing else, and a test sends a caller-supplied itemId and watches it be ignored. **T6** shows the tagged product’s standing — the stage word, not a number, because INV-003 says a figure invented to look like measurement is worse than a word that is true — always all six, with an absent row shown as a gap rather than as an assumption. **I under-triaged this batch and the reviewer was right to correct it.** I filed it Medium; it is Complex under section 15 — ten source files across POST-001, RPT-001, INV-003 and CMS-005 — and Complex asks for confirmation before coding, which I did not take. Section 15 forbids downgrading, so the correction is recorded rather than argued: the work was built, independently reviewed, and lands on development, which the owner promotes, but the step I skipped was skipped. **The review returned yellow with eleven findings and the three that mattered were about what the move silently costs.** (1) Appending changes the report’s words, so CMS-005 drops that draft’s translations — correct, and my version did it in silence. A save can be silent because the author is in the editor where the grid shows the loss; this is reached by a second person from a third surface on a draft they may not own. appendToDraft now returns the count, the route answers it, and the control says so before it is pressed. (2) It was writing author_id, which would have moved a number apps/web/src/content/grants.ts:erasureContentCounts reports to an admin about to delete a person’s data — handing over a fragment is not authorship. (3) A stored draft the validator can no longer read was answering 422 with a block index in a document the caller cannot see; it now raises UnreadableDraftError and answers 409. Also: the live region is always in the DOM and holds no counter, because a region carrying {words} announces on every keystroke past the threshold, and my concurrency harness polled for the barrier without asserting it engaged — a poll that simply fell out of its loop would have silently become the weaker case it exists to rule out. **Three gates caught things, and each was right.** check-content-access refused the route test for naming content_items: the fix was to build and read through the content module and to leave no open draft by PUBLISHING it rather than deleting rows, which then surfaced PeriodTakenError — RPT-002 allows one published report per period, so each case takes its own. validate-roadmap refused a depends_on RPT-001 edge I had added: it pushed three designs to depth 11, past the last wave band, which is the graph saying plainly that a runtime relationship is not a build order — T1..T4 shipped before RPT-001 mattered at all. And the INV-003 edge would have closed a cycle (INV-003 to INV-001 to POST-002 to POST-001), the same shape as iter 94. Both are written in prose on BOTH sides now, which is the part I got wrong first time: INV-003 section 4 got its reciprocal sentence in the same diff and RPT-001 did not, so a lane editing reports.ts would have learned nothing about draftReport having a consumer outside the report surfaces. Mutation-proved: removing the append lock reddens the concurrent case and nothing else, and that case forces the interleaving with a parking trigger rather than trusting Promise.all. Full suite green alone at 74 files and 1257 tests; make check 0 FAILs.
NEXT: POST-001 is complete and reads live. **RPT-001/T4 is the natural next** — the same board beside the report section that narrates it, reusing apps/web/src/portfolio/board.ts:standing, which this iteration built for the composer; INV-003 section 4 and RPT-001 section 4 each name the other’s reader. Then DECK-002/T3 and T5, LEGAL-GLOBAL-001/T1, T4 and T5, MAIL-002’s four, POST-002/T6 and RPT-002/T3. **INV-003/T4 and T5 are deliberately still open** and now carry a Note saying why: the read half is built and pinned, the composer is the first surface carrying the word-plus-date treatment, but the board those rows name belongs to INV-001/T1, which nobody has built — closing them on a read alone would call an unbuilt surface shipped. LEGAL-SG-001 stays where it was: T2 on INV-001/T1, T4 on the owner naming a DPO, T7 on OPS-DEC-03. **Still owed**: no browser drive of any admin surface, now thirteen, and the composer has grown two more panels that no test renders — this repo runs vitest under environment: node, so every rule they apply was moved into modules a test can reach and the rendering itself is unchecked. **Standing rules**: one test run at a time; read a section from the TARGET repo’s CLAUDE.md before citing it, never from the copy the workspace loads; and triage at PICK BATCH by counting files and designs, not by how familiar the pattern feels. Twelve owner decisions open; two block a task.

## 2026-09-13 · POST-001/T1..T4 (4 tasks) · iter 95
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: POST-001/T1..T4 — the update composer, which is the first surface in this room whose whole design problem is **cost rather than correctness**. The generic path asks for a type, a title, an address and an audience and then hands the author to an editor; this is one screen, and apps/web/src/content/updates.ts:composeUpdate writes the item and its first revision in a single transaction so an item with a title and no words cannot exist. The address is derived and never asked for, suffixed on collision, settled by the unique index. **T3 added content_items.product**, folded under DATA-DEC-01 — the six plus the company, only on an update, and null kept as its own answer because "the author did not say" is not "about the company", the same distinction accounts.locale drew last iteration. That closed the blocker on CMS-007/T3, which the deferral gate caught the moment the row went green; it now names only INV-001/T1, its remaining reader. **Two findings came from testing rather than reasoning, and both were mine.** (1) The concurrency test used Promise.all and the mutant removing the ON CONFLICT guard **SURVIVED** — the scheduler ran the two composes in sequence, so the second read a committed table and the retry never executed. Replaced with a forced interleaving (a trigger parks the first insert on an advisory lock while the colliding address is taken from another connection); both mutants now die on assertions. The first barrier attempt deadlocked against itself because the arming row’s lock was held by the transaction it was meant to overtake — parking on the title fixed it. (2) slugFrom, lifted verbatim from the shipped new-item form, turned "resume" into "re-sume": NFKD leaves a combining mark and the mark is not [a-z0-9], so the strip made it a separator. A latent defect in shipped code, found only because the rule was factored out and given a test. **The independent review found two more defects and a claim wider than its code.** A second press of the file control filed a DUPLICATE update - a return inside try still runs finally, so the button came back under the cursor mid-navigation, and the address rule suffixes rather than refuses, so the duplicate would have been accepted silently. Clearing the title field was a one-way trap: an empty string is not null, so the derivation never resumed and the only exit was a reload. A kind===null guard could never run (a disabled button cannot be pressed) while three artifacts claimed the focus move it would have made. And slugFrom carried only the letters NFKD DECOMPOSES - the rest are single code points, so Strasse became stra-e and Da Nang became a-nang, while my test probed only the class that already worked. **syncMediaRefs in composeUpdate was a second surviving mutant**: every document in the new tests was paragraphs only, so the call was inert. All fixed and mutation-proved here. **I also filed the rail defect in the wrong home**: I read section 12 from AMAVO’s CLAUDE.md, which the workspace had loaded as the primary, and VALOTech’s says the opposite - a finding is FIXED in the artefact that owns it until OPS-001. REVIEW/T2 retired, /admin/portfolio removed from the rail, which restores the seven destinations ADMIN-002/T2 describes. Full suite green alone; make check 0 FAILs; migrate-roundtrip up-down-up.
NEXT: No REVIEW row is open and none should be until OPS-001 — the rail is fixed in place. W3 continues: **POST-001/T5 and T6 remain** — the soft length marker that moves a too-long update into the current draft report, and the tagged product’s progress value beside the composer, the second of which wants INV-003’s board (portfolio carries a stage and a headline and no number today). RPT-001/T4, DECK-002/T3 and T5, LEGAL-GLOBAL-001/T1, T4 and T5, and MAIL-002’s four sit beside them. **Still owed and growing**: no browser drive of any admin surface, now thirteen, and this iteration adds a composer verified by no test at all — every rule it applies was moved into apps/web/src/content/derive.ts so something could reach them, but the screen itself has never been opened. **Standing rule**: one test run at a time. Twelve owner decisions open; two block a task (CMS-DEC-07 holds CMS-004/T6, OPS-DEC-03 holds LEGAL-SG-001/T7).

## 2026-09-13 · AUTH-003/T3, SEC-001/T4, MAIL-001/T6 (3 tasks) · iter 94
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: Three Critical rows, one surface: mail that reaches a named person. **AUTH-003/T3** - the invitation and the reset go out in the language the account records; accounts.locale was folded in as a nullable column, because "the admin did not say" is a different answer from "English" and a column that could not hold the first would turn every unconsidered invitee into a claim about what they read. **SEC-001/T4** - the public /forgot, which answers before the message is sent and never waits for it: an SMTP round trip is hundreds of milliseconds against the low milliseconds everything else costs, so awaiting it would answer slowly for an address an account holds and hand back the enumeration oracle AUTH-003/T5 exists to close. **MAIL-001/T6** - the retry, on retry_of at bigint rather than the uuid MAIL-DEC-02’s prose supposed, guarded by a partial unique index so one attempt is superseded once. **The independent review came back RED and it was right twice.** (1) The admin-initiated reset mailed NOTHING: the route awaited requestReset and discarded the ResetDelivery it now returns, so nobody ever called send() - and my own doc comment beside it asserted "the admin path does await it". Every other assertion in that file passed, because none of them pinned delivery. (2) /forgot was silently inert: with no credential the outcome is `unavailable`, which RESOLVES, so the .catch() caught nothing, no row was written, nothing was logged, and the person read "a link is on its way". Both fixed and mutation-proved - dropping the delivery again fails on "expected undefined to be defined", restoring the bare .catch() fails the stdout spy. The review also added three retry narrowings (bulk-only, a fixed claim order against deadlock, and reporting a claim somebody else took instead of dropping it), the Arabic `few` plural arm, and caught a stale Note contradicting its own Evidence. **One suggestion I measured and refused**: adding depends_on MAIL-001 to AUTH-003 closes a cycle (MAIL-001 -> ADMIN-001 -> AUTH-003) that this repo has no gate to catch, so the cost of the missing credential is named in MAIL-001’s own inert_until, which is the field an operator actually reads. Full suite green alone; make check 0 FAILs; migrate-roundtrip up-down-up.
NEXT: **AUTH-003 is back to in-progress with inert_until and the PRD row to `blocked`, which is the review’s most important finding and not a bookkeeping detail**: invitation works by hand, but a password reset is impossible by ANY path without SMTP_URL - the link is never shown to an admin, because one holding it could set an active account’s password and sign in as its owner. An investor who forgets their password cannot get back in today, and docs/operator-checklist.md#SMTP-MAILBOX now says so where it used to say "nothing is blocked - only automated". SEC-001 is complete and live; MAIL-001 stays in-progress on the same credential. **W3 is the frontier and POST-001 is the next whole feature** - six open rows, the update composer, nothing blocking any of them; RPT-001/T4, DECK-002/T3 and T5 beside it, and LEGAL-SG-001/T4 needs only a named DPO. **Still owed and now larger**: no browser drive of any admin surface, now twelve, and this iteration adds two public pages - the /forgot form and its unavailable panel, the latter verified by no test at all because this application runs vitest under environment: node. **Standing rule**: one test run at a time. Twelve owner decisions open; two block a task (CMS-DEC-07 holds CMS-004/T6, OPS-DEC-03 holds LEGAL-SG-001/T7).

## 2026-09-13 · REVIEW/T1 (1 task) · iter 93
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: REVIEW/T1 — the suite that passed file by file and failed a different handful in aggregate. **The cause was mine and it took a controlled experiment to see it.** Each database-backed file owns a database named by a fixed constant and drops it in beforeAll with WITH (FORCE), which terminates whatever is connected — so a second vitest invocation pulls the first one’s database out from under it mid-test. Every red run in the record came from me overlapping invocations, and I mis-attributed it three times before testing it: a full run alone is green at 65 files and 1149 tests; the same run with a second invocation of three of its files alongside failed exactly the two files the two runs shared, and nothing else. **The four refutations are kept in the row** because each narrows a future search: not a cold database connection (apps/web/src/ops/logger.test.ts opens none and fails identically); not a cold filesystem cache (a cold start does cost about forty times a warm one, 6569ms against 149ms, but the next warm run failed worse); not the migrations (0.86s to apply every one to a fresh database); and not contention between the twenty-six per-suite databases (one drop and create 0.52s, four concurrently 1.33s, eight concurrently 2.86s). **What shipped is a rule, not a mechanism**: the constraint is written into apps/web/vitest.config.mts beside the worker cap it belongs with, because a per-run database name would leak one on every run — the drop is at setup, not teardown — so one run at a time is the trade rather than something to engineer around. The per-test timeout also moves from five seconds to twenty, on its own smaller measurement rather than as the fix: a test that legitimately took 6569ms cold would fail a five-second limit on a machine also serving three development servers, and twenty stays a sixth of the hook budget these files declare, so a genuine hang still fails fast. Verified against PostgreSQL 17.11: **65 files, 1149 tests, green, run alone** — twice, once before the change and once after. 232 closed, and no REVIEW row open.
NEXT: The ledger is clean again and the frontier is W3. **MAIL-001/T6** is open and unblocked — the retry of failed recipients, wanting the retry_of column MAIL-DEC-02 settled (additive, absent today) and the surface to offer it. **AUTH-003/T3** is open — the invitation mail, wanting a locale column on accounts, with the send build-ahead until the owner supplies SMTP_URL. Then POST-001’s six, RPT-001/T4, and LEGAL-SG-001/T4 whenever a DPO is named — the setting is already there to receive it. **A standing rule for every future iteration**: never start a second test run while one is in flight, and never run `make check` expecting it to be free — one run at a time, then read it. **Still owed**: no browser drive of any admin surface, now eleven of them; the shared development database predates the audit fold, which is the precondition, and recreating it disturbs three live peer servers. Twelve owner decisions are open; two block a task (CMS-DEC-07 holds CMS-004/T6, OPS-DEC-03 holds LEGAL-SG-001/T7).

## 2026-09-13 · MAIL-001/T4,T7,T8 (3 tasks) · iter 92
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: MAIL-001/T8, T7 and T4 — the SMTP adapter, the composer that works without a credential, and the guard on the send. Critical tier (sending mail), built by critical-impl and verified here. **The Mailer port has had no adapter since it was written**, so every task needing delivery named it and waited; nodemailer was already the owner’s choice at MAIL-DEC-03 and is now installed, pinned exactly, with no transitive dependencies. **TLS is required and cannot be argued out of**: requireTLS on both the 465 and 587 shapes, certificate verification on, and neither ignoreTLS nor opportunisticTLS ever named — a connection that cannot be secured fails the send rather than falling back, and a URL asking for something weaker changes nothing. One connection carries the send and closes once. **Without a credential everything except sending works**: the composer renders the exact bytes and the list resolves with its exclusions, because an admin who cannot see who they would have mailed cannot prepare the mail while the credential is arranged. Two different things withhold sending and say so differently — an absent SMTP_URL, and mail.enabled, which the registry calls a kill switch and which a first sender ignoring would have made false. The count is typed and every recipient re-resolved first; the audit carries the actor, the subject and the count and no address on either side. Verified against PostgreSQL 17.11: the six mail suites 69 passed, and the four suites that fail in aggregate 49 passed in isolation at --maxWorkers=1. Seven mutants KILLED with no timeout and every file restored byte-identical, sha256 compared: the plaintext fallback allowed, certificate verification off, the transport rebuilt per message, the typed count not compared, the audience-change refusal deleted, the re-resolve replaced by a raw account read, and an address added to the mail.send allow-list. **What rests on the library rather than on a test is named rather than implied**: that a pooled single connection is one TCP connection, that requireTLS actually aborts against a server refusing STARTTLS, and that verification actually refuses a bad certificate — no TLS handshake was performed, because a test-only SMTP server is a dependency nobody has approved. **Landing the seam falsified every row that named it**: MAIL-001/T6 and AUTH-003/T3 were blocked on the adapter and are now ordinary open work, each carrying what its blocker knew, and SEC-001/T4’s note follows the same fact. **The full suite is RED in aggregate and that is not waved away**: four consecutive full runs failed, at --maxWorkers=1 and under the canonical npm test alike, every failure a timeout and never an assertion, nine different files between them and no mail suite among them. Raising the per-test timeout to forty-five seconds moved the failures to `Hook timed out in 120000ms` in five files, which is what isolated the cause: twenty-six suites each drop, recreate and migrate their own database, four at a time, contending on template1. Every one of them passes alone — the four worst took 22.9s for 49 tests green. So what is claimed here is what was run: the six mail suites, 69 tests, green. The aggregate failure is REVIEW/T1, it predates this change, and it is a harness defect the isolation pattern grew into one suite at a time. STATUS is green for the work and the standing defect is filed rather than folded in. 231 closed.
NEXT: **MAIL-DEC-04 is filed and is the interesting one**: the send writes a queued mail_log row per recipient before attempting any, and a send abandoned part-way leaves the rest queued for ever — MAIL-DEC-02’s retry is keyed on failed, so nothing reads them again. A stranded row is genuinely ambiguous, never attempted or accepted a moment before the row could be updated, which is why it is the owner’s: retrying every one can mail an investor twice, and retrying none drops somebody silently. The safe default retries nothing automatically. Next in this lane: **MAIL-001/T6** is now open and wants the retry_of column MAIL-DEC-02 settled (additive, absent today) plus the surface; **AUTH-003/T3** is open and wants a locale column on accounts, the send being build-ahead until the owner supplies SMTP_URL. Then POST-001’s six and RPT-001/T4, and LEGAL-SG-001/T4 whenever the owner names a DPO — the setting is already there to receive it. **Owed and growing**: no browser drive of any admin surface, now eleven of them including this composer; the shared development database predates the audit fold, which is the precondition, and recreating it disturbs three live peer servers. Twelve owner decisions are open; two block a task.

## 2026-09-13 · LEGAL-SG-001/T1,T6 (2 tasks) · iter 91
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: LEGAL-SG-001/T1 and T6 — the privacy notice at /privacy, in twenty locales, disclosing the backup window. **The translation question I flagged last iteration turned out to be settled already**: CMS-DEC-04 refused machine translation for CONTENT — a CMS item’s locale drafts, written by a human in the review screen — while the notice is an application surface, and LEGAL-SG-001 §3 asks for twenty locales outright. Doctrine, not a new decision; reading it was the whole of the answer. **The address the notice publishes is a setting rather than a string**: who holds the mailbox a data-subject request arrives at changes without the words around it changing, and a deploy to move an address is a deploy nobody makes. It falls back to the address the gateway already publishes, so the sentence is true whatever the table holds, and naming a DPO stays T4 and the owner’s act. **The twenty message files are held against each other for the first time**: I18N-002’s parity gate reads the gateway’s static HTML against the gateway’s dictionary and nothing read the application’s own, so a key added to en.json and forgotten in nineteen others was unseen rather than refused. apps/web/src/i18n/dictionary.test.ts pins identical key sets, nothing blank, and every ICU placeholder surviving every translation — a contactBody that lost {address} tells somebody to write to nobody. Verified against PostgreSQL 17.11: 1103 tests over 62 files, none skipped at --maxWorkers=1. Five mutants KILLED with no timeout and every file restored byte-identical, three of them deliberate data defects that prove the new parity suite fires rather than merely runs. **Stated plainly: nineteen of the twenty translations have not been read by a native speaker.** That is I18N-001/T4’s open external blocker and it matters more here than anywhere else, because this text is legally operative; the notice ships in twenty languages because the design asks for twenty, and the read it owes is named rather than implied. T2 is half done and says so — sign-in carries the link, and there is no room to put a footer in. 228 closed.
NEXT: **LEGAL-SG-001/T7 is blocked on [OPS-DEC-03](decisions-log.md#OPS-DEC-03), filed this iteration**: a cross-border transfer assessment is about whether personal data leaves Singapore, so it turns on which AWS region holds it — and no region is settled in INFRA-DEC-03, INFRA-DEC-05, OPS-001 or deploy/, which is a README. MAIL-DEC-01 already removed the other half, since SMTP against the company’s own mailbox means no third party holds an investor’s address. OPS-001’s Terraform needs the same answer before it can name a provider block, so this one unblocks two lanes. **T4 needs the owner** — a named DPO is a person and an address, and the setting is already there to receive it. Next in W3: MAIL-001/T4, T7 and T8, POST-001’s six, RPT-001/T4. MAIL-001/T8 remains the highest-leverage — the SMTP adapter whose credential is the only external residue, unchaining AUTH-003/T3, MAIL-001/T6 and the third surface of [~] SEC-001/T4 — and it is Critical tier (sending mail), so it wants critical-impl. **Owed**: a browser drive of ten admin surfaces and now the notice, which is the first reader-facing page this product serves; the shared development database predates the audit fold, so recreating it is the precondition and it disturbs three live peer servers. Eleven owner decisions are open; two now block a task.

## 2026-09-13 · ADMIN-001/T10 (1 task) · iter 90
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: ADMIN-001/T10 — an admin can correct a person’s name and address, which is the control LEGAL-SG-001 §3 has claimed all along and nothing implemented. Critical tier (a migration fold and the sign-in identifier), so critical-impl built it and this session verified and closed it. **The trail records which fields moved and neither value**: RECORDABLE_FIELDS may name no personal field under any action, so account.correct records name, email or name,email in a table retained seven years — a trail holding both addresses of every correction would keep two personal data points forever about the act of removing one. **Correcting an address destroys every unconsumed invitation and reset token in the same transaction**, because invitations carries both on one table with no kind column and the old mailbox holds a live way in; no session ends, because the person is the same person and their reach has not moved. Verified against PostgreSQL 17.11: **1094 tests over 61 files, none skipped, at --maxWorkers=1, run alone**. Eight mutants KILLED; a ninth — removing the early no-op return — SURVIVED and was kept, because the narrowed UPDATE re-evaluates the same predicates and matches nothing, so the guard is the readable statement of a rule the UPDATE enforces; reported rather than counted. **Two claims of mine were wrong and are corrected**: the Note I wrote on this row at iter 89 said the act would audit both values, which RECORDABLE_FIELDS forbids under any action — it was wrong before it was built; and LEGAL-SG-001/T3’s Evidence still described the correction path as an absence after the runbook stopped saying so. **A gate was blind and now is not**: scripts/check-doc-paths.py could not see any citation containing a Next.js [id] segment — not refused, unseen — and every admin route lives under one; widened to 160 citations, and I broke a citation to prove it now fails rather than trusting the claim. A pre-existing defect went with it: the invite route shape-checked an address with no length bound, so an account could be created at an address AUTH-001 turns away at sign-in; isAddressShaped now serves both surfaces. Filed **ADMIN-DEC-05**: correcting an address becomes a takeover path the day AUTH-003/T3 mails the reset link — correct an admin’s address to your own mailbox, press reset, two audited acts and no refusal between them. 226 closed.
NEXT: **The privacy notice is the next real work** — LEGAL-SG-001/T1 with T2 (linked from the sign-in page and the hall’s footer) and T6 (the backup window disclosed) closing on the same surface, so one batch rather than three. It wants a decision first, not after: the notice is legally operative text and nineteen of its twenty locales would be written by a loop, which is what CMS-DEC-04 refused for content; whether that refusal binds the application dictionary is worth settling before 200 strings exist. T4 needs the owner — a named DPO is a person and an address. **LEGAL-SG-001/T7 is blocked and nobody had noticed**: the cross-border transfer assessment turns on which AWS region holds the data, and no region is settled anywhere — not in INFRA-DEC-03, not in INFRA-DEC-05, not in OPS-001, and deploy/ is a README. File that as a decision next iteration; it gates OPS-001’s Terraform too. Then MAIL-001/T4, T7 and T8, POST-001’s six, RPT-001/T4. **Owed and unverified**: this panel has no test and no browser drive — the shared development database predates the audit fold, so a drive would 500 at the insert, and recreating it disturbs three live peer servers. A peer lane is committing into this clone (628a502, ea8119d); check the push range, not the exit code. Ten owner decisions are open; one blocks a task.

## 2026-09-13 · DECK-001/T6 + LEGAL-SG-001/T3,T5 (3 tasks) · iter 89
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: DECK-001/T6, LEGAL-SG-001/T3 and T5 — the editor writes a section’s speaker note, and the PDPA posture gains the two operator documents it names. **T6 closes the loop iter 88’s gate opened**: a heading’s context could be set only by writing JSON by hand, since T5 closed on stripping it from a reader and T2 built the card that shows it while nothing ever wrote one. The field is offered on a deck’s level-2 headings and nowhere else — a section is a level-2 heading, and a report has no moment where somebody is speaking over it — so the editor is now told the item’s type rather than showing every field every type could have. An empty note and no note are one fact, so clearing removes the field rather than storing an empty string, and that decision lives in apps/web/src/content/blocks.ts:withSpeakerContext rather than in the handler, which is INFRA-DEC-08’s discipline applied the first time it came up: what a component holds is what goes unverified. **The runbooks name only controls that exist.** Revoking a grant has no console surface (DECK-004/T4 open) and the breach runbook says so instead of describing one; MAIL-002/T3 would stop an unsubscribe suppressing a breach notice and is open, so today the enforcement is the person sending. Containment is written before assessment because containment takes minutes and assessment takes hours, and the assessment asks its two questions against DATA-002’s manifest rather than against an impression. **Writing the rights runbook found that the correction right cannot be answered at all**: ACCOUNT_ACTIONS is resend-invitation, reset-password, suspend, reinstate and end-sessions plus delete, and no production code updates accounts.name or accounts.email — so LEGAL-SG-001 §3’s "an admin edits the account" describes a control nobody built. Filed as ADMIN-001/T10, which moved ADMIN-001 off implemented and its PRD row to building; the runbook writes the absence down and gives the two paths that do work. Verified against PostgreSQL 17.11: 1065 tests over 60 files, none skipped at --maxWorkers=1. Four mutants KILLED with no timeout and every file restored byte-identical. **DECK-001 is live** — the first W3 feature to finish. My own errors: a runbook step naming a grant-revocation control that does not exist and another citing an open row as though it enforced something, both caught on a re-read before commit; and a mangled relative path in a task note. 224 closed.
NEXT: W3 in roadmap order from here: **LEGAL-SG-001/T1** — the privacy notice at /privacy in twenty locales — with T2 (linked from the sign-in page and the hall’s footer) and T6 (the backup window disclosed rather than omitted) closing on the same surface, which makes them one batch rather than three. It wants its own iteration and care: the notice is legally operative text, and nineteen of its twenty locales would be written by a loop that CMS-DEC-04 already refused to let machine-translate *content* — whether the same refusal binds the application dictionary is worth settling before writing 200 strings, not after. **T4 needs the owner**: a named DPO is a person and an address, not something a loop can invent. Then MAIL-001/T4, T7 and T8, POST-001’s six, and RPT-001/T4. MAIL-001/T8 stays the highest-leverage — the SMTP adapter whose credential is the only external residue, unchaining AUTH-003/T3, MAIL-001/T6 and the third surface of [~] SEC-001/T4. **ADMIN-001/T10** is small and now blocks a stated compliance path, so it is worth taking early rather than by roadmap depth. Nine owner decisions are open; one blocks a task (CMS-DEC-07 holds CMS-004/T6). Browser drive owed on nine admin surfaces.

## 2026-09-13 · DECK-001/T3 (1 task) · iter 88
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: DECK-001/T3 — a deck’s sections reorder by drag and by keyboard, and the move is an edit of the block array rather than of an order stored beside it. One task, because every candidate beside it needed its own iteration: RPT-001/T4 wants a portfolio read nothing has built, and MAIL-001/T7 wants a mail console page the rail does not name. apps/web/src/content/sections.ts:reorderSections is the move — a section is a contiguous slice, so moving one moves its slice whole and the blocks that come back are the blocks that went in. **The save is the editor’s own write path** (POST /admin/content/<id>/draft), so there is no second route to keep in step. **A route taking a permutation was written and rejected**: positions mean something only against the array they were computed from, so a move sent after another tab saved would apply 1 and 4 to different sections and store a document nobody wrote — and the revision id that would have guarded it does not change, because saveDraft updates an open draft in place. Sending the document back is the weaker failure and the one the console already has everywhere else. **The keyboard is the same operation as the drag**: space picks a card up, the arrows move it while held, space puts it down, escape puts it back, and the arrows move focus when nothing is held — every act announced in a live region, because a reorder is invisible to somebody not watching the list move. Cards are keyed by a number the list mints and carries, which is the constraint iter 87 created and named. Verified against PostgreSQL 17.11: 1059 tests over 60 files, none skipped, at --maxWorkers=1 in 186s. Six mutants KILLED with no timeout and every file restored byte-identical, three of them reddening the pure suite and the write-back case together. **The weakest part of this iteration, stated plainly: no test drives the interaction.** vitest runs environment: node and this tree has no DOM library, so neither the drag nor the key handling is exercised by anything — filed as [INFRA-DEC-08](decisions-log.md#INFRA-DEC-08), since adding one is a new dependency. The browser drive is owed and blocked by measurement rather than assumption: netstat shows three servers listening on 3100, 3131 and 3141, all serving the one apps/web/.next. My own errors: a permutation route built before the concurrency hole was thought through, a render prop passed across the server boundary where functions cannot go, and a test sorting block objects, which compares every one as "[object Object]" and asserts nothing. 221 closed.
NEXT: **DECK-001 is not finished and the gate is what said so**: closing T3 left all five rows closed, which forced the design to declare itself implemented or inert, and neither was true — nothing in apps/web/src writes a heading block’s speaker context. The editor offers no field for it and every other mention reads, validates, strips or renders one, so the overview built last iteration is the first surface that can show a context nobody can set. Filed as **DECK-001/T6**, measured rather than suspected, and it is the next row on this surface. After it, W3 in roadmap order: LEGAL-SG-001’s six rows (the privacy notice in twenty locales, the DPO, the breach runbook — a compliance lane that wants its own iteration and the compliance sources read rather than a regulation asserted from memory), MAIL-001/T4, T7 and T8, POST-001’s six, and RPT-001/T4. MAIL-001/T8 stays the highest-leverage of those: it is the SMTP adapter whose credential is the only external residue, and it unchains AUTH-003/T3, MAIL-001/T6 and the third surface of [~] SEC-001/T4. Nine owner decisions are open; one blocks a task (CMS-DEC-07 holds CMS-004/T6). Browser drive owed on eight admin surfaces.

## 2026-09-13 · CMS-004/T7, DECK-001/T2, DECK-001/T4 (3 tasks) · iter 87
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-004/T7, DECK-001/T2 and DECK-001/T4 — a refusal the store can express, and the deck overview deriveSections has been waiting for. **T7 was filed last iteration from a measurement and is now the thing measured**: publish and withdraw raised 22P02 on an identifier that is not one — a 500 whose message repeats the value supplied (DATA-R02) — and NoResultError on a well-formed one naming nothing. apps/web/src/content/publish.ts:NoSuchItemError is both, because they are one fact: there is nothing here to move a pointer on, and answering them differently tells a caller which identifiers are well-formed. The shape is asked before the value reaches the column, through apps/web/src/content/items.ts:isItemId, which apps/web/src/content/overview.ts also reads — and the mutant that accepts anything reddens apps/web/src/app/admin/content/[id]/publish.test.ts and apps/web/src/content/overview.test.ts together, which is what says it is one rule and not a copy per caller. **DECK-001/T1 closed with deriveSections having only its own test as a caller** — the fifth all-rows-closed-but-inert instance — so nothing in the product ever derived a section. apps/web/src/content/overview.ts:overviewFor and apps/web/src/app/admin/content/[id]/overview/page.tsx are that caller. **The design named /admin/decks/<id>/overview and there is no decks section of the console**: the rail lists seven destinations and none is decks, a deck is a content item, and /admin/content/<id> is already an item’s screen — so a second hub would be one the rail cannot name and nothing links to. DECK-001 §3 now names the hub that exists, and so does DECK-004 §3, which pointed at the same phantom and would have sent W4 to re-derive the answer. A card carries the heading, the first line of prose, and what the section holds in words rather than as a mark (A11Y-R02); a picture is not a line, which is why §3 asks for both. The tally leads the page, because the number is the point. What counts as a word is the decision the count is only honest with: prose a reader receives, never alternative text (a deck of photographs would read as long as one of argument) and never speaker context (served to nobody). Verified against PostgreSQL 17.11: 1052 tests over 60 files, none skipped, at --maxWorkers=1 in 152s. Nine mutants KILLED with no timeout and every file restored byte-identical. My own error: the overview fixture used a figure whose mediaId nothing had stored, which saveDraft rightly refuses — figure counting belongs to the pure suite, and the database fixture now carries no unstored media. 220 closed.
NEXT: W2 is closed but for CMS-004/T6, which waits on [CMS-DEC-07](decisions-log.md#CMS-DEC-07), so the descent is in W3 from here. **DECK-001/T3 is the next row on the surface just built** — reordering by drag and by keyboard, writing back to the block array — and it carries a constraint this iteration created: the overview renders cards keyed by index, which is correct for a list that does not move and wrong the moment one does, so T3 needs a client component with a key that survives a reorder. A section has no stored identity (the block array is the single source), so that key has to be derived and held for the life of the drag rather than read from the data. The rest of W3 in roadmap order: DECK-001/T3, then LEGAL-SG-001 (six rows, none blocked), MAIL-001/T4, T7 and T8 — T8 being the SMTP adapter on nodemailer whose credential is the only external residue, and the row that would unchain AUTH-003/T3, MAIL-001/T6 and the third surface of [~] SEC-001/T4 — then POST-001’s six and RPT-001/T4. Owed and still in the ledger: a browser drive of seven admin surfaces now, this overview included; three servers on this box serve the one apps/web/.next and Turbopack refuses an isolated build. Eight owner decisions are open; one blocks a task. **Not a defect but worth knowing**: apps/web/src/content/grants.ts has no production caller either, and that is honestly recorded rather than hidden — CMS-006/T3 closed on the mechanism and named DECK-004 as the surface, whose T2, T4, T5 and T6 are open in W4.

## 2026-09-13 · RPT-001/T5, POST-002/T5, RPT-002/T5 (3 tasks) · iter 86
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: RPT-001/T5, POST-002/T5 and RPT-002/T5 — three rows, one surface: the sentences the item screen is allowed to say before an act. apps/web/src/content/audience.ts:changeAudience had shipped with its row lock, its no-op guard and its content.audience_change audit all proven at CMS-006/T6 and **no production caller**, so an item held whichever audience createItem was given and nothing in the product could change it. The control (apps/web/src/app/admin/content/[id]/audience-control.tsx) and its route are that caller, and the work was the words rather than the write. **Public says what public means**: anyone, including a competitor, no sign-in, linkable and indexable — not the name of an audience an admin has to decode. **Narrowing says what it cannot recall**: a page somebody has open stays on their screen, and a copy fetched while the item was public may sit in a browser for ten minutes. **That ten minutes is now one constant** — apps/web/src/content/audience.ts:PUBLIC_CACHE_SECONDS, which apps/web/src/app/content/[id]/route.ts reads instead of declaring a second 600 — and the mutant that matters moved it to 900 and reddened the new suite and apps/web/src/content/content-serve.test.ts together, which is what says the window an admin is told and the window a reader is granted are one number. **The grant count is the fact that changes an answer**: narrowing to granted with nothing granted turns a document every investor could read into one nobody can, so the confirmation counts and says so. For a report, withdrawal now states the period becomes a gap and which report becomes current — **but only when there will be a gap**: RPT-002 §3 assumes there always is, while withdrawing moves the pointer to the revision published before, so a report on its second revision falls back to its first and keeps its period. Read rather than assumed. Verified against PostgreSQL 17.11: 1026 tests over 59 files, none skipped, at --maxWorkers=1 in 152s, with 15 new cases on a database of its own. Eight mutants KILLED with no timeout and every file restored byte-identical: the direction of a move inverted, a cache window claimed for every item, the grant count always zero, the shared constant moved, the vocabulary check dropped, a missing item not refused, a gap always promised, and the successor query without its self-exclusion. My own error: the first probe of a sibling route ran without a session, so requireAdmin answered before any query and the reading proved nothing — re-run with an admin cookie it raised 22P02. 217 closed.
NEXT: **CMS-004/T6 is blocked, not skipped, and the reason is a contradiction between designs.** CMS-004 §3, CMS-006 §3 and POST-002 §3 all say a public document may sit in a CDN and the cache is purged on publish and withdraw; OPS-001 §3 settles that Cloudflare caches the assets and never the documents, because every route renders per request for the CSP nonce. So there is no cache of a document to purge, and the one that does hold a public copy is the reader’s own browser, which no server reaches. Two doctrine clauses conflicting with neither precedence case applying is the owner’s to settle (§1.11), so it is [CMS-DEC-07](decisions-log.md#CMS-DEC-07) — keep the ten minutes, revalidate with an ETag, or build the purge and reverse OPS-001 — with the shipped lifetime as the safe default and the recommendation resting on one concrete thing: under the first answer the withdrawal confirmation has to end "and a reader who already opened it may still see it for up to ten minutes". CMS-007/T5 and T6 now carry their real blocker rather than looking actionable: CMS-007 §2 puts the field above the stream and §4 names INV-001 as its surface, and no hall page exists. **The next buildable in W2 is CMS-004/T7, filed this iteration from a measurement rather than a suspicion**: publish and withdraw raise 22P02 on an identifier that is not one, a 500 whose message repeats the value supplied, which CMS-006 §6 says a route must not do; the audience route answers that as a 404 already, so the shape is settled and the two siblings have to catch up — it wants a refusal the store functions can express rather than a shape check copied into each route. Owed and still in the ledger: a browser drive of six admin surfaces now, this audience panel included. Three servers on this box serve the one apps/web/.next and Turbopack refuses an isolated build, so it waits for a free window or a distDir the environment can point elsewhere. Eight owner decisions are open; one now blocks a task.

## 2026-09-13 · CMS-004/T1,T2 (2 tasks) · iter 85
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-004/T1 and T2 — a draft can be previewed the way a reader will get it. Two iterations deferred this because preview must render "through the reader’s own components" and nothing rendered a block outside the editor’s field widgets; the resolution is that a renderer built inside the preview is the thing the design forbids, so apps/web/src/content/render.tsx is shared content machinery every reading surface goes through, with RPT-003/T1 and DECK-003/T1 as its next callers — both are page layouts over rendered blocks rather than renderers. Marks are offsets rather than nesting, so a paragraph is cut at every mark boundary and each piece wrapped in the elements its marks name; that splitting existed only inside the translation module, and apps/web/src/content/marks.ts:piecesOf is now where both callers reach it. **The audience is evaluated, never bypassed, and through the rule the reader’s own query composes**: apps/web/src/content/access.ts:visibleTo is now built from an audienceAdmits clause rather than beside one, so a preview asks the same question a reader’s read asks minus the published check it deliberately relaxes. Previewing an investor-only item as a visitor shows nothing, which is the answer an admin needs. A role is a generic member of its kind, so a granted item admits no investor and the admin’s own view is the default — which is how they read a granted item at all. T2 is structural rather than a new check: the preview is a page under /admin, so requireAdminPage gates it before it renders, and the only two parameters are each checked against a closed set with anything else a 404, so no crafted URL widens what is shown and there is no token for one to go in. Verified against PostgreSQL 17.11: 1011 tests over 58 files, none skipped at --maxWorkers=1, with 12 pure render cases and 11 preview cases. Six mutants KILLED with no timeout and every file restored byte-identical: the audience bypassed, the published revision read instead of the latest, a generic investor admitted to a granted item, the same clause widened under the reader’s own predicate, a mark covering the whole paragraph, and an image losing its alternative text. **The fourth is the one worth keeping**: widening the shared clause failed four cases in read.test.ts as well, which is what says the clause is one rule rather than a copy that happens to agree today. Three casts were written and removed rather than shipped — localeFor’s parameter narrowed to the two columns it reads, a rest-object that claimed to be a row, and a locale narrowed instead of asserted; each was a claim about data the code had not fetched. My own error: the first render test asserted a bare link where the overlap genuinely nests both marks, and the renderer was right. 214 closed.
NEXT: CMS-004 has one row left: T6, a public item’s cache purged on publish and withdraw. The cache is a Cache-Control lifetime on apps/web/src/app/content/[id]/route.ts rather than anything this application holds, so a purge means a CDN and a credential — build-ahead territory: a port, a default that does nothing but say so, and the lifetime as the bound on staleness meanwhile. Then CMS-007/T5 and T6, which want a search field in a room whose reader pages are W4 — worth checking whether the field belongs to the console instead, because the search read itself is closed and proven. **The renderer landing changes the W4 picture**: RPT-003/T1 and DECK-003/T1 are now page layouts over something that exists rather than greenfield, which makes the W3/W4 reading views considerably cheaper than the roadmap’s depth suggests. Owed and in the ledger: a browser drive of five admin surfaces now — the translation review screen, the content list, the settings screen, the item screen and this preview. Three servers on this box serve the one apps/web/.next and an isolated build is refused by Turbopack, so it waits for a free window or a distDir the environment can point elsewhere. Seven owner decisions are open; none blocks a task.
