# dev1 iteration log

> **Not for `main`.** This file lives on `development` only (`.claude/CLAUDE.md` §1.1).

Working state, not an archive. Each iteration of `/dev1` appends one entry and
prunes the oldest until **twenty** remain — the bound is held by
`scripts/check-log-retention.py` and the reason it is safe is §13.1: an entry
is deleted only once every durable fact in it already lives in its permanent
home, which is `docs/tasks.md` for task state, `docs/decisions-log.md` for a
choice, and `docs/operator-checklist.md` for something waiting on a person.
`git log -p docs/dev1-iter-log.md` is the archive, so a pruned entry is
recoverable verbatim and no second copy is kept here.

A cold start reads the tail of this file to find out where the last one
stopped. `OUTCOME:` is fixed vocabulary so that reading can be mechanical:
`CLOSED` when at least one task moved to `[x]`, `IDLE` when the queue was
stale and the loop re-fires, `STOP` when it ended and the owner is needed.
`REASON:` on a non-`CLOSED` entry is one stable phrase, reused verbatim when
the same condition recurs, so three of them in a row can be detected rather
than noticed.

---

## 2026-09-10 · CMS-003/T1,T4,T7 (3 tasks) · iter 39
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the media store lifecycle (CMS-003/T1,T4,T7), a new apps/web/src/content/media.ts. sniffType reads the type from the bytes — png/jpeg/webp/pdf signatures and an svg XML-root check past a byte-order mark, an xml declaration and comments — and refuses anything else, never the filename or declared content type (SEC-001). storeMedia stores bytes under their sha256 with onConflict-do-nothing, so a duplicate is one row and a concurrent store of the same bytes settles on one row rather than erroring. deleteMedia refuses while any media_refs row exists (naming the items) and audits media.delete otherwise; because media_refs.media_id is ON DELETE CASCADE the database would cascade a referenced delete rather than refuse it, so the media row is locked FOR UPDATE (conflicting with the FOR KEY SHARE a media_refs insert takes) and no referenced file is deleted. T2 (re-encode) and T3 (SVG sanitise) stay CMS-DEC-03-blocked; the upload and serve routes are their surface. Standard tier, backend, content/ module (exempt from the content-access gate, which T5's serve join will need). Verified against PostgreSQL 17.11 by apps/web/src/content/media.test.ts on its own database: 458 tests (was 447 at batch start), typecheck clean, make check green. Mutations: sniff-returns-a-type (refuse test), remove-dedup (duplicate + concurrent tests), never-refuse-referenced (referenced + race tests), remove-audit (audit test) — each killed a named test; removing the FOR UPDATE lock did NOT deterministically redden the warmed race (an asymmetric delete-vs-insert race's bad interleaving is narrow), so the lock rests on the structural pattern and a deterministic lock test is owed. 139 closed.
NEXT: CMS-003 is 3/7. Remaining: T2,T3 (CMS-DEC-03-blocked re-encode/sanitise); T5 (GET /media/<id> serve route, joining media_refs to content_items and composing CMS-006's audience predicate, 404 on no match) and T6 (cache headers follow the audience — public cacheable, gated no-store) — a serve ROUTE enforcing CMS-R06/SEC-R01 access control on every request, the natural next CMS-003 work, needing CMS-006's predicate wired into a route. Other clean W2 backend: CMS-006/T4 (the shared 404) + T6 (narrowing audited, public short-cache), CMS-005/T3 (wants a translator). W1's admin-console UI (ADMIN-001/T2,T4,T6; ADMIN-002/T3), forms (I18N-DEC-02) and mail (MAIL-001/T1) stay blocked. 8 open owner decisions. Owed: a deterministic FOR UPDATE lock test for CMS-003/T7 (the warmed race did not mutation-kill it).

## 2026-09-10 · CFG-001/T2,T3,T4 (3 tasks) · iter 38
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the runtime-config write path (CFG-001/T2,T3,T4). apps/web/src/config/settings.ts gained validateSetting (type + bounds, refusing with the range rather than clamping — session.max_age_days 1-90, signin.rate_per_hour 1-1000, the two text lines at 280 chars, the bounds in the SETTINGS registry), changeSetting (validate, then in one transaction capture the current value or the declared default as previous_value, write the new value and changed_by, and audit config.change) and revertSetting (swap value and previous_value under a row lock, audit; reverting twice returns to the start; a no-op for a key with no previous value). The config row carries the key, both values, who and when — which a revert reads; the audit's before/after stay null under the SEC-DEC-01 safe default (and subject_id is a uuid the string key cannot hold), so the audit attests the act while the config row holds the detail, until SEC-002/T4. Standard tier, backend: the PUT/POST routes and the console settings page are the surface these await (no CFG-001 task names them; reached via ADMIN-002's /admin/config). Verified against PostgreSQL 17.11 — 447 tests (was 437), typecheck clean, make check and check-app green, four mutants each killed a named test (the max bound dropped, previous_value written as the new value, changeSetting's audit removed, the revert not swapping previous). 136 closed.
NEXT: W1 stays a blocked frontier for clean closure — its admin-console UI is caller-blocked (ADMIN-002/T3's destructive-action component has no built caller per §1.10; ADMIN-001/T2 the person page needs decks and the I18N-DEC-02-blocked session list AUTH-004/T2), its mail waits on the unbuilt MAIL-001/T1 Mailer port (AUTH-003/T3), and its forms wait on I18N-DEC-02 (AUTH-001/T4, AUTH-003/T4, AUTH-004/T2). The roadmap's "W1: 6 buildable" OVERCOUNTS by ~4: generate-roadmap reads frontmatter depends_on, not the task-level blockers stated in design §3/§4 prose, so AUTH-003/T3 (needs MAIL-001/T1) and AUTH-003/T4 (needs I18N-DEC-02) show [ ] buildable when they are not — a future iter could mark them [!] for honesty (T4 also needs I18N-DEC-02's Blocks to name it). This iter descended to W2's config backend. Next clean W2 backend is likely CMS-003/T1 (upload sniffs type from bytes and refuses) and T4 (content-hash storage), buildable without CMS-DEC-03 (which blocks only the re-encode T2/T3). 8 open owner decisions unchanged.

## 2026-09-10 · ADMIN-001/T1 (1 task) · iter 37
STATUS: green · TIER: Critical · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the account list (ADMIN-001/T1) — the first admin UI page and a Critical schema+auth slice, built by critical-impl and reviewed by deep-review. apps/web/migrations/1788744617066_accounts.sql folds `last_sign_in timestamptz` (nullable) into the accounts CREATE (DATA-R07: db.test.ts parses the CREATE, a fresh database is what deploys) and narrows the updated_at trigger to a WHEN on the defining attributes (email, name, role, state), so a sign-in does not restamp updated_at — matching the tested portfolio precedent, folded now because the file freezes after deploy. apps/web/src/app/api/auth/sign-in/route.ts stamps last_sign_in from the database clock on the one success path, before issue so no session is issued without a fresh value behind it. apps/web/src/admin/accounts.ts:listAccounts reads the five shown columns ordered last_sign_in asc nulls first then address; apps/web/src/app/admin/accounts/page.tsx renders the a11y table gated by the /admin layout, English. deep-review (yellow) confirmed the code sound by re-running every instrument but caught three now-false docs that §4 Step 4 requires true in the same commit — data-001's DATA-R01 minimisation sentence and its accounts column table, admin-001 §5's "nothing else", legal-sg-001's purpose cell — plus the route.ts atomicity claim (three interleavings, not two) and accounts.ts's docstring counts (two→four acts) with a pre-existing "nothing restores active yet" that reinstate/T8 had already falsified; all fixed here. Verified against PostgreSQL 17.11: migrate-roundtrip green, 437 tests pass (was 427 at the batch start), typecheck clean, seven mutants each killed a named test (the updated_at WHEN proven this iter — removing it reddens exactly one test and no other), browser-driven by critical-impl (200, five rows stalest-first, 0 console/CSP errors, hydration confirmed under the nonce). make check and make check-app green. 133 closed.
NEXT: ADMIN-DEC-02 filed OPEN — is the list "sortable" by a control or served already sorted by last sign-in; safe default is the served order, Revises ADMIN-001/T1, recommendation the served order. Open owner items are now 8: SEC-DEC-01, AUTH-DEC-02, OPS-DEC-02, I18N-DEC-02, AUTH-DEC-04, ADMIN-DEC-01, ADMIN-DEC-02, CMS-DEC-03. One residue a future iter owes: the fold convention (an ALTER plus teaching db.test.ts the effective schema) has no register home and must be filed when OPS-001 makes migrations immutable — flagged at iter 29 and again here, carried in session memory. The W2 descent past the exhausted clean backend runs into UI (ADMIN-001/T2 person page, T4 delete confirmation, T6 creation; CMS-002 block editor; CMS-004/T1,T2,T4; CMS-005/T4,T6), all needing browser verification; routes that do not exist yet (CMS-006/T4,T6; CMS-003/T5,T6); a translator (CMS-005/T3); and a Critical migration (CMS-007/T1 tsvector). ADMIN-001/T2, the person page, is the natural next — built on today's list read and the same /admin shell.

## 2026-09-10 · CMS-004/T3,T5 (2 tasks) · iter 36
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: publish and withdraw, closed by cross-reference (CMS-004/T3,T5). apps/web/src/content/publish.ts:publish and apps/web/src/content/publish.ts:withdraw were built and tested when the read path needed published pointers, and the ledger had not caught up. publish takes an explicit revision, re-validates its blocks, stamps published_at, moves content_items.current_revision_id and records content.publish in one transaction; withdraw moves the pointer to the previous published revision or to null, records content.withdraw, and refuses an item that shows nothing — the withdrawn revision stays on disk so re-publishing is moving the pointer forward again (SEC-R04, CMS-R01). No new code (§1.10): apps/web/src/content/content.test.ts's publish-and-withdraw describe already proves both — pointer moved, invalid body refused, withdraw to previous then to nothing, refusal of an item showing nothing — re-run green this iter (4 pass). Standard tier, docs-only. make check green, full suite unchanged at 427. 132 closed.
NEXT: the clean W2 backend is exhausted. What remains in W2 is UI (ADMIN-001/T1,T2,T4,T6 the account console, CMS-002 the block editor, CMS-004/T1,T2,T4 preview and confirmation, CMS-005/T4,T6 the review grid), routes that do not exist yet (CMS-006/T4 the 404, and the serve/cache of CMS-006/T6 and CMS-003/T5,T6), owner decisions (CFG-001 on SEC-DEC-01, CMS-003 on CMS-DEC-03), a translator (CMS-005/T3), and one Critical migration (CMS-007/T1 the tsvector). Per the roadmap the next clean buildable is a W2 UI task — ADMIN-001/T1, the account list, is English-only staff UI and unblocked, and would be the first iter to drive the built server in a browser (§17); the alternative is descending to W3's clean backends, the LEGAL-SG-001 and LEGAL-GLOBAL-001 posture documents and MAIL-001's Mailer port. OPEN owner items steady at seven: ADMIN-DEC-01, SEC-DEC-01, AUTH-DEC-02, OPS-DEC-02, I18N-DEC-02, AUTH-DEC-04, CMS-DEC-03.

## 2026-09-10 · CMS-005/T1,T2,T5 (3 tasks) · iter 35
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the locale serve (CMS-005/T1,T2,T5). apps/web/src/content/locales.ts:localeFor takes a revision and a requested locale and returns the blocks to show, the locale they are in, and whether that is a fallback. It filters content_locales on state = 'reviewed' (T1 — the state is a column the query reads, not an inference from a null reviewed_at), so a machine draft is served to nobody (T2, CMS-R05): a locale with only a machine row falls through exactly as one with no row. The order is the design's — the exact locale, then the language without its region (fr-CA takes a reviewed fr), then the authored English — and a script is never another's fallback (zt never takes zh). When none is reviewed it returns the revision's own English blocks with fellBack true, so the reading view can say the reader is reading English because their language is not yet ready (T5, I18N-R04, I18N-DEC-01, which is resolved). It is the serve side; the reading views that pass the reader's locale and the drafting that writes the rows (CMS-005/T3, which wants a translator) are separate. Standard tier, content module so content_locales may be named. Verified against PostgreSQL 17: the locale suite 6 pass, the full suite 427 pass, typecheck clean, make check and make check-app green. Mutation-proved four ways — removing the state filter (a machine row served), removing the region-strip, suppressing the fallback flag, and flagging a reviewed row as a fallback — each reddening a named test. 130 closed.
NEXT: CMS-005 is 3/6 — T3 (drafting, which wants a translator), T4 and T6 (UI) remain. Likely next clean work: CMS-004/T3 and T5 (publish and withdraw) — apps/web/src/content/publish.ts already exists, so these may close by cross-reference once read; and CMS-007/T1 (a generated tsvector column) is backend but a migration. CMS-006/T4 and T6 want a content serve route that does not exist; CMS-003/T1,T4 are entangled with CMS-DEC-03; ADMIN-001 and CFG-001 are UI- or owner-blocked. The descent is nearing the point where the remaining W2 work is UI, routes, or owner decisions. OPEN owner items unchanged at seven: ADMIN-DEC-01, SEC-DEC-01, AUTH-DEC-02, OPS-DEC-02, I18N-DEC-02, AUTH-DEC-04, CMS-DEC-03.

## 2026-09-10 · DATA-002/T4 + OPS-002/T6 (2 tasks) · iter 34
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the retention sweep and the three-alerts runbook, completing DATA-002 (5/5) and OPS-002 (6/6). apps/web/scripts/retention-sweep.mjs deletes what each §3 window has outlived — expired sessions, consumed or expired invitations, mail_log past two years — reporting the counts; audit is excluded (append-only, DATA-R09) and content_revisions kept forever (CMS-R01). It is the operational sibling of the backup and roundtrip scripts, runnable as make retention and npm run retention (verified printing 0 of each against the empty development database); the cron that runs it is OPS-001's, like the backup's. Proven by apps/web/src/db/retention-sweep.test.ts against a database it creates for itself, so the whole-database sweep cannot reach a parallel suite's rows: each window's expired row goes and its live one stays, asserted by which row survives (a token_hash, a subject) so a sign flip is caught, and the audit trail is untouched; mutation-proved three ways. docs/runbooks/ops-002-alerts.md defines the three alerts each with its action — site down (the static main is the fallback), errors up (read by the request_id T2 now carries), a scrubber hit (fix the caller, the one alert that fires today, T4) — and states plainly that until OPS-001 adds a metrics backend nothing watches the first two, and mail delivery waits on MAIL-001. Descending past CMS-003 (media), its re-encode and SVG libraries were an unfiled new-dependency decision, now filed as CMS-DEC-03 (recommending jimp and refuse-SVG); CMS-003/T2,T3 are flipped to blocked on it. Standard tier. Verified against PostgreSQL 17: retention suite 4 pass, full suite 421 pass, typecheck clean, make check and make check-app green. 127 closed.
NEXT: DATA-002 and OPS-002 are complete. Clean W2 backend is thin now: CMS-005 locale-state (T1 the rows, T2 the machine row unreachable) is content-module backend and the likely next; CMS-006/T4 and T6 need a content serve route that does not exist; CMS-003/T1 and T4 (sniff, content-hash) are buildable but T2 and T3 wait on CMS-DEC-03 and T1 accepting a raster is unsafe without T2's re-encode. ADMIN-001/T1,T2,T4,T6 and CFG-001 remain UI- or owner-blocked. OPEN owner items now seven: ADMIN-DEC-01, SEC-DEC-01, AUTH-DEC-02, OPS-DEC-02, I18N-DEC-02, AUTH-DEC-04 and CMS-DEC-03 — every one shipping a fail-closed safe default.

## 2026-09-10 · OPS-002/T2 + ADMIN-001/T5 (2 tasks) · iter 33
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the edge request id, and the deletion-refusal rule closed by cross-reference (OPS-002/T2, ADMIN-001/T5). apps/web/src/proxy.ts mints a uuid request id per request (apps/web/src/ops/request-context.ts:newRequestId) and sets it on the request it hands the render and on the response. apps/web/src/ops/request-context.ts carries it to the logger through an AsyncLocalStorage, because a Next proxy runs before the handler in a separate async context and cannot hand it over directly: withRequestId wraps each of the three api routes and opens the context from the x-request-id header, currentRequestId reads it, and apps/web/src/ops/logger.ts now reads it in place of the null it returned while deferred. A line logged outside any request still carries null, the honest value. This un-defers the iters-27-and-28 currentRequestId stub and satisfies OPS-R01 — a request id on every structured line. ADMIN-001/T5 (deleting the last admin or yourself is refused) closed by cross-reference: the refusal is iter-32's eraseAccount guard (DATA-002/T2), which ADMIN-001 is the admin half of; the delete UI that presents it is ADMIN-001/T4, unbuilt. Standard tier, main session. Verified against PostgreSQL 17: the request-context, logger and proxy suites 61 pass, the full suite 417 pass, typecheck clean, make check and make check-app green. Mutation-proved OPS-002/T2 six ways — currentRequestId null, withRequestId ignoring the header, newRequestId constant, the logger ignoring the context, and the proxy omitting the request or the response header — each reddening a named test. 125 closed.
NEXT: clean W2 backend frontier: OPS-002/T6 (the three alerts — the scrubber one fires; site-down and errors-up want a watcher, so build-ahead to a runbook with OPS-001 wiring delivery and MAIL-DEC-01 for mail); DATA-002/T4 (retention sweep — sessions, invitations and mail_log deletions, audit append-only and excluded, the cron OPS-001's); CMS-003 media backend (T1 sniff-by-bytes, T4 content-hash storage; T2 and T3 want an image and an SVG library, a dependency decision). ADMIN-001/T1,T2,T4,T6 are admin console UI needing the built server in a browser. OPEN owner items: ADMIN-DEC-01 (safe default in force), SEC-DEC-01, I18N-DEC-02, and the schema-evolution convention.

## 2026-09-10 · DATA-002/T2,T5 (2 tasks) · iter 32
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the erasure function and the content that survives it (DATA-002/T2,T5). apps/web/src/admin/accounts.ts:eraseAccount deletes the accounts row in one transaction and lets the foreign keys carry the deletion outward exactly as the manifest declares (T1) and its gate proves (T3): what is about the person cascades — the sessions they hold, the outstanding invitation, mail_log, unsubscribes, the grants they were given — and what they did on the company's behalf is set null — a revision's author_id, a grant's granted_by, a translation's reviewed_by, a file's uploaded_by, the board's updated_by, a config change's changed_by. It joins suspend/changeRole/reinstate and takes their shape, with a guard weightier because erasure has no inverse (ADMIN-DEC-01): the self and last-active-admin cases are refused before any write. No session is ended by hand — the cascade to sessions signs the person out — and the account.delete audit outlives its subject (bare-uuid actor and subject, SEC-002) in the same transaction (DATA-R03: a real delete, the trail minimal). T5 is the set-null half: content_revisions.author_id SET NULL keeps the revision with a null author, and the content_grants split (account_id cascade versus granted_by set null) shows both dispositions on the one table that carries both. Tier Medium per VALOTech §15 (personal-data handling plus a privacy review — not Critical: no migration, existing schema), main session. Verified against PostgreSQL 17: accounts.test.ts 39 pass, the full suite 406 pass with a database, typecheck clean, make check and make check-app green. Mutation-proved against real PostgreSQL, each reddening a named test — and a no-match -t was confirmed to exit 0, so the kills are genuine failures and not empty runs: the self guard removed, the last-admin guard removed, the audit action mislabelled, author_id flipped to CASCADE, granted_by flipped to CASCADE. 123 closed.
NEXT: DATA-002 is 4/5; T4 (scheduled deletion per retention window — audit 7y, mail_log 2y, sessions and invitations to expiry) wants a scheduler this repository does not yet run, so it build-aheads to the deletion queries with the schedule as OPS-001 infra residue. eraseAccount now carries ADMIN-001/T5's backend (self and last-admin refused) and is the mechanism ADMIN-001/T4's delete surface will call — both close by cross-reference once that admin UI lands. Clean W2 backend still open: OPS-002/T2 (the edge request id — the currentRequestId deferral, which needs a Next-16 answer for carrying the id from proxy to handler) and CMS-003's media backend (T1 sniff-by-bytes, T4 content-hash storage; T2/T3 want an image and an SVG library, a dependency decision). OPEN owner items unchanged: ADMIN-DEC-01 (safe default in force), SEC-DEC-01, I18N-DEC-02, and the schema-evolution convention.

## 2026-09-10 · DATA-002/T1,T3 (2 tasks) · iter 31
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the erasure manifest and the gate that proves it (DATA-002/T1,T3). The manifest is the table in docs/designs/data/data-002-erasure-and-retention.md §3 — it already held every table, what each holds about a person, and what erasure does to it; T1 closes because that is now verified complete and exact rather than asserted. scripts/check-erasure-manifest.py (T3) parses that table and the CREATE TABLE statements under apps/web/migrations and holds three invariants: every created table is declared (a new table cannot ship with its erasure undecided); every accounts reference's ON DELETE is the disposition the manifest names (a cascade the manifest calls set null, or the reverse, is caught here, not during an erasure); and a table declared to hold nothing personal has no column carrying a personal-data token — email, name, phone, address, ip — matched against underscore-delimited tokens, not substrings, because ip as a substring hits recipient and description; a real misfire is answered by a named EXEMPTIONS line, never a looser pattern. It parses the migrations, not a live database, so it runs in CI with no connection, like apps/web/src/db/db.test.ts. Wired as make check-erasure in the make check aggregate; reports 14 created tables and 11 accounts-references, all matching, exit 0. Standard tier, main session. Mutation-proved: a missing table row, a swapped disposition, a personal column exposed in a holds-nothing table, and accounts no longer declared deleted each redden the gate, restored byte-identical after. make check and make check-app green. 121 closed.
NEXT: DATA-002 is 2/5. T2 (the one erasure function driven by the manifest) and T5 (content authored by an erased account survives with a null author) are buildable now — the schema's on-delete already matches the manifest, so the function is the transaction that deletes the accounts row and lets the foreign keys do the rest, with the account.delete audit recording the action under SEC-DEC-01's safe default; T4 is the scheduled deletion that enforces each retention window. T2 is Critical-tier (a hard-delete path), so it takes critical-impl plus deep-review. OPEN owner items unchanged: ADMIN-DEC-01, SEC-DEC-01, I18N-DEC-02, and the schema-evolution convention.

## 2026-09-10 · CFG-001/T1,T5,T6 (3 tasks) · iter 30
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the runtime-config read path (CFG-001/T1,T5,T6). apps/web/src/config/settings.ts:SETTINGS is the registry — the five keys CFG-001 names, each with a type and a default declared in code (T1), distinct from the boot-time environment reader apps/web/src/config/index.ts. apps/web/src/config/settings.ts:Settings reads through a five-second-refresh cache (T5): an absent key returns its default (an empty table is a working application), a stored value is parsed to the key's type, a corrupt integer fails safe to the default; the clock is injected so a test crosses the refresh without waiting. The accessor refuses a secret-shaped key before any read (T6, isSecretShaped) so config can never hold a credential (SEC-R05). The config table already existed in the platform migration — no schema change, no audit write, so no SEC-DEC-01 entanglement, which is why the read path closes clean while the write and audit tasks do not. Standard tier, main session. Foundational: no consumer reads runtime config yet (AUTH-002 and SEC-001 read the environment), which the design names as future readers. Verified against PostgreSQL 17: full suite 397, make check and make check-app green (the registry and guard tests run in CI without a database). Mutation-proved: the secret-guard, the secret pattern, the empty-table default, the cache refresh, and the boolean parse — five mutants, each reddening a test. 119 closed.
NEXT: CFG-001 is 3/6. The write path — T2 (validate against type and bounds, refusing not clamping), T3 (change in one transaction, audited with both values), T4 (revert) — is blocked on SEC-DEC-01 for the audit's before and after values, though the change and revert mechanism itself is buildable with the audit recording the action alone (build-ahead). The clean W2 backend body remaining is DATA-002 (the erasure manifest and its schema gate). ADMIN-001's remaining tasks stay entangled (UI, DECK-004, ADMIN-002/T3, or the schema-evolution convention flagged last iter). OPEN owner items: ADMIN-DEC-01, SEC-DEC-01, I18N-DEC-02, and the schema-evolution convention.

## 2026-09-10 · ADMIN-001/T8 (1 task) · iter 29
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: reinstating a suspended account (ADMIN-001/T8). apps/web/src/admin/accounts.ts:reinstateAccount flips state suspended->active in one transaction — UPDATE ... WHERE state='suspended' RETURNING is the idempotency check — and records one account.reinstate audit row, rolling back together on failure (SEC-R04). The inverse of iter-26's suspendAccount and simpler: no session or invitation restored (suspension deleted them; the person signs in afresh), and no guard — reinstating only adds to the admins who can sign in, so it cannot strand the room, and a suspended holder cannot sign in to call it. account.reinstate joined the closed audit vocabulary by FOLDING it into the audit CREATE (the platform migration) and AUDIT_ACTIONS: db.test.ts is parse-only and proves the two equal, node-pg-migrate does not checksum, and dev DBs are disposable (the accounts suite recreates its own), so folding is the pre-deploy-safe edit here rather than an ALTER a CREATE-parsing test could not see. Standard tier, main session. Verified against PostgreSQL 17: full suite 390, make check and make check-app green. Mutation-proved: the state='suspended' predicate, the audit write, and the action name — three mutants, each reddening a test. 116 closed.
NEXT: ADMIN-001 is 4/8, the rest entangled — T1 (list) needs a stored last_signed_in_at the accounts schema lacks (a column plus a write in the sign-in path) and a list page; T2 (person page) needs DECK-004's grants (W4) for its Access section; T4/T5 (delete and guard) need the unbuilt ADMIN-002/T3 confirmation component; T6 (invite) is a console form. A CONVENTION QUESTION blocks any column or CHECK change to an existing table: db.test.ts parses that vocab from the CREATE body, so evolving a shipped table means folding its CREATE (pre-deploy-safe, as done here) or a new ALTER migration plus teaching db.test.ts to read the effective schema — the latter becomes required once migrations are immutable at deploy (OPS-001 / INFRA-DEC-03). Flagged for the owner. The clean W2 backend body is CFG-001 (config accessor and validation; its change-audit T3 touches SEC-DEC-01) and DATA-002 (erasure manifest and gate).

## 2026-09-10 · OPS-002/T4,T5 (2 tasks) · iter 28
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the logger's scrubber alert and the health endpoint (OPS-002/T4,T5). T4: apps/web/src/ops/logger.ts:emit raises a second error line, event log.scrubbed, whenever apps/web/src/ops/logger.ts:scrubFields masks a field value or the message — naming the source event and the masked field names, never a value; a log.scrubbed line never raises its own (the SCRUBBED_EVENT guard), so a hit costs one extra line, not a cascade. The point is that a scrubber hit means a caller logged something it should not, and the fix is that caller. T5: apps/web/src/app/health/route.ts answers a health check with { ok, version, db } and nothing else — db a real select 1 (a pool status would call a stopped database healthy), a failed query answering 503, version from the optional BUILD_VERSION the deploy stamps (default unknown, read through apps/web/src/config/index.ts), never cached (force-dynamic plus no-store). BUILD_VERSION is the fourteenth declared variable, in config and env.example, held equal by INFRA-001/T2. Standard tier, main session. Verified against PostgreSQL 17: full suite 384, make check and make check-app green. Mutation-proved: the alert (removed, field and message detection dropped, source event mis-named) and the route (status forced 200, the real query skipped, the version mis-read, no-store dropped) — eight mutants, each reddening a test. 115 closed.
NEXT: OPS-002 is 4/6 — T2 (the request id minted at the edge in proxy.ts and carried through a request, which needs a context a Next proxy cannot share with its handler) and T6 (the three alerts) remain; T6's conditions are already in the design but its firing and delivery wait on OPS-001 (a monitoring backend) and MAIL-DEC-01 (delivery), so it is a build-ahead runbook rather than a clean close. The buildable body is W2 backend — CFG-001 (runtime config), DATA-002 (erasure), CMS-003 (media), and the ADMIN-001 delete slice.

## 2026-09-10 · OPS-002/T1,T3 (2 tasks) · iter 27
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the one structured logger (OPS-002/T1,T3). apps/web/src/ops/logger.ts:log writes JSON lines to stdout — { ts (UTC), level, event, request_id, msg, ...fields } — with event constrained to a closed LOG_EVENTS vocabulary and fields typed to primitives, so a whole object cannot be handed in to serialise (the structural half of DATA-R02); apps/web/src/ops/logger.ts:scrub is the backstop, masking an address and any 40+-char base64url run (a token is 43, a SHA-256 hash 64) in every string value and the message, while a 36-char UUID account id stays readable. debug is off when app.env is production. The vocabulary's first member is db.pool_error, emitted by apps/web/src/db/index.ts:handlePoolError — the pool error listener that logs a structured line and exits, which clears the Deferred: OPS-002/T1 marker db/index.ts carried and gives the logger a real caller. No console.* call exists in non-test source, checked by a comment-stripping scan in the test (itself proven to catch a real console.log). request_id is a null slot pending T2. Standard tier, main session. Verified against Node (no database): full suite 377 (the logger's 11 run in CI too, needing no DB), make check-app green. Mutation-proved: the address scrub, the token/hash scrub, the uuid-kept threshold, the message scrub, the debug-in-production guard, the pool handler's exit, and the no-console scan — seven mutants, each reddening a test. 113 closed.
NEXT: OPS-002 is 2/6 — T2 (the request id minted at the edge in proxy.ts and carried through, which needs a request-context mechanism a Next proxy cannot share with its handler), T4 (a scrubber hit is itself an alert), T5 (/health with a real query and build version), T6 (the three alerts) remain; T5 is the cleanest next (a route and a real SELECT). W1's clean frontier stays thin: AUTH-003/T3,T4 and SEC-001/T4 are entangled with the i18n-blocked set-password and reset forms (I18N-DEC-02) and the Mailer port (MAIL-001/T1), ADMIN-002/T3 wants its ADMIN-001 delete caller, and AUTH-004/T4 waits on a gated surface (INV-002). The buildable body is W2 backend — CFG-001, DATA-002, CMS-003, and the ADMIN-001 delete slice.

## 2026-09-10 · ADMIN-001/T3,T7 (2 tasks) · iter 26
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the two admin account mutations and their guard (ADMIN-001/T3,T7), and the audit the self-service end-all-sessions path had deferred (AUTH-004/T3). apps/web/src/admin/accounts.ts:suspendAccount and changeRole each mutate the account, end every session and record one act (account.suspend / account.role_change) in one transaction (SEC-R04); the UPDATE's RETURNING row is the idempotency check, race-safe under a row lock. critical-impl built the pair and both deep-review rounds ran, but the weekly limit cut the subagent off mid-Fix-3; the main session finished the Critical fixes. The guard is ADMIN-DEC-01's safe default: isLastActiveAdmin locks the active-admin rows in id order inside the transaction and refuses when the subject is the actor or the last admin who can sign in — counted state='active', a deviation from deep-review's role-only guard (a suspended admin cannot sign in to undo anything, so role-only would let a room with one active and one suspended admin be stranded); ADMIN-DEC-01's title, safe-default and Revises text moved from "last admin" to "last active admin" to match. Fix 3 closes a suspend-vs-issue race: apps/web/src/auth/invitation.ts:issueTokenIn selects `state<>'suspended' FOR UPDATE` in one statement and throws SuspendedAccountError, so no re-issue re-opens a just-suspended account. AUTH-004/T3: apps/web/src/app/api/account/sessions/all/route.ts now records session.invalidate_all in the same transaction as the delete — its Deferred marker named the now-closed SEC-002/T3 as its blocker (stale; before/after stay null under SEC-DEC-01, which blocks only T4). accounts.test.ts runs on a database of its own: the guard reads the GLOBAL active-admin set, so a "last admin" test is deterministic only when this suite owns the whole set, which four parallel workers creating active admins otherwise deny — a beforeEach clears it. Verified against PostgreSQL 17: make check green, make check-app green, full suite 366 (10 clean runs confirming the invitation-probe flakiness is gone, then 3 more with the route change). Mutation-proved: the last-admin guard, its state='active' filter, its subject-id check, the self-check, the concurrent-demotion race (forUpdate removed, 10/10 killed), Fix 3's suspended predicate, and the route audit (2/2). 111 closed.
NEXT: ADMIN-001 is 3/8 — T1 (the list), T2 (the person page), T4 (delete with typed-name confirmation), T5 (delete guard: last admin and self), T6 (create issues an invitation) and T8 (reinstate) remain. T5 is the natural next Critical-tier build and should reuse the active-admin semantics ADMIN-DEC-01 now fixes and isLastActiveAdmin's shape. The other W1 frontiers stay AUTH-003 T3/T4 (mail and the set-password form, both on I18N-DEC-02) and SEC-001/T4 (rate limits, unblocked since the reset and invitation endpoints exist). ADMIN-DEC-01 and SEC-DEC-01 remain OPEN for the owner.

## 2026-09-08 · AUTH-003/T5,T7 (2 tasks) · iter 25
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the invitation and reset service layer (AUTH-003/T5,T7). apps/web/src/auth/invitation.ts:inviteAccount creates an invited account + a seven-day invitation + an account.create audit in one transaction and returns the on-screen link the admin delivers by hand (SMTP send deferred to AUTH-003/T3); requestReset answers identically for an address an account holds and one it does not, via an INSERT…SELECT with no process branch. critical-impl built it; deep-review found the anti-enumeration DEFEATED by a lock asymmetry — SELECT…FOR UPDATE took a row lock only when the account existed, amplifying to 5.8x under an 8-client burst (a real enumeration oracle) — plus a reset silently destroying a pending invitation (unauthenticated denial-of-access), a three-way email-normalisation split, a concurrent-raise, and a mailUnavailable honesty bug. All fixed in place: an address-keyed pg_advisory_xact_lock (both paths now wait identically, residual flat ~1.5x) replaces the FOR UPDATE; a shared address.ts:normaliseAddress; the reset narrowed to active accounts (the AUTH-DEC-04 safe default, so an invited account's invitation stands); deliverByHand non-empty in both branches. Filed AUTH-DEC-04, fixed two stale docs (SEC-DEC-01's safe default, the design §4 MAIL-DEC-01 sentence). Verified against PostgreSQL 17.11: 341 tests (7 clean runs), the anti-enumeration barrier re-run under mutation by me (removing the address lock reddens it), make check green. critical-impl's mutation harness destroyed the uncommitted invitation.ts mid-run and rebuilt it from HEAD — I confirmed the rebuild sound by the 341-test run, the mutation spot-check and typecheck. 109 closed.
NEXT: AUTH-003 is 5/7 — T3 (the localised mail + the Mailer port MAIL-001/T1) and T4 (the set-password form) are the mail and i18n residue (I18N-DEC-02). SEC-001/T4 (rate limits on reset and invitation) is now unblocked since those services exist. A residual for ADMIN-001: the public issueToken serialises on the account row, not the address, so a future resend-invitation path must take the address lock. The other W1 frontier is ADMIN-001 (account management).

## 2026-09-08 · SEC-001/T3,T5 (2 tasks) · iter 24
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the security baseline's CI and input-parsing halves (SEC-001/T3,T5). T5: .github/workflows/ci.yml already ran gitleaks (full history, the release binary pinned to v8.30.0) and tsc; this iter added `npm audit --audit-level=high` to the app job — the design's blocking dependency check, measured clean — and verified every action SHA against upstream rather than the file, which caught the checkout action pinned to a v6-development commit mislabelled `# v4` and re-pinned it to the verified v4.4.0 across all three jobs. T3: the one body-taking route parses through a schema (sign-in's readCredentials returns `Credentials` or null, a bad body answering 400 not 401) and the cookie-only routes have no unparsed input; closed on the existing parsers, with route.test.ts driving the malformed cases. The `[~]` note on T3 had described T5's scanning work — misfiled — and is reconciled into T5's evidence. make check green, 107 closed. No app code changed, so the batch's proof is the ci.yml YAML validity, the clean npm audit, and the SHA verification against upstream.
NEXT: SEC-001 is 4/5 — T4 (rate limits on sign-in, reset and invitation, per account and per address) waits on the reset and invitation endpoints, which are AUTH-003/T5 (anti-enumeration) and T7 (the degraded-delivery invitation); those are the top service-buildable work but need a mail-send port and an invited-account create that reaches into ADMIN-001's surface. The other W1 frontiers are ADMIN-001 (account management) and the i18n-blocked visitor forms on I18N-DEC-02.

## 2026-09-07 · SEC-001/T1,T2 (2 tasks) · iter 23
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the security baseline (SEC-001/T1,T2). apps/web/src/proxy.ts sets five headers on every response from the middleware that already carried AUTH-004's no-store — HSTS (two years, includeSubDomains, preload), nosniff, Referrer-Policy, Permissions-Policy, and a per-request Content-Security-Policy with no unsafe-inline. Refusing inline script costs one nonce, minted per request and set on the request so Next stamps it onto its own inline tags; that needs a per-request render, so every route is force-dynamic (apps/web/src/app/layout.tsx) — a §14 architecture consequence the loop settled under §1.11 (doctrine refuses unsafe-inline, measurement killed the two static alternatives) and filed reversible at decisions-log.md#SEC-DEC-02. Built via critical-impl; deep-review found the CSP broke the framework's inline-styled 404 and 500 — real, since the admin gate's current destination is the 404 — fixed in place with custom pages on an external CSS module (apps/web/src/app/not-found.tsx and global-error.tsx). Verified in a browser (Playwright): the start page hydrates with zero console errors, both inline scripts nonced; the 404 renders styled; a forced root-layout crash renders the custom global-error dynamically, nonced, styled, no CSP violation. deep-review's other findings fixed in place: two false facts in the registers (SEC-DEC-02's warrant, AUTH-004/T4's Note), the live browsing-topics token added beside the retired interest-cohort, and matcher, nosniff and CDN-caching notes onto the design and the operator checklist. make check green, full suite 317, proxy.test.ts 35 with the header drops and the no-store conditional mutation-proved. 105 closed.
NEXT: SEC-001 is 2/5 — T3 (schema input parsing) is in-progress, T4 (rate limits on sign-in, reset and invitation) waits on the reset and invitation endpoints (AUTH-003/T5,T7), T5 (the CI gate) is buildable. The large W1 frontiers stay AUTH-003 (T5 anti-enumeration and T7 the degraded-delivery invitation are service-buildable; T3 and T4 are the mail and the i18n form) and ADMIN-001 (account management). The i18n-blocked visitor forms wait on I18N-DEC-02.

## 2026-09-07 · CMS-006/T3 + AUTH-002/T4 (2 tasks) · iter 22
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the grant write side (CMS-006/T3), so an admin can now grant and revoke an account's access to a granted item, not only read through a grant that already stands. apps/web/src/content/grants.ts:addGrant inserts a content_grants row ON CONFLICT DO NOTHING and apps/web/src/content/grants.ts:removeGrant deletes one, each in a transaction with the audit that records it (SEC-R04 — recordAudit takes the transaction, so the row and the audit commit or roll back together), each idempotent: a re-grant and a revoke of nothing return false and write nothing, so the trail carries acts not re-assertions. The audited subject is the grantee account, because a grant is a permission on an account. The read side is unchanged — the granted branch of visibleTo (iter 20) admits an item to an investor only where such a row names them — and the deck admin UI that calls these is DECK-004's (depended_by); this ships the mechanism it composes. AUTH-002/T4 (the deck-isolation test) closed by cross-reference (§1.10): apps/web/src/content/read.test.ts already proves an investor asking for another investor's granted deck gets null and never a redirect, now reinforced by a deck-type, granted-audience case that turns visible across addGrant and back to null across removeGrant. Verified against PostgreSQL 17.11: content read suite 21 tests, the grant case mutation-proved three ways — dropping addGrant's idempotency guard, mislabeling the grant.add action, dropping removeGrant's guard. make check and make check-app green, 103 closed.
NEXT: CMS-006 is 4/6 — T4 (the read route: one 404 for a refusal, a missing item and a malformed id alike, narrowing forReader's row to drop author_id — design §6 F5/F6) and T6 (audience-change audit and a short cache) remain, both on the same read path no route mounts yet. The large W1 frontiers are ADMIN-001 (account management, wiring the account audit callers, unblocks AUTH-003/T7) and SEC-001 (T1 the headers, entangled with T2 CSP and its nonce). SEC-002 stays 3/5 (T2 external, T4 pending SEC-DEC-01); the i18n-blocked UI waits on I18N-DEC-02.

## 2026-09-07 · SEC-002/T5 (1 task) · iter 21
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the audit trail's read side, now that the admin shell exists to hang it on (SEC-002/T5). apps/web/src/audit/read.ts:recentAudit reads the trail newest first — the identity id descending, the order things happened without trusting a clock — narrowed by any of actor, subject and action. apps/web/src/app/admin/audit/page.tsx renders it under the admin segment's inherited role check (ADMIN-002) as a read-only table with a filter form and no edit or delete control, because the trail is append-only in the database and a control that does not exist cannot be reached by a bug. This completes the audit feature's visible half: publish and withdraw write their rows (iter 19), and an admin can now read them. Verified against PostgreSQL 17.11: audit suite 10 tests (recentAudit's newest-first mutation-proved), full app suite 295 green; measured against a next build then next start artifact — an admin GET answered 200 and listed the trail, an investor GET answered 404 through the inherited gate, and filtering to one action dropped the other's row. make check green, 101 closed.
NEXT: SEC-002 is 3/5 — T2 (the deploy REVOKE) is pending-external, T4 (the changed-field allow-list) pending SEC-DEC-01. AUTH-002/T4 (the deck-isolation test) is now buildable since forReader exists — an investor request for another investor's granted deck returns nothing. The large W1 frontiers remain ADMIN-001 (account management, which wires the account audit callers and unblocks AUTH-003/T7) and the CMS-006 remainder (T3 grants, T4 route, T6 audience-change); the i18n-blocked UI waits on I18N-DEC-02.

## 2026-09-07 · CMS-006/T1,T2,T5 + CMS-001/T6 (4 tasks) · iter 20
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the content access predicate — the design calls it the single most dangerous in the repository, the one that fails invisibly — built by critical-impl and hardened through deep-review. apps/web/src/content/access.ts:visibleTo turns a reader into the one WHERE fragment every content query composes: a visitor sees published public items, an investor public and investor content plus granted items they hold a grant for (a correlated EXISTS, never a join), an admin everything; the published check is inside the predicate, so a draft is an item with no audience. apps/web/src/content/read.ts:forReader and forAuthor are separate functions so admin-sees-all cannot leak into a reader path, and scripts/check-content-access.py refuses content-table SQL outside the module, wired into make check.
  deep-review found no live leak but four latent defects, each fixed and mutation-proved: visibleTo granted by fall-through to any non-admin role — the one site in the tree that admitted an unknown role rather than refusing it — now names investor and makes a future role a compile error and fail-closed (F1, P1); the gate covered only content_items while content_revisions and content_locales hold the body — now the four content tables, matched case-insensitively since PostgreSQL folds identifiers, scanning all of apps/web (F2, F3); and forReader's join trusted two invariants the schema does not enforce, a pointer to another item's or an unpublished revision — now bound to the item and to published (F4). F3 also corrected a measurably-false sentence in T5's Evidence. Verified against PostgreSQL 17.11: read.test.ts 20 tests, full app suite 294 green and deterministic, make check green — 100 closed.
  Separately, the pre-existing flaky sign-in rehash test (an Argon2 encoding occasionally containing the two-character password vector, about 2 in 15 runs) is fixed with a long distinctive vector and its regenerated old-cost fixture, committed apart as test(AUTH-001).
NEXT: CMS-006/T3 (grant write and revoke, and the admin surface), T4 (the route's 404, id-shape validation, and narrowing the reader's revision to drop author_id — F5 and F6, recorded in the CMS-006 design §6), and T6 (audience-change audit, short public cache). CMS-007's search must join the published pointer, not compose the predicate alone, to hide a draft from its author (recorded in CMS-007 §6). ADMIN-001 remains the other high-leverage W1 frontier; the module's write path is gated by CMS-002/CMS-004's routes.
