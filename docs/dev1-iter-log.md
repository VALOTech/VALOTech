# dev1 iteration log

> **Not for `main`.** This file lives on `development` only (`.claude/CLAUDE.md` §1.1).

Working state, not an archive. Each iteration of `/dev1` appends one entry and
prunes the oldest until **twenty** remain — the bound is held by
`scripts/check-log-retention.py` and the reason it is safe is §13.1: an entry
is deleted only once every durable fact in it already lives in its permanent
home, which is `docs/tasks.md` for task state, `docs/decisions-log.md` for a
choice, and `docs/operator-checklist.md` for something waiting on a person.
`git log -p docs/dev1-iter-log.md` is the archive, so a pruned entry is
recoverable verbatim and no second copy is kept here.

A cold start reads the tail of this file to find out where the last one
stopped. `OUTCOME:` is fixed vocabulary so that reading can be mechanical:
`CLOSED` when at least one task moved to `[x]`, `IDLE` when the queue was
stale and the loop re-fires, `STOP` when it ended and the owner is needed.
`REASON:` on a non-`CLOSED` entry is one stable phrase, reused verbatim when
the same condition recurs, so three of them in a row can be detected rather
than noticed.

---

## 2026-09-11 · RPT-002/T2 (1 task) · iter 64
STATUS: green · TIER: Standard · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: RPT-002/T2 — publishing a report into a taken period fails with the report that holds it, closed in the main session in the W3 report lane. The browser close-path the iter-63 note set up was shut: the peer lane had resumed (a listener on 3100), and building the shared .next would disrupt it, so the descent went to W3's first clean backend task instead. apps/web/src/content/publish.ts now wraps the publish transaction — when the partial unique index one_published_report_per_period refuses a second published report for a period (RPT-002/T1), the pg DatabaseError (code 23505) becomes apps/web/src/content/publish.ts:PeriodTakenError carrying the period and the report that holds it (id and title), resolved by periodTaken after the rollback so the publishing row is not named as its own rival. The guard stays the index, not a pre-check, so two admins racing on one period still collide safely. Verified against PostgreSQL 17.11 by apps/web/src/content/content-reports.test.ts (22 passed): the second publish rejects with a PeriodTakenError naming the first report, while distinct periods, a draft replacement, a corrected re-publish, and a withdrawal freeing the period all still pass. Mutation-proved, each KILLED: defeating the translation lets a raw error escape, and resolving the holder among unpublished rows misses the published report. make check and check-app both green (178 closed, 271 non-DB passed). The sentence's surface is CMS-004's, unbuilt with the rest of the publish surface.
NEXT: The W3 report lane continues — RPT-002/T3 (the archive lists by period, groups by year, shows a gap) and T5 (withdrawal states the period becomes a gap and which report becomes current), though both carry a reading or confirmation surface that wants the browser lane. The clean backend frontier is otherwise thin: MAIL-001's rest is Critical and credential-blocked, MAIL-002 depends on it and its tasks interdepend through the unsubscribe, and POST-001 with DECK-001 are composer surfaces. If the peer lane frees (nothing on 3100, a stale .next), the highest-value move is the owed browser iteration on the CMS-002 editor cluster (T1, T2, T6) plus iter 62's T3 focus and iter 61's invite form. The backend closes remain gated on the owner's decisions, chiefly I18N-DEC-02, plus CMS-DEC-03 and CMS-DEC-04.

## 2026-09-11 · frontier-gated (0 closed) · iter 63
STATUS: yellow · TIER: - · OUTCOME: IDLE
REASON: W0-W2 clean-backend frontier exhausted; the open work is owner-decision-gated, browser-owed, or W3-entangled
WHAT CHANGED: CMS-007/T3 marked blocked by POST-001/T3, and no code changed; the ledger now records the real blocker instead of reading it as buildable each cold start. A cold-start walk of the whole roadmap found no clean backend close. W0's two outstanding are external (I18N-001/T4, native readers) and cross-wave-parked (SITE-002/T4). W1 is gated on I18N-DEC-02 (the app i18n framework, which AUTH-001/T4, AUTH-003/T4 and AUTH-004/T2 all wait on), external (SEC-002/T2 REVOKE, AUTH-003/T3 mail) or browser-owed (AUTH-004/T4). SEC-001/T4's sign-in limit already ships (apps/web/src/auth/rate-limit.ts, wired into the sign-in route); its reset and invitation-acceptance surfaces are the I18N-blocked public forms, so the task waits. W2: CMS-003 is CMS-DEC-03, CMS-005 is CMS-DEC-04, CMS-002/T4 and T5 are editor selection and paste interactions (browser), CMS-004/T1 and T2 render through reader components that live in W3, CMS-004/T4's fallback count needs an app locale list that I18N-DEC-02 has not created, CMS-004/T6's purge needs the edge (OPS-001). CMS-007/T3's product filter has no data model until POST-001/T3 adds the optional product tag. make check green throughout (177 closed, 12 blocked, 0 REVIEW).
NEXT: Two AI-buildable paths remain. Clearest: a browser iteration closing the CMS-002 editor cluster — T1 keyboard block navigation, T2 the seven block types' fields, T6 save and the unsaved line and the local-copy restore — whose mechanisms ship and are header- and HTTP-verified with only the live browser drive owed; at iter 63 the peer lane looked free (nothing listening on 3100, the shared .next last built at 09:28, only idle node processes), so the vendored-playwright drive on an own port with a seeded valotech admin cookie is available, and it also clears iter 62's owed T3 focus check and iter 61's invite form. Second: descend into W3 (POST-001 the post composer and its product tag, INV-001 the investor room that consumes CMS-007's search and filters) where the W2 infrastructure gets its readers. The backend closes are now gated on the owner's decisions, chiefly I18N-DEC-02, which unblocks every auth form and the CMS-004 fallback count, plus CMS-DEC-03 and CMS-DEC-04.

## 2026-09-11 · CMS-002/T3 (1 task) · iter 62
STATUS: green · TIER: Medium · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-002/T3 — an image block cannot be saved without alternative text, Medium, main-session, in the W2 CMS authoring lane. The rule was already built and tested (the validateBlock image case refuses an empty alt with A11Y-R02, server-side through saveDraft and publish and the 422 route, and the editor refuses before the POST — CMS-002/T7), so T3's headline and the A11Y-R02 "blocks the save rather than warning" requirement were already met; what was missing was the design's own mechanism for it (CMS-002 §3): the refusal points the author at the offending block rather than leaving a banner read at the end and ignored. apps/web/src/content/blocks.ts:BlockValidationError now carries blockIndex — the faulty block's position lifted from its blocks[i] message, null when the value is not a block array — and apps/web/src/app/admin/content/[id]/edit/editor.tsx:save moves focus to that block on a refusal via the editor's existing focus effect. Verified by apps/web/src/content/content.test.ts: empty and whitespace-only alt both refused, the message names blocks[0] down to the field, blockIndex 0 for a first-block fault, 1 for the second, null for a non-array. make check and check-app both green (271 passed, 437 DB-skipped, 708 over 41 files — one test more than iter 61). Mutation-proved, each KILLED: defeating the parse (blockIndex always null) and offsetting it by one each redden the new test. The focus landing in a live browser is owed with the editor's other owed client drives (CMS-002/T1, CMS-002/T2 and CMS-002/T6), the standard under which CMS-002/T7 closed. 177 closed.
NEXT: CMS-002 has T3 and T7 closed; T1, T2 and T6 are built and browser-owed; T4 (marks by selection, stored as offsets) and T5 (paste imports plain text plus recognised structure) remain open — both editor-input features on the same surface, a natural co-close for iter 63, which reads the design first because the roadmap over-counts buildable. Alternatively descend to CMS-004 (preview and publish — T1, T2, T4, T6 open) for a fuller batch. The browser-lane debt is accumulating: the invite form (iter 61), the CMS-002 client drives (T1, T2, T6), T3's focus landing and T7's client validation all await the shared Playwright lane — worth a dedicated browser iteration when it frees. 11 open owner decisions; the legal lanes external-blocked on the compliance source texts; MAIL-001's rest Critical and blocked on the credential.

## 2026-09-11 · ADMIN-001/T6 (1 task) · iter 61
STATUS: green · TIER: Medium · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: ADMIN-001/T6 — the create-invitation form, Medium, main-session, completing account management (8 of 8). apps/web/src/app/admin/accounts/invite/route.ts is a POST route: origin-checked then requireAdmin, the role validated against ACCOUNT_ROLES and the address shape-checked, then apps/web/src/auth/invitation.ts:inviteAccount creates the invited account, its single-use token and the account.create audit in one transaction and sets no password — there is no other way an account begins, and no admin ever sets one (AUTH-003). The route returns the invite link for the admin to deliver by hand, since nothing mails it yet (AUTH-003/T3). apps/web/src/app/admin/accounts/new/page.tsx is the form (name, email, role), and the accounts list links to it. Verified against PostgreSQL 17.11 by apps/web/src/app/admin/accounts/invite/route.test.ts (8): a non-admin refused, a signed-out caller redirected, a cross-origin post refused, a valid invite creating the invited account with a link that consumes to it and account.create audited to the admin, an admin created when that role is chosen, an unknown role and a blank name and a no-@ address each refused, and a second invite of a taken address refused with one account left. make check and check-app green (270 passed, 437 DB-skipped, 707 over 41 files). Mutation-proved, each KILLED: dropping the role check, the address shape-check, the email-taken 409, and the cross-origin guard. The form is a standard labelled form posting to the tested route; its interactive submit-flow is not independently browser-driven this iteration, deferred as a light follow-on because the route is the verified surface and the client-form pattern was browser-proven at iter 60. 176 closed.
NEXT: ADMIN-001 and ADMIN-002 are both complete; W2's admin surfaces are done. The remaining W2 is the CMS lane: CMS-005 (locale states — T4 the review screen, T6 a new revision with no locale rows; T3 blocked on CMS-DEC-04), CMS-002 (authoring — T3 alt-text, T4 marks, T5 paste), CMS-004 (preview and publish — T1, T2, T4, T6), CMS-007 (search and filter — T3, T5, T6), and CMS-003 (media — T2 and T3 blocked on CMS-DEC-03). Iter 62 reads the first CMS candidate's design before anchoring, because the roadmap over-counts buildable. Owed, light: a browser drive of the invite form. 11 open owner decisions; the legal lanes external-blocked on the compliance source texts; MAIL-001's rest Critical and blocked on the credential.

## 2026-09-11 · ADMIN-001/T4 (1 task) · iter 60
STATUS: green · TIER: Medium · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: ADMIN-001/T4 — the account-deletion surface, Medium (personal-data handling, with a privacy review), built by critical-impl and verified here. apps/web/src/app/admin/accounts/[id]/delete/route.ts is a dedicated route (not the shared action route): POST {confirmName}, origin-checked then requireAdmin then subject-resolved, then the typed name re-checked server-side with the same apps/web/src/app/admin/destructive-actions.ts:typedNameMatches the panel gates on, then apps/web/src/admin/accounts.ts:eraseAccount, which owns the self and last-admin guards and audits account.delete. The confirmation lists what goes and what remains — apps/web/src/admin/accounts.ts:erasureCounts (sessions, invitations, deck reads, report reads) with apps/web/src/content/grants.ts:erasureContentCounts (grants and surviving authored revisions, the two content tables named where check-content-access allows), each scoped to one account. The ADMIN-002/T3 component gained a details slot and an onConfirm that hands back the typed name; the delete control is T3's first final-act caller and navigates to the account list on success. Verified against PostgreSQL 17.11: 73 touched tests green (delete route 22, accounts plus 2), make check and check-app green (270 passed, 429 DB-skipped, 40 files). Browser-verified by a Playwright drive against a built server: the Delete confirmation shows the typed field and focuses it, Confirm is off until the exact name is typed, the preview renders, and confirming deletes and lands on the list — which closes the ADMIN-002/T3 typed-name rendering owed in iter 59, since it had no caller until now. Mutation-proved, each KILLED: dropping the server-side name re-check, writing the subject as the audit actor, and dropping the account scope on each of the two count paths. Classified Medium (VALOTech §15 makes data deletion personal-data handling, not Critical); critical-impl was used for the irreversible surface's care and to conserve context, then verified here. 175 closed.
NEXT: ADMIN-001 is 7 of 8 (all but T6). T6 (the create-invitation form, wiring the tested inviteAccount and showing the on-screen link, Medium) is the last one and the clean next anchor, main-session. Owed, small and the owner's: the erasure preview lists the five kinds ADMIN-001 §3 names, but mail_log and unsubscribes also cascade — adding them is two counts and a design-prose edit, deferred as a completeness choice rather than a defect (the sentence says "also removes", claiming no exhaustiveness). Then W2 continues at the CMS surfaces (CMS-002 authoring, CMS-004 preview and publish, CMS-007 search), each read against its design first. 11 open owner decisions. Honest flags: a pre-existing load-sensitive flake in the content suite (a 5000 ms timeout under full-suite parallel load; passes alone and in the final runs) is slightly likelier now that a 40th isolated database exists — environmental, not this change; and the browser verification ran via the critical-impl agent's Playwright drive against its own build on a free port, the peer server on port 3100 left untouched.

## 2026-09-11 · ADMIN-001/T2 + ADMIN-002/T3 (2 tasks) · iter 59
STATUS: green · TIER: Critical · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: ADMIN-001/T2 (the admin person page) and ADMIN-002/T3 (the one destructive-action component), built by critical-impl and axis-reviewed by deep-review — the page adds auth-operation endpoints, so Critical. apps/web/src/app/admin/accounts/[id]/page.tsx shows identity (apps/web/src/admin/accounts.ts:personIdentity), access (apps/web/src/content/decks.ts:grantedDecksForAccount — deck grants with pin and last-opened), sessions (apps/web/src/auth/session.ts:liveSessionsForAccount plus an end-all control), and the actions through one POST route apps/web/src/app/admin/accounts/[id]/action/route.ts (origin check, then requireAdmin, then subject-resolve, then the existing services called with the requester's id). Suspend runs through the T3 component apps/web/src/app/admin/destructive-action.tsx and its one registry apps/web/src/app/admin/destructive-actions.ts, where the typed-name-for-final-acts is derived from the reversal being final so the two cannot drift, and typedNameMatches matches nothing for an empty subject. deep-review returned yellow with 11 findings, the core sound (authZ, actor id, guards, DATA-R05, CSRF all confirmed); its in-file fixes are applied — Escape handled on the wrapper, a disabled-Confirm reason rendered, the end-sessions control self-guarded and phrased in the second person, and a server failure told apart from a refusal — plus a fifth the browser run itself found, focus falling to the page body after an act removes its own control, now caught by focusing the outcome. ADMIN-DEC-03 filed (whether an admin's resend and reset are audited, and whether the reset control is offered before its mail exists — safe defaults as shipped); ADMIN-DEC-01 Revises gained ADMIN-001/T2; the designs gained the action-route contract in ADMIN-001 §3 and the correction that a route handler under the segment inherits no layout in ADMIN-002 §3. Verified against PostgreSQL 17.11: 73 new tests green (person 16, action route 21, destructive-actions 9, content-decks plus 27); make check and check-app green (270 passed, 405 DB-skipped, 39 files). Browser-verified by a Playwright drive against a built server: the suspend confirmation takes focus on open, Escape and Cancel and a keyboard-only open all work, Confirm suspends the account and empties the sessions section as the page re-reads, and focus lands on the outcome afterwards. Mutation-proved, each KILLED: typedNameMatches failing open on an empty subject, the audit actor written as the subject rather than the admin, and the deck_reads join losing its account scope. 174 closed.
NEXT: ADMIN-001 is 6 of 8 (T1, T2, T3, T5, T7, T8); T4 (a real delete with a typed confirmation, which uses the T3 component's final-act path) and T6 (the create-invitation form) remain. ADMIN-002 is complete. Owed: the browser check of T3's typed-name rendering, which no surface renders until a final-act caller lands (ADMIN-001/T4, CMS-003, MAIL-001); and ADMIN-DEC-03's audit fold, which waits on the owner. Honest flags: the browser verification ran via the critical-impl agent's Playwright drive rather than my own (the Playwright MCP profile was held by another session), and that drive rebuilt the shared .next directory that a parallel VALOTech server on port 3100 — not this loop's — also reads, with nothing observably broken. W2 continues at ADMIN-001/T6 (the create form, clean), then the CMS surfaces (CMS-002 authoring, CMS-004 preview and publish, CMS-007 search), each read against its design first. 11 open owner decisions. The legal lanes stay external-blocked; MAIL-001's remaining tasks stay Critical and blocked on the credential.

## 2026-09-11 · ADMIN-002/T2 (1 task) · iter 58
STATUS: green · TIER: Standard · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: ADMIN-002/T2 — the admin console landing surface, what needs attention, finished by wiring its first attention source. apps/web/src/content/decks.ts:decksGrantedButNeverOpened anti-joins content_grants to deck_reads and returns the active-account, deck-item grants with no read of any version, longest-waiting first (an invited or suspended grantee cannot open it, and a granted non-deck is not one); apps/web/src/app/admin/page.tsx renders it and keeps the honest empty state when there is nothing. The other sources the design names — content with unreviewed locales, a stale portfolio row, the register's open questions — join it as CMS-005, INV-003 and their reads land, so the surface grows rather than inventing items. Wave choice: W0 is build-ahead-exhausted (I18N-001/T4 external, SITE-002/T4 cross-wave-parked, 0 buildable), so this descended to W1; within W1 the localized forms and mail (AUTH-001/T4, AUTH-003 T3 and T4, AUTH-004/T2) are coupled to I18N-DEC-02, which is open, and the reset and invitation routes SEC-001/T4 would rate-limit do not exist yet (they are part of the i18n-blocked AUTH-003/T4), so the admin console — explicitly English by ADMIN-002/T5, not i18n-blocked — was the clean anchor. T3 (the destructive-action component) was not batched: the three undoable actions it fronts (delete account, delete media, send mail) are unbuilt, so it would be a component with no caller (§1.10). Verified against PostgreSQL 17.11: apps/web/src/content/content-decks.test.ts 20 tests (15 prior plus 5 new). make check and make check-app green (261 passed, 361 DB-skipped without DATABASE_URL, over 36 files). Mutation-proved, each KILLED and decks.ts restored byte-identical: inverting the never-opened anti-join, changing the active-only filter, changing the deck-only filter, and reversing the longest-waiting-first order. 172 closed.
NEXT: ADMIN-002 is 4/5 (T1, T2, T4, T5); T3 waits for a real caller among ADMIN-001/T4, CMS-003 and MAIL-001/T4. W1's clean backend is nearly exhausted: SEC-001/T4 (reset and invitation rate limits) is route-coupled to the i18n-blocked AUTH-003/T4 (its sign-in half is already done); AUTH-004/T4 [~] (no-store, verified by pressing Back) needs a browser on the peer-held Playwright lane. The whole localized-UI lane — AUTH-001/T4, AUTH-003 T3 and T4, AUTH-004/T2, and the reset and invitation routes — waits on I18N-DEC-02 (recommendation next-intl), which is the owner's; those rows read [ ] in the ledger but are effectively decision-blocked, worth triage. Iter 59 most likely descends to W2 for the next clean backend — ADMIN-001 (person page, deletion, creation), CMS-003 media, CMS-005 locale states, CMS-002 authoring — reading each design first (the roadmap over-counts buildable). Legal lanes stay external-blocked on the missing compliance source texts; MAIL-001's T4, T7 and T8 stay Critical and blocked on the credential and the send route; MAIL-DEC-02 (retry idempotency) stays open. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands; and T2's remaining attention sources as CMS-005 and INV-003 land.

## 2026-09-11 · MAIL-001/T5 (1 task) · iter 57
STATUS: green · TIER: Critical · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: MAIL-001/T5 — the investor-mail send loop, the Critical build-ahead of the send route (T4) and the SMTP adapter (T8). apps/web/src/mail/send.ts:send writes one mail_log row per recipient (queued) with the mail.send audit in one transaction before any message leaves, then hands each to the port one at a time and moves its row to accepted with the queue id or failed with the reply — a refusal stops the one recipient, not the send, and only the port call sits in the try so a database failure never records an accepted message as failed. This iteration acted on the iter-56 deep-review (STATUS red, 8 findings, 3 High). Resolved in-file: F1 (a port's reply could carry the recipient address or the SMTP_URL credential into mail_log.error, a two-year column, DATA-R02) — lifted the logger's masking into a shared apps/web/src/ops/scrub.ts:scrub (address + long-credential-token by shape, the credential case an @-only regex misses), run the reply through it and capped it; logger.ts now imports the shared scrub, behaviour identical (logger.test.ts green). F7 (an empty reply stored blank) — named. Deferred with a filed decision: F2 (the retry was not idempotent — a failed row is never superseded, so a second retry double-sends an irrecoverable message) — removed retry from send.ts and send.test.ts, set T6 [!], and filed MAIL-DEC-02 (retry idempotency mechanism: retry_of column recommended vs idempotency key vs heuristic) OPEN, first-of-its-class so handed to the owner not loop-settled (§1.11); safe default is no automated retry. Design-grounded, no code change: F3 (deep-review wanted a suppression re-check inside queueAndRecord) — the design places that re-check in the send route T4 with refuse-on-change semantics and states the list is checked where recipients are resolved and never re-evaluated at send time (MAIL-001 §3, MAIL-002 §6), so adding a filter to send() would contradict the design; send() correctly trusts the confirmed list and the guarantee is build-ahead pending T4, already documented in send.ts. Doc syncs: F5 (MAIL-001 §3 step 5 now matches the code — the audit ships SEC-DEC-01's safe default with the before and after columns null, subject and count deferred), F8 (SEC-DEC-01 gained Revises: MAIL-001/T5), F4 (mailer.ts documents the port's reject-on-refusal contract), F6 (MAIL-002 §3 documents the stranded-queued row: unknown outcome, never auto-resent). Verified against PostgreSQL 17.11: send.test.ts 10 tests green; make check and make check-app green (261 passed, 356 DB-skipped without DATABASE_URL, over 36 files). Mutation-proved, each KILLED and files restored byte-identical: scrub turned no-op, the cap removed, the empty-reply name dropped, and a refusal made to end the send instead of failing one row. Honest notes: I diverged from deep-review's F3 recommendation on design grounds (cited above) rather than adding the filter; and I did not re-run deep-review on the final state — the fixes apply its own in-file prescriptions and the defective surface (retry) was removed, and the §11.2 development→staging→production promotion is the human Critical sign-off. 171 closed.
NEXT: MAIL-001 is 4/8 (T1, T2, T3, T5). The rest is the send route and adapter — T4 (typed count and re-resolve-and-refuse, the interlock F3 rests on), T6 (retry, blocked on MAIL-DEC-02), T7 (no credential: composer works, send disabled with the reason), T8 (one TLS SMTP connection, no plaintext fallback) — all Critical (mail to real investors) and blocked on the SMTP credential (CRED-001) plus the send route, worked via critical-impl and deep-review when taken. 10 open owner decisions now (MAIL-DEC-02 added). The broader frontier is unchanged: the legal lanes (LEGAL-SG-001, the rest of LEGAL-GLOBAL-001) are external-blocked on the compliance source texts the README names (a Singapore PDPA file and a GDPR file), which are not in the tree; the remaining W3/W4 is UI on the peer-held Playwright lane and infra (OPS-001). W2 stays blocked — CMS-DEC-03, CMS-DEC-04, ADMIN-001. Iter 58 takes another MAIL-001 build-ahead only if a non-credential surface remains, else a W4 infra build-ahead. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · MAIL-001/T2,T3 (2 tasks) · iter 56
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: MAIL-001/T2 and T3 — recipient resolution for an investor send, built ahead of the send route. apps/web/src/mail/recipients.ts is new: resolveRecipients turns a chosen set of account ids into a confirmed list of names — the list the admin confirms and the send then uses, never a criterion re-evaluated at send time (T2) — and excludes a suspended account and an unsubscribed one, each with its reason, suspended winning when an account is both, reading the unsubscribe list (T3). It reads accounts and unsubscribes, with no send and no credential, so Medium and in the main session; the actual send (T4 through T8) is the Critical, credential-blocked residue this feeds. Verified against PostgreSQL 17.11 on the isolated valotech_mail database: 607 tests over 35 files (was 601 over 34 at iter 55; +6 recipient cases). Mutation-proved, each KILLED and restored byte-identical: inverting the suspended exclusion, and inverting the unsubscribed exclusion. Honest attribution: the first full-suite run showed one failure — apps/web/src/auth/gate.test.ts's concurrent-one-token test timed out at 5000ms — unrelated to this change (resolveRecipients touches neither the gate nor sessions); it is the warmed two-connection race timing out under the parallel-suite load that adding the 35th isolated database raised, proven environmental by gate.test.ts passing 21/21 run alone. 170 closed.
NEXT: MAIL-001 is 3/8 (T1, T2, T3). The rest is the send — T4 (typed count and re-resolve), T5 (a mail_log row per recipient before the attempt and the send loop), T6 (retry only the failed), T7 (no credential: the composer works and the send is disabled with the reason), T8 (one SMTP connection, TLS, no plaintext fallback) — all Critical (mail to real investors) and blocked on the SMTP credential (CRED-001) plus the send route, worked via critical-impl and deep-review when taken and built ahead of the credential. The broader frontier: the legal lanes (LEGAL-SG-001, and the rest of LEGAL-GLOBAL-001) are external-blocked on the compliance source texts the README names — a Singapore PDPA file and a GDPR file — which are not in the tree and which the README says those tasks cannot close without; the remaining W3 and W4 is UI on the peer-held Playwright lane and infra (OPS-001). Iter 57 takes the MAIL-001 send pipeline via critical-impl, or a W4 build-ahead mechanism. W2 stays blocked — CMS-DEC-03 and CMS-DEC-04, ADMIN-001. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · LEGAL-GLOBAL-001/T3 (1 task) · iter 55
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: LEGAL-GLOBAL-001/T3 — the read-tracking objection, honoured across the write path and the admin surface. accounts gains a read_tracking_objected boolean (folded into the accounts migration, DATA-R07; left out of the updated_at trigger's WHEN like last_sign_in, so exercising it does not restamp the account), and a new audit action is folded into the platform migration's action check and AUDIT_ACTIONS. apps/web/src/admin/accounts.ts:objectToReadTracking sets the flag, deletes the account's deck read and report read rows, and audits the act in one transaction (SEC-R04); the narrowed UPDATE is the check, so a second objection is a no-op. apps/web/src/content/decks.ts:recordDeckRead and apps/web/src/content/reports.ts:markReportRead now read the flag first and record nothing when it is set, so the objection stops future tracking while the room keeps working — unread marking degrades to everything looking new. No shared dev database recreation: the folded column is read only by recordDeckRead and markReportRead, and no shared-database test calls either — their callers are the isolated deck, report and admin-account suites, which recreate and migrate. Medium tier, a personal-data control. Verified against PostgreSQL 17.11: make check and check-app green, migrate-roundtrip green (both folds), 601 tests over 34 files (was 596 at iter 54). Mutation-proved, each KILLED and restored byte-identical: not setting the flag, inverting the record guard, and deleting the wrong read table. 168 closed.
NEXT: LEGAL-GLOBAL-001 is 3/5, and its remainder is docs and i18n behind the LEGAL-SG-001 base — T1 (the GDPR statements in twenty locales) waits on LEGAL-SG-001/T1 (the base privacy notice, unbuilt), T4 (the 72-hour breach runbook) on LEGAL-SG-001/T5, and T5 (the record of processing) is a legal document. The clean W3 backend is now nearly exhausted: what remains is UI on the peer-held Playwright lane (DECK-001, POST-001, RPT-001 remainders), Critical-tier mail (MAIL-001, MAIL-002), the LEGAL-SG-001 notice and runbooks (owner-review-sensitive legal content, LEGAL-SG-001/T4 a DPO decision), and decision-blocked W2. Iter 56 should re-walk the roadmap and, if no clean AI-buildable backend remains, consider LEGAL-SG-001/T1's notice as a grounded content-and-translation task or descend to a W4 reader-surface mechanism. W2 stays blocked — decisions CMS-DEC-03 and CMS-DEC-04, ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · LEGAL-GLOBAL-001/T2 (1 task) · iter 54
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: LEGAL-GLOBAL-001/T2 — the admin JSON export of everything held about one person (GDPR portability). apps/web/src/admin/accounts.ts:exportPersonData gathers the five things the system holds — the account's own fields (never the password hash), the decks granted, which deck versions and reports were opened and when, and the mail subjects and dates — each scoped to the one account (DATA-R05), and returns null for an id no account holds. It lives in the admin module beside eraseAccount, the sibling per-account privacy act (design §4). The grants read is the new apps/web/src/content/grants.ts:grantsForAccount reading content_grants, because content_grants is one of the four tables check-content-access confines to the content module; deck_reads, report_reads and mail_log are not confined, so the export reads them directly. What unblocked this was report_reads landing at iter 53. No new storage and no route yet — the admin surface is build-ahead. Medium tier, a personal-data read aggregation. Verified against PostgreSQL 17.11: make check and check-app green, 596 tests over 34 files (was 591 at iter 53; +3 export, +2 grantsForAccount). Mutation-proved, each KILLED and restored byte-identical: grantsForAccount dropping the pinned version, and exportPersonData widening its account scope (caught by the export scopes test and suspendAccount's isolation test). Note: the first check run failed because an earlier draft Evidence line put the word "make" before a plain word, which check-tasks parsed as a Makefile-target citation — reworded and re-ran green; memory filed. 167 closed.
NEXT: LEGAL-GLOBAL-001 is 1/5. T3 (an objection flag that stops writing deck_reads and report_reads and deletes the existing rows) is the next backend, but it needs a flag store — a fold on accounts adding the objection column — plus a check in recordDeckRead and markReportRead and a delete-and-audit act in the admin module: a moderate cross-surface change. T1 (the notice's GDPR statements in twenty locales) is blocked on LEGAL-SG-001/T1 (the base notice, unbuilt), T4 (the 72-hour breach runbook) on LEGAL-SG-001/T5, and T5 (record of processing) is a legal doc. After T3 the frontier is largely UI, docs, and the LEGAL-SG-001 base. W2 stays blocked — UI on Playwright, decisions CMS-DEC-03 and CMS-DEC-04, ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · RPT-002/T4,T6 (2 tasks) · iter 53
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: RPT-002/T4 (the room's current report) and RPT-002/T6 (the per-account report read state), both in the report domain. T4: apps/web/src/content/reports.ts:currentReport returns the published report of the greatest period the reader may read — by period rather than publication date, since the two differ when a late report is published after a newer one — composing visibleTo (CMS-R03) with the published pointer required explicitly, because an admin's predicate is TRUE and would otherwise make a draft current. Its tests need a pristine database (the query returns the global greatest-period row, which another suite's reports would pollute), so they live in a new file apps/web/src/content/content-report-current.test.ts on an isolated database of their own. T6: a new migration apps/web/migrations/1788758340000_report_reads.sql creates report_reads (one row per account per report, account_id and item_id cascading, deleted with the account per DATA-002, already declared in the manifest — the twin of deck_reads); apps/web/src/content/reports.ts:markReportRead records a read idempotently, so a re-read changes nothing: one read state, not a visit count. apps/web/src/db/types.ts and apps/web/src/db/db.test.ts carry the report_reads schema. Closing T6 unblocks LEGAL-GLOBAL-001/T2 (the export walks the read state) and T3 (objection deletes it). Medium tier. Verified against PostgreSQL 17.11: make check and check-app green, migrate-roundtrip green, 591 tests over 34 files (was 578 over 33 at iter 52; +4 current-report, +4 read-state, and the schema gate's new table). No shared dev database recreation — report_reads is new and no shared-database test touches it. Mutation-proved, each KILLED and the file restored byte-identical: currentReport ordering ascending, currentReport admitting an unpublished pointer, and markReportRead recording the account id where the report belongs. 166 closed.
NEXT: RPT-002 is 3/6; T2 (the taken-period publish failure naming the report and the two choices) and T3 (the archive by period and year with gaps) are surfaces over the constraint and the list (UI), and T5 (the withdrawal confirmation) is UI. With report_reads built, LEGAL-GLOBAL-001 unblocks at the data layer: T2 (an admin JSON export of everything held about one person — accounts, grants, deck_reads, report_reads, mail_log — an aggregation that names several content tables, so it lives in the content module or splits) and T3 (an objection flag that stops writing deck_reads and report_reads and deletes the existing rows — needs a flag store, likely a fold on accounts). Both are the next clean backend. W2 stays a blocked frontier — UI on Playwright, decisions CMS-DEC-03 and CMS-DEC-04, ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · DECK-002/T4 (1 task) · iter 52
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: DECK-002/T4 — the deck read record, the second and last piece of behavioural data the product keeps. A new migration apps/web/migrations/1788758240000_deck_reads.sql creates the deck_reads table: one row per account per deck version, account_id and deck_id both cascading, so it is deleted with the account (DATA-002), already declared in the erasure manifest docs/designs/data/data-002-erasure-and-retention.md. It is a new migration file rather than a fold, so it runs on every database — the fold hazard is only for editing a shipped migration; a new table is a new file, the pattern the auth_store and invitation migrations set. apps/web/src/content/decks.ts:recordDeckRead upserts on the account-deck-version key: the first open sets both timestamps, and each later open moves last_opened_at to now() while first_opened_at stays, so the row answers which version was shown and when it was first and last opened, and nothing more (no duration, no scroll, no sections — DECK-002 §6). apps/web/src/db/types.ts gains the DeckReadsTable interface, the Database entry and the SCHEMA descriptor; apps/web/src/db/db.test.ts gains the selectable and insertable samples. Medium tier — an additive table of personal behavioural data, with the erasure manifest as the privacy control. Verified against PostgreSQL 17.11: make check and check-app green, migrate-roundtrip green (the new down drops the table), 578 tests over 33 files (was 569 at iter 51; +4 read-record cases and the schema gate's new table). No shared dev database recreation — deck_reads is new and no shared-database test touches it; the isolated valotech_decks database gets it by recreating and migrating. Mutation-proved, each KILLED and the file restored byte-identical: updating first_opened_at instead of last_opened_at on a re-open, and dropping the version from the conflict target. 164 closed.
NEXT: DECK-002 is 4/6; T3 (an unpinned reader told once when the version changed, with what changed by section) and T5 (the publish confirmation naming every investor who will see the new version) are reader and publish surfaces (UI), though T3's section diff and T5's grantee query are mechanisms buildable ahead of them. Next clean W3 backend is RPT-002/T4 (the room's current report is the most recent period the reader may read — a query composing visibleTo, the shape of prefillStructureFor), then RPT-002/T6 (a per-account report read state — a new table plus erasure, the report_reads already declared in the manifest, the twin of deck_reads). LEGAL-GLOBAL-001/T2 (an admin JSON export over the erasure manifest's tables) remains. W2 stays a blocked frontier — UI on Playwright, decisions CMS-DEC-03 and CMS-DEC-04, ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · RPT-001/T2 (1 task) · iter 51
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: RPT-001/T2 — a new report opens with the previous report's structure rather than a blank page. apps/web/src/content/reports.ts is new: prefillStructureFor returns the heading blocks of the previous published report — the one of the greatest period below the new one, by string period order, so a skipped period falls back to the last that exists — with every non-heading block dropped, or DEFAULT_REPORT_STRUCTURE (the six suggested headings the design names) when there is no previous report. It composes visibleTo like every content read (CMS-R03), and the inner join to the published pointer keeps a draft from being carried forward. This resolves the "previous period" ambiguity flagged at iter 50 in favour of period order (greatest period below): the faithful reading of "the previous period's report" and the one that handles gaps — a loop-settled interpretation, not an owner decision, so nothing is filed. Build-ahead: the create route (no API layer yet) will wire it into createItem plus saveDraft; this is the structure it carries. Medium tier. Verified against PostgreSQL 17.11: make check and check-app green, 569 tests over 33 files (was 565 at iter 50; +4). New tests in apps/web/src/content/content-reports.test.ts on the isolated valotech_reports database with well-separated period ranges: a first report gets the suggested structure, a later one carries the greatest-below report's headings and none of its text, a gap falls back to the last that exists, and a draft is never carried. Mutation-proved, each KILLED and the file restored byte-identical: inverting the heading filter, ordering ascending rather than descending, and reversing the period comparison. 163 closed.
NEXT: RPT-001 is 3/5 now; T4 (progress-board values beside the section) and T5 (the public-report confirmation) are UI. Next clean W3 backend is DECK-002/T4 (a read record per account per version, deleted with the account) — a new deck_reads table, a record function, and erasure-manifest wiring, which both the deck reading view (DECK-003) and the from-the-deck grant view (DECK-004/T4) consume, so it is worth building ahead. Then LEGAL-GLOBAL-001/T2 (an admin JSON export of everything held about one person — a read across the erasure manifest's tables that names several content tables, so it lives in the content module or splits). W2 stays a blocked frontier — UI on Playwright, decisions CMS-DEC-03 and CMS-DEC-04, ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · RPT-001/T1,T3 (2 tasks) · iter 50
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: RPT-001/T1 (create a report against a period, fixed at creation) and RPT-001/T3 (metrics are figure blocks carrying their numbers as data). T1: apps/web/migrations/1788749077541_content.sql gains the content_items_period_format check (folded, DATA-R07) so a report's period is shaped YYYY-Qn or YYYY-MM in the database — matching the existing period-not-null check's enforce-in-the-database posture rather than a code-only validator; apps/web/src/content/items.ts:createItem stores it, and no function updates the period afterwards, so it is fixed at creation. The fold is additive-equivalent — no valid data violates it — so Medium tier and no shared-database recreation: a malformed period is written by no test, so the shared-DB suites stayed green, and the rejection is proven on the isolated valotech_reports database. The fold did break the aPeriod helper in apps/web/src/content/content-reports.test.ts, which minted p-uuid periods the check now refuses; it was fixed to a far-future YYYY-Qn per call, valid and unique. T3: a cross-reference close — the figure block in apps/web/src/content/blocks.ts already carries its numbers as a required data array (CMS-001), which is the whole mechanism, so no report-specific code is added (§1.10). Verified against PostgreSQL 17.11: make check and check-app green, migrate-roundtrip green, 565 tests over 33 files (was 557 at iter 49; +8 RPT-001/T1 cases). Mutation-proved on the isolated database, each KILLED and the migration restored byte-identical: a period pattern that accepts anything (the five reject tests fail) and one that rejects a valid quarter (the create path fails). 162 closed.
NEXT: RPT-001/T2 (a new report prefilled with the previous period's structure and none of its text) is the next clean RPT backend, but "the previous period's report" carries a real ambiguity — the immediately-prior period (which needs period ordering across YYYY-Qn and YYYY-MM) versus the most-recently-published report (recency, matching the design's "comparable with the last one"), plus gap handling — so resolve that before building the period module and the structure extraction. RPT-001/T4 (progress-board values beside the section) and T5 (the public confirmation) are UI. Other clean W3 backend: DECK-002/T4 (a read record per account per version, deleted with the account — a new table plus erasure-manifest wiring). W2 stays a blocked frontier — UI on Playwright, decisions CMS-DEC-03 and CMS-DEC-04, ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 when the report-publish surface lands.

## 2026-09-11 · DECK-001/T1,T5 + DECK-004/T1,T3 (4 tasks) · iter 49
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: four W3 backend tasks across two designs — deck authoring's section derivation and speaker context, and deck-access grants' audited grant/revoke and suspended-account refusal. DECK-001/T1: apps/web/src/content/sections.ts:deriveSections (new) splits a deck's blocks into sections at each level-2 heading, the block array the single source — the sections concatenate back to the input, a level-3 heading stays within its section, and a run before the first heading is one leading section. Pure logic, no database; the overview that will consume it (DECK-001/T2) is UI and unbuilt, so this is the mechanism built ahead of it. DECK-001/T5: a heading block gains an optional speaker context (apps/web/src/content/blocks.ts), surfaced per-section by deriveSections and removed by apps/web/src/content/blocks.ts:withoutSpeakerContext, which apps/web/src/content/decks.ts:deckRevisionFor applies so the reading view never carries it (the overview and the presenter print read the revision directly). The block vocabulary is jsonb, so no migration and no fold. DECK-004/T1: a cross-reference close — apps/web/src/content/grants.ts already audits both addGrant and removeGrant in the same transaction (SEC-R04) with the pinned version optional; a new isolated-database test proves the audit row per act, the idempotent re-grant, and the no-op revoke. DECK-004/T3: apps/web/src/content/grants.ts:addGrant now locks the account row and refuses a grant to a suspended account with the reason (the invitation lock pattern), while an invited or active account is allowed. Medium tier, main session. Verified against PostgreSQL 17.11: make check and check-app green, 557 tests over 33 files (was 537 over 31 at iter 48). New: apps/web/src/content/sections.test.ts (10, pure) and apps/web/src/content/content-grants.test.ts (7, isolated database valotech_grants), plus two validateBlocks context cases in content.test.ts and the T5 read-path strip in content-decks.test.ts. Mutation-proved, each KILLED with its specific failing assertions read and the file restored byte-identical: splitting sections at level 3 rather than level 2; the strip targeting non-headings; dropping the strip from deckRevisionFor; inverting the suspended-account check; and recording the wrong audit action. No shared dev database recreation this iter — the context field is optional (existing headings carry none) and the grant state-check refuses only a suspended account (apps/web/src/content/read.test.ts grants to an active one), so the shared-database suites stayed green. 160 closed.
NEXT: DECK-001 is 2/5 and DECK-004 is 2/6; both remainders are UI — DECK-001/T2 (overview cards), T3 (reorder by drag and keyboard) and T4 (counts) wait on the overview surface (peer-held Playwright), and DECK-004/T2 (the confirmation wording), T4 and T5 (the two views) and T6 (bulk grant's confirmation) are UI over buildable mechanisms. Next clean W3 backend candidate is RPT-001/T1 (create a report against a period): apps/web/src/content/items.ts:createItem already takes a period and the database enforces that a report has one, but the format (YYYY-Qn or YYYY-MM) is unenforced — closing it well is a choice between a folded CHECK constraint (the enforce-in-the-database posture, at the shared-database recreation cost) and a domain validator, and deserves its own iter. Also LEGAL-GLOBAL-001/T2 (an admin JSON export over the erasure manifest's tables — it names several content tables, so it lives in the content module or splits). W2 stays a blocked frontier — UI on Playwright, decisions CMS-DEC-03 and CMS-DEC-04, ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 and its T3 when the report-publish surface and the period-format enumeration land.

## 2026-09-11 · DECK-002/T2,T6 (2 tasks) · iter 48
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: an optional pinned deck version on a grant and its resolution (DECK-002/T2,T6). content_grants gains a nullable pinned_version (folded, DATA-R07); apps/web/src/content/grants.ts:addGrant takes an optional pin (null default); apps/web/src/content/decks.ts:deckRevisionFor (new) resolves it — a grantee pinned to version n reads that revision, an unpinned grantee and an admin read the current published one — composing apps/web/src/content/access.ts:visibleTo first, so a reader who may not see the deck gets nothing whatever a pin would resolve to (CMS-006). A pin survives withdrawal (T6): deckRevisionFor finds the revision by its version, not through the item's current pointer, and apps/web/src/content/publish.ts:withdraw never deletes a revision or clears its version. pinned_version is Generated<number | null> so addGrant may omit it. Medium tier, cross-reader access. Verified against PostgreSQL 17.11: make check and check-app green, 537 tests over 31 files (was 533 at iter 47). apps/web/src/content/content-decks.test.ts (9 tests, isolated database): the pin stored and null when unpinned, a pinned grantee reading version 1 while an unpinned one reads version 2 after a new publication, a non-grantee getting null and an admin the current version, and the withdrawn-pin case (version 2 pinned still serves version 2 after a withdrawal to version 1). Mutation-proved, each KILLED and restored byte-identical: inverting the pinned-versus-current branch serves the wrong version, and dropping the visibleTo clause serves a non-grantee. Folding pinned_version required recreating the shared dev database again (apps/web/src/content/read.test.ts calls addGrant, which now writes the column) — the standing fold-staleness (memory valotech-shared-dev-db-stale-to-folds). 156 closed.
NEXT: DECK-002 is 3/6 and its backend is essentially done — the remainder is UI: T3 (a section-level change diff, and the note when an unpinned reader's version moves) and T5 (the publish confirmation naming grantees), plus T4 (a deck_reads record) which wants the read surface. Next clean W3 backend candidates: DECK-004/T1 (grant and revoke, audited, the pin optional — addGrant and removeGrant with the pin exist now, so partly done, though the confirmation naming what the person will read is UI); RPT-001/T1 (create a report against a period — may close by cross-reference to createItem plus the period CHECK); LEGAL-GLOBAL-001/T2 (an admin JSON export of everything held about one person — a data-aggregation over the erasure manifest's tables, but it names several content tables so it must live in the content module or split). W2 stays a blocked frontier — UI on the peer-held Playwright lane, decisions (CMS-DEC-03 and CMS-DEC-04), ADMIN-001 entangled. 9 open owner decisions. Owed: the CMS-002/T1,T2,T6 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7.

## 2026-09-11 · DECK-002/T1 (1 task) · iter 47
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: a monotonic deck version at publication (DECK-002/T1), and a robustness fix to CMS-007's blocks_text that testing it surfaced. apps/web/migrations/1788749077541_content.sql folds a nullable content_revisions.version; apps/web/src/content/publish.ts:versionForPublish assigns it inside the publish transaction only for a deck and only when the revision has none yet — coalesce(max(version for this deck), 0) + 1, read under the item-row lock apps/web/src/content/publish.ts:lockItem holds so two publications of one deck cannot collide. A re-publish keeps the version a grantee read (it keeps resolving, CMS-R01); a report or an update takes none (the column stays null and no non-deck publish reads it); a withdrawal leaves a hole rather than reusing. version is Generated<number | null> in apps/web/src/db/types.ts and omitted from apps/web/src/content/items.ts:ContentRevision until DECK-003 needs it. Medium tier, additive schema plus a gated branch in the publish machinery. Verified against PostgreSQL 17.11: make check and check-app green, 533 tests over 31 files (was 527 at iter 46). apps/web/src/content/content-decks.test.ts (5 tests, isolated database): number from one, each deck from its own one, a re-publish keeps its version, a withdrawal leaves a hole (next is max + 1), and an update takes null. Mutation-proved, 3 of 3 each KILLED and restored byte-identical: inverting the deck gate, turning max + 1 into + 0, and inverting the re-publish guard. FOLD HAZARD, twice over: testing this fold needed apps/web/src/content/read.test.ts (which publishes a deck) to carry the version column, but the shared development database is stale to every fold node-pg-migrate will not re-apply (search from iter 43, the report-period index from iter 45, version now), so I recreated it (drop then make migrate — dev seeds are disposable, §7.2). That surfaced a real latent bug: CMS-007's blocks_text called jsonb_array_elements on a body that is not a JSON array and threw, failing the write of one (apps/web/src/content/locales.test.ts inserts a non-array dummy) — a generated column runs on every write and must not throw. Fixed with a jsonb_typeof guard (a non-array body yields no text), migrate-roundtrip green, a regression added to apps/web/src/content/content-search.test.ts. Both were hidden by the stale local database and would have hit a fresh CI run or a deploy. 154 closed.
NEXT: W3 backend. DECK-002's remainder: T2 (an optional pinned_version on content_grants — a schema column plus the read resolving it, partly clean backend) with T6 (withdrawal keeps a pin, which follows from T2); T3 (a section-level change diff) and T5 (the publish confirmation naming grantees) are UI; T4 (a deck_reads table) wants the read surface. Other clean-ish W3 backend: RPT-002/T3 needs a period enumeration and the read state; MAIL-001's send flow is UI plus the credential-blocked adapter. STANDING: the local dev database is stale to folds, so a folded-schema test must run on a fresh isolated database, and recreating the shared one applies every pending fold at once. W2 stays a blocked frontier — UI on the peer-held Playwright lane, decisions (CMS-DEC-03 and CMS-DEC-04), ADMIN-001 entangled, CMS-004/T6 a CDN purge. 9 open owner decisions. Owed: the CMS-002/T1,T2,T6 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7.

## 2026-09-11 · MAIL-001/T1 (1 task) · iter 46
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: the mail port and the message composer (MAIL-001/T1), the foundation of investor mail. apps/web/src/mail/mailer.ts declares the Mailer port — send(to, subject, text, html) returning a Receipt that carries only the SMTP queue id — behind which the single SMTP adapter will live (MAIL-DEC-01 resolved, MAIL-001/T8), and apps/web/src/mail/mailer.ts:compose renders a subject and a plain-text body into the ComposedMessage a send transmits and a preview shows, so the two are the same bytes: the text half is the body verbatim, the HTML half is that same body split into paragraphs on blank lines, each escaped so its content cannot be read as markup and a single newline becoming a line break, inventing no structure the author did not write. No templating in the port — the message is data before it reaches the wire. Medium tier, no new dependency (the SMTP library is T8's; config.mail already reads SMTP_URL and MAIL_FROM and reports unavailable-with-reason, from AUTH-003 and CFG-001). Verified: make check and check-app green, 527 tests over 30 files (was 520 at iter 45). apps/web/src/mail/mailer.test.ts (7 tests, pure): subject passthrough, text verbatim, the markup characters escaped so a script tag cannot survive, paragraph split, line break, and an empty body rendering no HTML. Mutation-proved, 4 of 4 each KILLED and restored byte-identical: dropping the `<` escape (injection), blanking the text half, removing the line break, and admitting empty paragraphs. Solo iter — MAIL-001/T7 (no-credential behaviour) needs the recipient list and UI; T2 through T6 (the send pipeline) and T8 (the SMTP adapter, credential-blocked on SMTP_URL) build on this port. 153 closed.
NEXT: W3 backend. MAIL-001's remainder is largely the send flow, entangled: T2 (recipients a confirmed list) and T4 (a typed-count send) are the send surface (UI); T5 (a mail_log row per recipient before the attempt) and the actual send need the adapter T8, which is credential-blocked; T3 (excluded accounts shown) is UI. The cleaner next W3 backend is DECK-002/T1 (a monotonic deck version assigned at publication, never reused, holes kept — a schema plus assignment logic), then RPT-002/T3 (the archive, once a period enumeration and the per-account read state exist). W2 stays a blocked frontier — UI on the peer-held Playwright lane, decisions (CMS-DEC-03 and CMS-DEC-04), ADMIN-001 entangled, CMS-004/T6 a CDN purge. 9 open owner decisions. Owed: the CMS-002/T1,T2,T6 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 and T3 when the report-publish surface and a period format land.

## 2026-09-11 · RPT-002/T1 (1 task) · iter 45
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: a partial unique index making "one published report per period" a database guarantee (RPT-002/T1). apps/web/migrations/1788749077541_content.sql folds one_published_report_per_period on content_items (type, period) WHERE type = 'report' AND current_revision_id IS NOT NULL: publishing a second report into a taken period fails at the pointer move, a draft replacement is exempt (partial on the published pointer), and it is on the item not the revision (CMS-R01) so a corrected report is a new revision and withdrawing frees the period. Medium tier, additive schema, folded per DATA-R07 (make migrate-roundtrip green — up, down, up — and the index drops with the table). Verified against PostgreSQL 17.11 by apps/web/src/content/content-reports.test.ts (5 tests, isolated database): the refusal by the index by name, the exempt draft, a same-item re-publish, distinct periods, and withdrawal freeing the period. Mutation-proved and restored byte-identical: weakening the index to a non-unique one lets the second report publish, reddening the refusal test. FOLD HAZARD confirmed and handled: the shared development database does not carry the folded index — node-pg-migrate is idempotent and will not re-apply a folded migration (checked: pg_indexes returned 0 rows) — so the test runs on a fresh isolated database that gets the fold rather than the shared-database model suite, the convention CMS-007's search column already followed. Solo iter: RPT-002/T2 (the friendly period-taken error) is entangled — apps/web/src/content/publish.ts is CMS-004's and cannot import RPT-002 without reversing the depends_on, and the holder-naming surface is unbuilt — and T3 (archive) needs a period-format enumeration and the read state (T6). make check and check-app green, 520 tests over 29 files (was 515 at iter 44). 152 closed.
NEXT: W3 backend. MAIL-001/T1 (the Mailer port and the composer that renders the send's exact bytes) is the next substantial clean build-ahead — MAIL-DEC-01 (the SMTP carrier) is already resolved, so the port interface and the pure composer build now with the SMTP adapter (T8) credential-blocked, the way AUTH-003/T7's no-credential path works; it is higher in W3 order than RPT-002 and unblocks the mail pipeline. Other W3 backend: DECK-002/T1 (a monotonic deck version assigned at publication). W2 stays a blocked frontier — UI on the peer-held Playwright lane (CMS-002/T3,T4,T5, CMS-004/T4, CMS-005/T4,T6), decisions (CMS-DEC-03 and CMS-DEC-04), ADMIN-001 entangled, CMS-004/T6 a CDN purge. 9 open owner decisions. Owed: the CMS-002/T1,T2,T6 browser check; a deterministic FOR UPDATE lock test for CMS-003/T7; RPT-002/T2 and T3 when the report-publish surface and a period format land.
