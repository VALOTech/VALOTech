# dev1 iteration log

> **Not for `main`.** This file lives on `development` only (`.claude/CLAUDE.md` §1.1).

Working state, not an archive. Each iteration of `/dev1` appends one entry and
prunes the oldest until **twenty** remain — the bound is held by
`scripts/check-log-retention.py` and the reason it is safe is §13.1: an entry
is deleted only once every durable fact in it already lives in its permanent
home, which is `docs/tasks.md` for task state, `docs/decisions-log.md` for a
choice, and `docs/operator-checklist.md` for something waiting on a person.
`git log -p docs/dev1-iter-log.md` is the archive, so a pruned entry is
recoverable verbatim and no second copy is kept here.

A cold start reads the tail of this file to find out where the last one
stopped. `OUTCOME:` is fixed vocabulary so that reading can be mechanical:
`CLOSED` when at least one task moved to `[x]`, `IDLE` when the queue was
stale and the loop re-fires, `STOP` when it ended and the owner is needed.
`REASON:` on a non-`CLOSED` entry is one stable phrase, reused verbatim when
the same condition recurs, so three of them in a row can be detected rather
than noticed.

---

## 2026-09-13 · CMS-004/T7, DECK-001/T2, DECK-001/T4 (3 tasks) · iter 87
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-004/T7, DECK-001/T2 and DECK-001/T4 — a refusal the store can express, and the deck overview deriveSections has been waiting for. **T7 was filed last iteration from a measurement and is now the thing measured**: publish and withdraw raised 22P02 on an identifier that is not one — a 500 whose message repeats the value supplied (DATA-R02) — and NoResultError on a well-formed one naming nothing. apps/web/src/content/publish.ts:NoSuchItemError is both, because they are one fact: there is nothing here to move a pointer on, and answering them differently tells a caller which identifiers are well-formed. The shape is asked before the value reaches the column, through apps/web/src/content/items.ts:isItemId, which apps/web/src/content/overview.ts also reads — and the mutant that accepts anything reddens apps/web/src/app/admin/content/[id]/publish.test.ts and apps/web/src/content/overview.test.ts together, which is what says it is one rule and not a copy per caller. **DECK-001/T1 closed with deriveSections having only its own test as a caller** — the fifth all-rows-closed-but-inert instance — so nothing in the product ever derived a section. apps/web/src/content/overview.ts:overviewFor and apps/web/src/app/admin/content/[id]/overview/page.tsx are that caller. **The design named /admin/decks/<id>/overview and there is no decks section of the console**: the rail lists seven destinations and none is decks, a deck is a content item, and /admin/content/<id> is already an item’s screen — so a second hub would be one the rail cannot name and nothing links to. DECK-001 §3 now names the hub that exists, and so does DECK-004 §3, which pointed at the same phantom and would have sent W4 to re-derive the answer. A card carries the heading, the first line of prose, and what the section holds in words rather than as a mark (A11Y-R02); a picture is not a line, which is why §3 asks for both. The tally leads the page, because the number is the point. What counts as a word is the decision the count is only honest with: prose a reader receives, never alternative text (a deck of photographs would read as long as one of argument) and never speaker context (served to nobody). Verified against PostgreSQL 17.11: 1052 tests over 60 files, none skipped, at --maxWorkers=1 in 152s. Nine mutants KILLED with no timeout and every file restored byte-identical. My own error: the overview fixture used a figure whose mediaId nothing had stored, which saveDraft rightly refuses — figure counting belongs to the pure suite, and the database fixture now carries no unstored media. 220 closed.
NEXT: W2 is closed but for CMS-004/T6, which waits on [CMS-DEC-07](decisions-log.md#CMS-DEC-07), so the descent is in W3 from here. **DECK-001/T3 is the next row on the surface just built** — reordering by drag and by keyboard, writing back to the block array — and it carries a constraint this iteration created: the overview renders cards keyed by index, which is correct for a list that does not move and wrong the moment one does, so T3 needs a client component with a key that survives a reorder. A section has no stored identity (the block array is the single source), so that key has to be derived and held for the life of the drag rather than read from the data. The rest of W3 in roadmap order: DECK-001/T3, then LEGAL-SG-001 (six rows, none blocked), MAIL-001/T4, T7 and T8 — T8 being the SMTP adapter on nodemailer whose credential is the only external residue, and the row that would unchain AUTH-003/T3, MAIL-001/T6 and the third surface of [~] SEC-001/T4 — then POST-001’s six and RPT-001/T4. Owed and still in the ledger: a browser drive of seven admin surfaces now, this overview included; three servers on this box serve the one apps/web/.next and Turbopack refuses an isolated build. Eight owner decisions are open; one blocks a task. **Not a defect but worth knowing**: apps/web/src/content/grants.ts has no production caller either, and that is honestly recorded rather than hidden — CMS-006/T3 closed on the mechanism and named DECK-004 as the surface, whose T2, T4, T5 and T6 are open in W4.

## 2026-09-13 · RPT-001/T5, POST-002/T5, RPT-002/T5 (3 tasks) · iter 86
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: RPT-001/T5, POST-002/T5 and RPT-002/T5 — three rows, one surface: the sentences the item screen is allowed to say before an act. apps/web/src/content/audience.ts:changeAudience had shipped with its row lock, its no-op guard and its content.audience_change audit all proven at CMS-006/T6 and **no production caller**, so an item held whichever audience createItem was given and nothing in the product could change it. The control (apps/web/src/app/admin/content/[id]/audience-control.tsx) and its route are that caller, and the work was the words rather than the write. **Public says what public means**: anyone, including a competitor, no sign-in, linkable and indexable — not the name of an audience an admin has to decode. **Narrowing says what it cannot recall**: a page somebody has open stays on their screen, and a copy fetched while the item was public may sit in a browser for ten minutes. **That ten minutes is now one constant** — apps/web/src/content/audience.ts:PUBLIC_CACHE_SECONDS, which apps/web/src/app/content/[id]/route.ts reads instead of declaring a second 600 — and the mutant that matters moved it to 900 and reddened the new suite and apps/web/src/content/content-serve.test.ts together, which is what says the window an admin is told and the window a reader is granted are one number. **The grant count is the fact that changes an answer**: narrowing to granted with nothing granted turns a document every investor could read into one nobody can, so the confirmation counts and says so. For a report, withdrawal now states the period becomes a gap and which report becomes current — **but only when there will be a gap**: RPT-002 §3 assumes there always is, while withdrawing moves the pointer to the revision published before, so a report on its second revision falls back to its first and keeps its period. Read rather than assumed. Verified against PostgreSQL 17.11: 1026 tests over 59 files, none skipped, at --maxWorkers=1 in 152s, with 15 new cases on a database of its own. Eight mutants KILLED with no timeout and every file restored byte-identical: the direction of a move inverted, a cache window claimed for every item, the grant count always zero, the shared constant moved, the vocabulary check dropped, a missing item not refused, a gap always promised, and the successor query without its self-exclusion. My own error: the first probe of a sibling route ran without a session, so requireAdmin answered before any query and the reading proved nothing — re-run with an admin cookie it raised 22P02. 217 closed.
NEXT: **CMS-004/T6 is blocked, not skipped, and the reason is a contradiction between designs.** CMS-004 §3, CMS-006 §3 and POST-002 §3 all say a public document may sit in a CDN and the cache is purged on publish and withdraw; OPS-001 §3 settles that Cloudflare caches the assets and never the documents, because every route renders per request for the CSP nonce. So there is no cache of a document to purge, and the one that does hold a public copy is the reader’s own browser, which no server reaches. Two doctrine clauses conflicting with neither precedence case applying is the owner’s to settle (§1.11), so it is [CMS-DEC-07](decisions-log.md#CMS-DEC-07) — keep the ten minutes, revalidate with an ETag, or build the purge and reverse OPS-001 — with the shipped lifetime as the safe default and the recommendation resting on one concrete thing: under the first answer the withdrawal confirmation has to end "and a reader who already opened it may still see it for up to ten minutes". CMS-007/T5 and T6 now carry their real blocker rather than looking actionable: CMS-007 §2 puts the field above the stream and §4 names INV-001 as its surface, and no room page exists. **The next buildable in W2 is CMS-004/T7, filed this iteration from a measurement rather than a suspicion**: publish and withdraw raise 22P02 on an identifier that is not one, a 500 whose message repeats the value supplied, which CMS-006 §6 says a route must not do; the audience route answers that as a 404 already, so the shape is settled and the two siblings have to catch up — it wants a refusal the store functions can express rather than a shape check copied into each route. Owed and still in the ledger: a browser drive of six admin surfaces now, this audience panel included. Three servers on this box serve the one apps/web/.next and Turbopack refuses an isolated build, so it waits for a free window or a distDir the environment can point elsewhere. Eight owner decisions are open; one now blocks a task.

## 2026-09-13 · CMS-004/T1,T2 (2 tasks) · iter 85
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-004/T1 and T2 — a draft can be previewed the way a reader will get it. Two iterations deferred this because preview must render "through the reader’s own components" and nothing rendered a block outside the editor’s field widgets; the resolution is that a renderer built inside the preview is the thing the design forbids, so apps/web/src/content/render.tsx is shared content machinery every reading surface goes through, with RPT-003/T1 and DECK-003/T1 as its next callers — both are page layouts over rendered blocks rather than renderers. Marks are offsets rather than nesting, so a paragraph is cut at every mark boundary and each piece wrapped in the elements its marks name; that splitting existed only inside the translation module, and apps/web/src/content/marks.ts:piecesOf is now where both callers reach it. **The audience is evaluated, never bypassed, and through the rule the reader’s own query composes**: apps/web/src/content/access.ts:visibleTo is now built from an audienceAdmits clause rather than beside one, so a preview asks the same question a reader’s read asks minus the published check it deliberately relaxes. Previewing an investor-only item as a visitor shows nothing, which is the answer an admin needs. A role is a generic member of its kind, so a granted item admits no investor and the admin’s own view is the default — which is how they read a granted item at all. T2 is structural rather than a new check: the preview is a page under /admin, so requireAdminPage gates it before it renders, and the only two parameters are each checked against a closed set with anything else a 404, so no crafted URL widens what is shown and there is no token for one to go in. Verified against PostgreSQL 17.11: 1011 tests over 58 files, none skipped at --maxWorkers=1, with 12 pure render cases and 11 preview cases. Six mutants KILLED with no timeout and every file restored byte-identical: the audience bypassed, the published revision read instead of the latest, a generic investor admitted to a granted item, the same clause widened under the reader’s own predicate, a mark covering the whole paragraph, and an image losing its alternative text. **The fourth is the one worth keeping**: widening the shared clause failed four cases in read.test.ts as well, which is what says the clause is one rule rather than a copy that happens to agree today. Three casts were written and removed rather than shipped — localeFor’s parameter narrowed to the two columns it reads, a rest-object that claimed to be a row, and a locale narrowed instead of asserted; each was a claim about data the code had not fetched. My own error: the first render test asserted a bare link where the overlap genuinely nests both marks, and the renderer was right. 214 closed.
NEXT: CMS-004 has one row left: T6, a public item’s cache purged on publish and withdraw. The cache is a Cache-Control lifetime on apps/web/src/app/content/[id]/route.ts rather than anything this application holds, so a purge means a CDN and a credential — build-ahead territory: a port, a default that does nothing but say so, and the lifetime as the bound on staleness meanwhile. Then CMS-007/T5 and T6, which want a search field in a room whose reader pages are W4 — worth checking whether the field belongs to the console instead, because the search read itself is closed and proven. **The renderer landing changes the W4 picture**: RPT-003/T1 and DECK-003/T1 are now page layouts over something that exists rather than greenfield, which makes the W3/W4 reading views considerably cheaper than the roadmap’s depth suggests. Owed and in the ledger: a browser drive of five admin surfaces now — the translation review screen, the content list, the settings screen, the item screen and this preview. Three servers on this box serve the one apps/web/.next and an isolated build is refused by Turbopack, so it waits for a free window or a distDir the environment can point elsewhere. Seven owner decisions are open; none blocks a task.

## 2026-09-13 · CFG-001/T8 + CMS-004/T4 (2 tasks) · iter 84
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CFG-001/T8 and CMS-004/T4 — two features whose stores were closed and proven with nothing calling them. CFG-001 was marked live while changeSetting, revertSetting and getSettings had no caller anywhere, no page existed at /admin/config and neither route its own design names was built; publish and withdraw were in the same state, so no page in the product could publish anything. apps/web/src/app/admin/config/page.tsx over apps/web/src/config/settings.ts:settingsForConsole is the settings screen, and **the list is the registry rather than the table** — a key nobody has changed appears showing the default the application is actually reading, because the setting an operator needs at three in the morning is usually the one nobody has touched. Changing takes a typed value and an explicit Save; reverting is one action with no confirmation, because a confirmation on the safe direction trains people through the one on the dangerous direction. A refused value comes back with the bound it missed and nothing is written. apps/web/src/app/admin/content/[id]/page.tsx is the item’s own screen, the hub the content list now opens, and the publish confirmation states the two things the screen cannot otherwise tell you: the version being replaced, and how many languages are reviewed against how many will read the English. That count is of the revision being published rather than of the item — locale rows belong to a revision and a new one starts with none, so the item’s count would report a translation of text nobody is about to be shown. Withdrawing carries no dialogue and still says which of its two outcomes will happen before it is pressed. Verified against PostgreSQL 17.11: 988 tests over 56 files, none skipped at --maxWorkers=1, with 17 on the settings surface and 14 on the item’s. Seven mutants KILLED with no timeout and every file restored byte-identical: the settings list taken from the table so an unchanged key disappears, a refused value clamped instead of refused, any key accepted as configurable, the confirmation never naming a replaced version, the language count taken from the item, withdrawing jumping to the oldest version, and an empty revision id accepted. **Two of those mutants survived a first pass and the tests were sharpened rather than the mutants dropped** — one revision cannot show the difference between the item’s reviewed languages and the revision’s, and two published versions cannot show the difference between one step back and the oldest, so the suite now uses two and three. A third attempted mutant was withdrawn as unreachable and said so rather than counted: the published_at narrowing in withdrawReturnsTo guards a state the store cannot produce. My own error, for the third iteration running: a test named a content table directly and check-content-access refused it; a suite outside apps/web/src/content must read through the module, and this one counts revisions through localeGrid. CFG-001 advances to implemented and live; the content list’s title now opens the item screen rather than the editor, as the account list’s name opens a person, and CMS-002/T8’s evidence says so. 212 closed.
NEXT: CMS-004 has three rows left and two of them want something that does not exist: T1 and T2 ask preview to render through the reader’s own components, and nothing renders a block outside the editor — RPT-003/T1 and DECK-003/T1 would build the first one, so whoever takes T1 decides first whether preview builds that renderer or waits for it, because the design says preview must not have one of its own. T6 wants a public item’s cache purged on publish and withdraw, and the cache is a Cache-Control lifetime on apps/web/src/app/content/[id]/route.ts rather than anything this application holds, so a purge means a CDN and a credential — build-ahead territory: a port with a default no-op adapter, leaving the token external. CMS-007/T5 and T6 want a search field in a room whose reader pages are W4. **The rail now resolves everywhere except Portfolio (INV-003/T4, open and honest), so the three-in-three run of features that were complete with no surface is closed out.** The next anchor is CMS-007/T5+T6 or the CMS-004/T6 build-ahead, and either is a fair descent. Owed and in the ledger: a browser drive of the translation review screen, the content list, the settings screen and the item screen — four surfaces now. Three servers on this box serve the one apps/web/.next and an isolated build is refused by Turbopack, so it waits for a free window or a distDir the environment can point elsewhere. Seven owner decisions are open; none blocks a task.

## 2026-09-13 · CMS-002/T8,T9 (2 tasks) · iter 83
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-002/T8 and T9 — the console lists what an admin has written, and can start some. Before them the rail listed Content and no such page existed, so nothing in the product linked to an item and the editor, the translations and the files a document uses were reachable only by somebody who already knew an item’s id; and apps/web/src/content/items.ts:createItem had no production caller at all, so a list would have been one nothing could add to. apps/web/src/content/items.ts:itemsForConsole is the read: title, type, audience, whether a reader sees anything today, whether later work exists that nobody has seen, how many languages are ready, and when it last changed, most recently changed first with a saved revision counting as a change. Published and drafted are two cells rather than one word, because an item can be both and that pair is the row an author comes to find. The title opens the editor and the languages cell opens the translation grid, which are different places and the reason the list exists. apps/web/src/app/admin/content/create/route.ts and apps/web/src/app/admin/content/new/page.tsx start one: the type is asked first because it decides the rest, the address is offered from the title and stays the author’s, and creating is not publishing — the row carries no revision and the form says so. Verified against PostgreSQL 17.11: 957 tests over 54 files, none skipped at --maxWorkers=1, with 5 on the list (on a database of its own, because a read scoped to nothing would otherwise answer with every other suite’s items) and 19 on the route. Six mutants KILLED with no timeout and every file restored byte-identical: the order reversed, the change read from the item’s own timestamp so a saved revision moves nothing, the open draft always false, the language count including the ones served to nobody, an update createable with no kind, and any string accepted as an address. Two of my own errors are worth keeping. The create suite named content_items directly and check-content-access refused it — the same gate that caught me at iter 81 — so it reads back through itemsForConsole now, which is what the rule is for. And the any-address mutant created the malformed slugs the suite posts, whose rows outlived the restore and then failed the honest test: a mutant that makes a write succeed leaves residue on a shared database, so those addresses are unique per run now. Three ledger truths written down. AUTH-003/T3 is [!] Blocked by MAIL-001/T8, which its own design has said since it was written. CFG-001 was live with 7/7 closed while nothing reads or writes a setting — no page, neither route its design names, no caller for getSettings, changeSetting or revertSetting — so it is building with CFG-001/T8 filed. And CMS-001/T6’s evidence claimed forReader and forAuthor were the only reads the content module exposes, which later work falsified three times over; it now states the rule that is actually held. CMS-002 and CMS-005 both advance to implemented and live. 210 closed.
NEXT: CFG-001/T8 — the console’s settings screen — is the next anchor: the store beneath it is closed and proven, the design names both routes, and it is the last rail destination that leads nowhere now that Content resolves (Portfolio is INV-003/T4, which is open and honest about it). After it, CMS-004 is the largest remaining W2 lane, and two of its rows want something that does not exist yet: T1 and T2 ask preview to render through the reader’s own components, and nothing renders a block outside the editor — RPT-003/T1 and DECK-003/T1 are the tasks that would build one, so whoever takes CMS-004/T1 should decide first whether preview builds that renderer or waits for it, because the design says preview must not have one of its own. T4 wants a publish confirmation and no page carries a publish control at all. CMS-007/T5 and T6 want a search field in a room whose reader pages are W4. Owed and in the ledger: a browser drive of the translation review screen and of this list — three servers on this box serve the one apps/web/.next, and an isolated build is refused by Turbopack because a node_modules junction points out of the project root. Seven owner decisions are open; none blocks a task.

## 2026-09-13 · CMS-001/T7 + CMS-005/T3,T4 (3 tasks) · iter 82
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: Three tasks, and the second and third are the ones that matter. CMS-001/T7 — apps/web/src/content/items.ts:saveDraft is the one place an item’s blocks are written, so it is the one place the rows depending on them can be kept in step, and it now brings two into step inside its own transaction. media_refs is made equal to what the item names, taken over every revision rather than over the draft just written: a revision is never deleted and withdrawing can restore an earlier published one, so a file dropped from today’s draft is still the file that document would show if it came back. What does leave is a file named only by the draft that was replaced. A block naming a file the library does not hold is refused, because the block validator is pure and can say a mediaId is a string but never whether anything is stored under it. That closes CMS-003’s inert_until, which named exactly this task: a stored file is now provably not servable while its item is a draft and servable the moment it publishes, so CMS-003 and CMS-001 both advance to implemented and live. CMS-005/T3 and T4 — a language can be started from the source and reviewed. Starting copies the row inside the database so the document lands byte for byte; reviewing sends strings, never blocks, and the document is rebuilt on the server from the source, so no caller can give one language a structure or a picture the others do not have. Two things were measured rather than assumed, and both changed the build. First, marks: my first implementation edited a paragraph as one field and handed the new text to marks.ts:remapMarks — which reads a wholly replaced paragraph as one edited run and grows every mark to cover it, so a link over two English words would have become a link over the whole translation. The test caught it and my expectation was the thing that was wrong, not the code; the answer is to cut a paragraph at its mark boundaries and offer each piece as its own field, which is what CMS-005 meant by a translator keeping the marks and changing only the words. Second, the axis review found that only the source was validated, so a translator emptying an image’s alternative text could ship an unlabelled image in one language (A11Y-R02); the rebuilt document is validated too. Also closed a hole the seed opened: saveDraft replaces the open draft rather than adding a revision, so a translation of it would keep its reviewed state over words that moved — and be served. The save now drops that revision’s locale rows. Verified against PostgreSQL 17.11: 933 tests over 52 files, none skipped at --maxWorkers=1, with 21 pure tests on the field walk, 12 on the reference sync (on a database of its own, because the media store is keyed by the bytes and a shared database carries a previous run’s references), 12 on seeding and reviewing, and 16 on the two routes. Seven mutants KILLED with no timeout and every file restored byte-identical: the reference never removed, the set taken from the open draft alone, the unstored-file refusal dropped, the translation left to outlive its text, the seed taken from the oldest revision, the paragraph left uncut, and the rebuilt document left unvalidated. Filed CMS-002/T8: the console’s rail lists Content and no such page exists, so nothing links to an item and both the editor and these new screens are reachable only by a typed URL — CMS-005 is therefore in-progress with inert_until naming it, and CMS-002 drops to a build underway. 208 closed.
NEXT: CMS-002/T8 — the console lists an admin’s content — is the next anchor, and it is worth more than its size: it is the entrance to the editor, to the media a document uses and to the translation screens this iteration built, and until it lands every one of them is reachable only by someone who already knows an item’s id. It is also the second time in two iterations that a feature was complete and unreachable, which is worth noticing rather than closing twice. After it: CMS-004’s preview and publish (T1, T2, T4, T6), which is the largest remaining W2 lane, and CMS-007/T5 and T6. Owed and named in the ledger: a browser drive of the review screen. It could not be done here — three servers on this box serve from the one apps/web/.next and rebuilding it answers their pages with 404s, while an isolated build is refused by Turbopack because a node_modules junction points out of the project root and copying node_modules is impractical on this filesystem. A `distDir` this repository can point elsewhere from the environment would end that standoff for every lane and is one line in apps/web/next.config.mjs; it is not filed, because it is a convenience rather than product, and the next lane that wants a browser should either take it or wait for a free window. The mail chain is still the W1 blocker — AUTH-003/T3 has no send path until MAIL-001/T8, and its ledger row does not say so. Seven owner decisions are open; none blocks a task.

## 2026-09-13 · CMS-003/T10 (1 task) · iter 81
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-003/T10 — an admin can now see what is in the media library and take a file out of it, which closes the tenth of CMS-003’s ten rows. apps/web/src/content/media.ts:libraryContents is the staff read: kind, size, when it arrived, who brought it, and how many items point at it, newest first. It composes no audience predicate, because the question is what the library holds rather than what one person may see, and the uploader is left-joined so a file outlives the erasure of the person who uploaded it (DATA-002) while naming nobody. There is no filename anywhere in it, and that is a property of the store rather than an omission: a file is keyed by its bytes (CMS-003/T4), so two uploads of one picture under different names are one row and no name was ever kept — which is also why the delete panel asks for the id typed, since the id is the only name a file here has. apps/web/src/content/media.ts:itemsUsing turns the ids deleteMedia refuses with into titles a person can read, outside the transaction that refused; apps/web/src/app/admin/media/[id]/delete/route.ts re-checks the typed name with the same typedNameMatches the panel gates on, because a posted body is whatever the caller sent. apps/web/src/app/admin/media/library.tsx reaches the act through ADMIN-002/T3’s one destructive control, whose media.delete entry has sat in that registry with no caller since it was written; this is its first, so the registry’s own paragraph now says which entries have callers instead of naming two that did not. The honest half of the iteration is what happened after the row closed. All ten CMS-003 rows are shut and the feature still does nothing for an investor: nothing anywhere writes a media_refs row, so the serve path’s join finds no reference, every stored file answers 404, and every use count in the library reads zero. Marking it implemented would have said a stored picture reaches a reader, which is false, so the design is in-progress with inert_until naming what would end it, the PRD row stays building, and the missing writer is filed as CMS-001/T7 where CMS-003 §4 already said it lives. CMS-001’s own §4 never said so back — the one-directional edge §1.12 warns about — and now does, which drops CMS-001 to in-progress and to building on the PRD until T7 closes. Verified against PostgreSQL 17.11: 871 tests over 48 files, none skipped at --maxWorkers=1, 24 of them under src/app/admin/media (13 upload, 11 delete). Driven in Chromium against next build then next start on 3141: the table read Kind, Size, Added (UTC), By, Used by, Delete; a row read "PNG 1 KB 2026-09-12 21:35 Drive Admin nothing Delete…"; the delete control took focus and opened on Enter; the panel said the file leaves the library and anything pointing at it renders nothing, said it cannot be undone, and asked for the id; Confirm stayed off before typing and off for a wrong name, turned on for the exact id, and the row left the table and was still gone after a reload, with no console errors. Five mutants KILLED with no timeout and every file restored byte-identical: the typed-name re-check dropped, the refusal answering with ids rather than titles, the admin gate dropped, the listing ordered oldest first, and the use count always zero. Three of my own errors are worth keeping. The script that closed the row replaced everything between the row and the next one, which swallowed the "## CMS-004" heading and its Design line — the same defect as iter 79, caught this time by check-tasks reporting twelve CMS-004 rows as both missing and unmatched, and restored verbatim from HEAD to a 3-insertion, 2-deletion diff. A backticked code reference was eaten by the shell out of a §6 bullet and repaired. And check-content-access refused the delete suite for naming content_items in a beforeEach reset; the reset is gone rather than exempted, since nothing in the suite counts items. Said plainly: I rebuilt the shared apps/web/.next while a peer lane held servers on 3100 and 3131, which can answer their pages with 404s for chunks that moved. 205 closed.
NEXT: CMS-001/T7 — saving a revision syncs media_refs to the blocks naming a file — is the next anchor, and not only because it is W1’s highest buildable at depth 4: it is the one task that ends CMS-003’s inert state, so until it lands every picture uploaded through the surface built in iters 80 and 81 reaches nobody. It is a sync rather than an insert — saveDraft knows which blocks the revision carries now and which it dropped, so it must end references as well as begin them, and the delete refusal built this iteration is what proves the ending half. After it: CMS-005/T3 and T4 (seed a locale from the source, then the review screen, CMS-DEC-04=B), then CMS-004’s preview and publish, which is four rows and the largest remaining W2 lane. The mail chain is still the W1 blocker — AUTH-003/T3 has no send path until MAIL-001/T8. Seven owner decisions are open; none blocks a task. Practical notes for the next drive: `npm run start` pins --port 3100 whatever PORT says, so use `npx next start --port <free>`; the Playwright MCP profile is usually held by a peer session and importing playwright from a sibling checkout as a file:// URL works; scope any outcome locator to the section under test, because the console’s environment bar is role=status; and Node 24 must lead PATH for anything npm runs here.

## 2026-09-13 · CMS-003/T9 (1 task) · iter 80
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-003/T9 — a file can now be put into the media library, which is the first front-end built under ADMIN-DEC-04 and so the one that sets the console’s bar. apps/web/src/app/admin/media/upload/route.ts is origin-checked, then requireAdmin, then reads the file, caps it on the bytes that arrived rather than on a Content-Length the uploader also writes, sniffs the kind from the bytes and hands the rest to storeMedia. apps/web/src/app/admin/media/upload-control.tsx is the control, and it gives the console’s Media destination somewhere to land — that nav link had pointed at nothing. The method is VALO Ads’: pre-validate before sending, make the drop area a real control rather than decoration around one, hold a busy state, and answer with a sentence about this file. The identity is VALO Tech’s — the console’s own tokens, no second palette. Driven in Chromium against next build then next start on 3131, signed in through the real form: /admin/media answered 200 with no-store; the terms read before a file is chosen; the drop area was reached by Tab alone in eight stops with a solid 2px focus ring at 3px offset; the file input reported tabIndex -1, so it is not a second silent tab stop; Enter and then Space each delivered exactly one click to the picker; an upload answered "chart.png is in the library.", the same file again "chart.png was already in the library. It is one file, not two.", and a GIF named .png "disguised.png is not a PNG, a JPEG or a PDF." in a role=alert whose left border is solid, so no refusal rests on colour; dragging over turned the border solid for the same reason. Two of my own measurements were wrong first and are worth keeping: the instrument printed "Enter opened the file chooser" before it had awaited anything, and getByRole('status') matched the console’s environment bar rather than the outcome, so three readings measured nothing until the locators were scoped and the claim replaced by a counter. A mutant survived too — trusting the filename instead of the bytes — because the fixture it was tested against could not be decoded either, so the refusal proved nothing about which check made it; rebuilt on a GIF the decoder genuinely reads, it bit. Verified against PostgreSQL 17.11: 860 tests over 47 files, none skipped, at --maxWorkers=1, with 13 of them the route’s own. Five mutants KILLED and every file restored byte-identical: the cap dropped, the origin check dropped, the admin gate dropped, a repeat upload always reported as new, and the type taken from the name. CMS-003/T10 is filed open for the same reason T9 was: §3 says a refused deletion shows the admin which items use the file, and deleteMedia returns exactly that list to a caller that does not exist. A file can go in and the room can serve it; nobody can see what is stored or take one out. 203 closed.
NEXT: CMS-003/T10 — the library listing and the delete control that names what uses a file when it refuses — finishes the media feature and is the second surface under ADMIN-DEC-04, so it inherits this iteration’s shape: the same tokens, the same outcome voice, keyboard first. After it, CMS-005/T3 and T4 (seed a locale from the source, then the review screen, CMS-DEC-04=B) and CMS-004’s preview and publish. Practical notes for the next drive: `npm run start` pins --port 3100 whatever PORT says, so run `npx next start --port 3131`; the Playwright MCP profile is often held by a peer session, and a driver script importing playwright from a sibling checkout works (as a file:// URL on Windows); and the console’s environment bar is role=status, so scope any outcome locator to the section under test. Node 24 must lead PATH for any npm install here.

## 2026-09-13 · CMS-003/T8 (1 task) · iter 79
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-003/T8 — an uploaded PDF is stored without its metadata, through pdf-lib (CMS-DEC-05), inside storeMedia beside the raster re-encode and before the bytes are hashed. Three things go and each was measured rather than assumed. The Info dictionary is removed whole instead of blanked field by field, because a dictionary that is gone cannot keep an entry nobody thought to name — the dates leave with the author and the producer. The XMP packet goes from the catalogue and from every page. And each packet’s stream object is deleted from the document rather than unlinked, which is the finding worth keeping: unlinking leaves it orphaned and pdf-lib still writes it out, so the city a photograph was taken in sits in the file for any text extractor while every structural read reports it gone. That is why the tests read bytes as well as structure. Two things the build corrected in itself. The refusal boundary was the parse alone, and pdf-lib accepts a header with prose behind it, hands back a document with no catalogue, and throws a raw TypeError further down — storeMedia would have crashed rather than refused, so the whole scrub is the boundary now. And a comment claimed pdf-lib’s updateMetadata option was load-bearing; a mutant survived, direct measurement showed the bytes identical either way once the Info dictionary is gone, and the option and the claim both left rather than shipping a rationale that is false. Filing CMS-003/T9 is the other half of the iteration: all eight rows were closed and apps/web/src/content/media.ts:storeMedia has no caller outside its tests, because CMS-003 §3 specifies POST /admin/media and no task ever carried it. Serving works; uploading has no surface. Marking the feature live would have said an admin can upload a file, which is false, so the row is filed open with the gap named and the control is held to ADMIN-DEC-04. Verified against PostgreSQL 17.11: 847 tests over 46 files, none skipped, at --maxWorkers=1. Two mutants KILLED with no timeout and the file restored byte-identical — the XMP packet unlinked without deleting its object, and the Info dictionary kept. One assertion is NOT mutation-proved and is said rather than implied: that the stored file does not name the tool that scrubbed it, because no mutant available reproduces a producer stamp once the Info dictionary is gone. 202 closed.
NEXT: CMS-003/T9 is the upload surface this iteration named — a route that sniffs, caps at 10 MB and hands the bytes to storeMedia, and the admin control that posts to it, which is the first front-end built under ADMIN-DEC-04 and so the one that sets the bar for the console: read VALO Ads’ component tree first rather than inventing a form. After it, CMS-005/T3 and T4 (seed a locale from the source, then the review screen, CMS-DEC-04=B) and CMS-004’s preview and publish. Note for whoever picks next: a parallel lane is mid-sweep on design statuses and has introduced `in-progress` plus an `inert_until` field, which moves every in-progress feature into an "Outside the waves" section of the roadmap — thirteen features with buildable work sit there, so the wave descent alone will not reach them until that settles. The mail chain is still the W1 blocker: AUTH-003/T3 has no send path until MAIL-001/T8, and accounts carries no locale. Node 24 must lead PATH for any npm install here.

## 2026-09-13 · CFG-001/T7 + CMS-003/T2 (2 tasks) · iter 78
STATUS: green · TIER: M · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CFG-001/T7 — the runtime registry now holds only the three values an admin actually changes, and the session lifetime and sign-in rate are the environment’s (CFG-DEC-01). Removing them took the `int` type with them: it was the type only those two keys had, so its parse branch and its bounds had no caller and a registry still offering them would offer a shape nothing can hold. The registry’s membership is now pinned as an exact set, which is what makes the removal provable rather than merely done. CMS-003/T2 — a raster is re-encoded through jimp (CMS-DEC-03) inside storeMedia, before the bytes are hashed, so the stored file, the sha256 it is keyed by and what a serve returns are one thing and EXIF is in none of them. It sits inside the store rather than in front of it because there is no upload route yet to put it in front of, and a guarantee waiting for a caller to arrive and remember is not one. The best proof is a dedup: the same photo with and without its EXIF lands on one row, so anything the strip left behind would show up as two. Bytes that sniff as a raster and will not decode are refused with nothing stored, and the refusal names the type and never the file’s content. Two things this surfaced. jimp decodes no WebP, so the one accepted format the encoder cannot rewrite was the one whose metadata nothing strips — filed as CMS-DEC-06 with refusal as the fail-closed safe default, which is the move CMS-DEC-03 already made for SVG. And npm refused every install until the box’s Node 24 was put on PATH: the project declares engines node>=24 with engine-strict, PATH resolves node to a v20 first, and the error names the project rather than the package. Verified against PostgreSQL 17.11: 844 tests over 46 files, none skipped, at --maxWorkers=1. Four mutants KILLED with no timeout among them and every file restored byte-identical — the re-encode skipped, the decoder’s own error let out in place of the named one, the refusal path opened, and a security key returned to the registry. One mutant SURVIVED and is reported rather than hidden: adding WebP back to the accepted list alone changes nothing, because the sniff no longer has a branch for it, so that list can grow a dead entry silently. Two owner directives arrived mid-iteration and are filed: INV-DEC-01’s sibling ADMIN-DEC-04 holds the admin console to VALO Ads’ level of craft in VALO Tech’s own identity, pointed at from ADMIN-002 §5. CMS-003/T8 was dropped from the batch rather than half-built — the PDF scrub needs its own library chosen by measurement, and T2’s ripple plus a new decision was enough. 201 closed.
NEXT: CMS-003/T8 is the next anchor and now the only thing between the media library and complete: CMS-DEC-05=A keeps PDFs and strips their metadata through a library chosen by measurement, so it wants a pure-JavaScript candidate (pdf-lib reaches the Info dictionary and the XMP packet) proven against a PDF seeded with an author, a producer and a local path. It lands in storeMedia beside the raster re-encode, for the same reason that one is there. Then CMS-005/T3 and T4 (seed a locale from the source, then the review screen, CMS-DEC-04=B) and CMS-004’s preview and publish surfaces. W1’s remaining two both wait on the mail chain — AUTH-003/T3 has no send path until MAIL-001/T8’s nodemailer adapter, and accounts carries no locale, so "in the invitee’s locale" has no source; whoever takes it should read that gap first. Remember Node 24 must lead PATH for any npm install here.

## 2026-09-13 · AUTH-002/T5 (1 task) · iter 77
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: AUTH-002/T5 — the session cookie is signed, and the signature is checked before any row is looked up (AUTH-DEC-02=A). apps/web/src/auth/session.ts:signToken makes the cookie the token and an HMAC-SHA256 of it under SESSION_SECRET; tokenOfCookie reads it back with timingSafeEqual and refuses a value with no separator, which is what stops a bare token being a session. The check sits inside apps/web/src/auth/gate.ts:presentedToken — the one place a request’s cookie becomes a token — so every surface inherits it and a failed check is no token at all rather than a second kind of answer a caller could forget. The row still holds the hash of the bare token, so no schema moved, and SESSION_SECRET finally has a consumer: rotating it now ends every session in the world, which is what credentials/README.md and env.example had been describing as though it were already true. Built in the main session rather than through critical-impl: the change is two source files, iter 76’s dispatch cost a whole session and returned no verified proof, and the independent pass this tier asks for is better spent on review than on authorship. The test suite had to learn the difference between a cookie and a token — 27 failures on the first run, every one a place that held the cookie value where it meant the token — and the fix went into each suite’s helper rather than its call sites, so the files now say what they mean. Verified against PostgreSQL 17.11: 837 tests over 46 files, all passing, none skipped. Mutation-proved, each KILLED by named assertions with no timeout among them and every file restored byte-identical: the verification removed, the signature compared by length alone, the HMAC keyed on a constant rather than the secret, and issue() handing back the bare token. Also this iteration: iter 76’s blocked review finding is fixed now that the lane freed the file — DATA-002’s erasure manifest, its retention row and its DATA-R02 bullet no longer tell a reader the audit holds field names and no personal data, which was the justification two LEGAL designs cite for keeping it seven years past an erasure. INV-DEC-01 files the owner’s standard for investor-facing surfaces — shown rather than said, figures clear and grounded, never a wall of text — linked from INV-001, INV-003, RPT-003 and DECK-003, the four surfaces it binds. AUTH-002 advanced to implemented and live, which the peer lane’s new design-status gate required the moment T5 closed. An independent deep-review pass then returned yellow with nine findings, none of them in the code — it confirmed the verifier against every input it could think of, confirmed by grep that presentedToken is the only reader of the cookie header in the tree, and called the proxy narrowing the strongest part of the change. What it found was the operator documents. The sign-out runbook’s cache diagnostics asked with an unsigned cookie, so they now return nothing and instruct the operator to blame the proxy for a control that is intact; both auth runbooks promised a rollback that signs nobody out, which a cookie-format change makes false in both directions; and the AUTH-002 runbook named neither the rotation lever this commit built nor the split-secret failure it makes possible, which no query in it would show. All three are rewritten. ADMIN-001’s erasure-disclosure sentence held the last copy of the audit’s "no personal data" claim — the sentence that states what survives a delete — and now says what the allow-list actually guarantees. A refusal case in gate.test.ts signed two different tokens, so it only retested the row above it; the timeout constant’s comment said fifteen statements where the arrangement has about forty; and two content suites called a cookie value a token. Recorded rather than built, in AUTH-002 §6: a nominal token type would make the cookie-for-token mistake a compile error, which is the exact mistake that cost twenty-seven call sites in this iteration’s own test sweep. 195 closed.
NEXT: W1’s last buildable rows are AUTH-003/T3 (the invitation mail, which wants MAIL-001/T8’s nodemailer adapter first — MAIL-DEC-03=A — and the SMTP credential after it) and SEC-001/T4 [~], whose third surface is the public reset page behind that same mail. W2 is where the clean work is: CMS-003/T2 (raster re-encode through jimp, CMS-DEC-03=A, which adds the dependency the owner already approved), CMS-003/T8 (PDF metadata, CMS-DEC-05=A) and CMS-005/T3+T4 (seed a locale from the source, then the review screen, CMS-DEC-04=B). CFG-001/T7 still is not the two-line deletion its row reads as: removing session.max_age_days and signin.rate_per_hour leaves the registry with no int-typed key, so its parse and min/max validation would have no caller. Honest flag for whoever measures next: this box is shared, and at the configured four workers a peer lane’s load produced 20 failures that were every one a timeout and not one an assertion; the green reading above was taken at --maxWorkers=1, which is the honest instrument while another lane is running.

## 2026-09-13 · SEC-002/T4 + ADMIN-001/T9 (2 tasks) · iter 76
STATUS: green · TIER: C · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: The audit trail stopped being a record of acts alone and became a record of what they moved. apps/web/src/audit/record.ts:RECORDABLE_FIELDS is the per-action allow-list SEC-DEC-01 settled — exhaustive over the vocabulary by type, so folding in an action without saying what it may record does not compile, and no list names name or email; recordAudit checks both sides before the insert and raises UnrecordableFieldError on anything else, a refusal rather than a silent drop, whose message names the action and the field and never the value. Six call sites now carry values: the role a change replaced, a setting’s key and both values, a send’s subject and recipient count, the revision a publication replaced (CMS-R07, which no row could answer before), and both audiences of a narrowing. ADMIN-001/T9 folds account.invitation_resend and account.password_reset_request into the action CHECK (the ADMIN-001/T8 precedent, DATA-R07) and audits each inside its own act’s transaction; the reset row is keyed on who is asking and never on what the database holds, so the branch-free shape SEC-R03 rests on is untouched and the public form still records nothing. Seven ledger sentences that said before/after were null "until SEC-002/T4" now say what is true, and two that called the vocabulary fifteen values now say nineteen — both counts were already stale before this change. Verified against PostgreSQL 17.11: 824 tests over 46 files, none skipped; typecheck, make check and make migrate-roundtrip all green. Seven mutants each KILLED with every file restored byte-identical, including the one that matters most — the reset audit made conditional on an active account, which would put the timing oracle back. The C-axis found a residue SEC-DEC-01 does not cover — a config value, a mail subject and a portfolio headline are free text an admin types, so a name can still reach a seven-year row through a field the list permits — filed as SEC-DEC-03, non-blocking, safe default as shipped. An independent deep-review pass returned yellow over nine findings, none of them a security hole or a shipped data break, and its document defects are corrected in the same iteration: send.ts’s header still called SEC-DEC-01 unsettled while the function below it recorded values; grants.ts promised a field the settled allow-list rules out, and the design’s reason for that empty list named a subject the code does not write; the reset comment justified leaving the public path unaudited on a privacy ground the admin path contradicts, where the real difference is that SEC-R04 holds a privileged act; and the jsonb bound — a U+0000 or an unpaired surrogate makes PostgreSQL refuse the row — is named at the insert site for the first caller that will parse JSON from a request body. Two questions it surfaced are the owner’s and are filed: SEC-DEC-04 (a grant’s row names the account and never the item, so a revocation destroys its own evidence) and DATA-DEC-01 (eight shipped rows widen a shipped migration in place, which DATA-R07’s own words make a re-stamp rather than a fold — safe only while every database can be recreated, which OPS-001 ends). Honest notes: the implementation was built by critical-impl, which a session limit cut off before it ran the suite or reported a single proof, so every verification here is mine rather than inherited; and one finding is not fixed — DATA-002’s erasure manifest still tells a reader the audit holds field names and no personal data, which is the stated justification two LEGAL designs cite for retaining it seven years past an erasure, and a parallel lane holds that file uncommitted, so it is the next iteration’s first move rather than this one’s. 194 closed.
NEXT: First, the review finding this iteration could not reach — rewrite docs/designs/data/data-002-erasure-and-retention.md’s manifest row for audit, its retention "why", and its DATA-R02 bullet to say the trail holds the values each action’s allow-list names, linking SEC-DEC-03; the file was another lane’s uncommitted work and check-erasure-manifest.py cannot see inside a jsonb column, so nothing mechanical will catch it. Then W1’s remaining clean work is AUTH-002/T5 (sign the session cookie with SESSION_SECRET and verify the HMAC before the row lookup, AUTH-DEC-02=A) — Critical, and its own iteration, because a cookie-format change is separately revertible from this one; AUTH-003/T3 (the invitation mail) waits on MAIL-001/T8’s nodemailer adapter and the SMTP credential, and SEC-001/T4 [~] waits on the public reset page behind it. W2 then offers CMS-003/T2 (raster re-encode through jimp, CMS-DEC-03=A), CMS-003/T8 (PDF metadata, CMS-DEC-05=A) and CMS-005/T3+T4 (seed a locale, then the review screen, CMS-DEC-04=B). CFG-001/T7 is not the two-line deletion its row reads as: removing session.max_age_days and signin.rate_per_hour leaves the registry with no int-typed key at all, so the parse and the min/max validation would have no caller (§1.10) — read that consequence before anchoring on it. Four open decisions, none blocking.

## 2026-09-12 · CMS-003/T3 + four decisions (1 task) · iter 75
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: An owner-directed wrap-up. The loop had reached a decision-gated frontier (W1 mail-blocked, W2's clean work drained), so the four blocking decisions were surfaced to the owner with §1.11 options and recommendations, and the owner accepted all four: SEC-DEC-01=B (the audit records changed values under a per-action allow-list that can never name name or email), CMS-DEC-03=A (raster re-encode through jimp, SVG refused), CMS-DEC-04=B (no translation service — drafting seeds the target locale from the source and the admin translates in the review screen), MAIL-DEC-02=A (a retry_of column on mail_log). The register records each RESOLVED and moved to the Resolved section (commit 948acbe), their tasks are unblocked, and blocked tasks fall from ten to six. CMS-003/T3 (refuse SVG) is then closed on CMS-DEC-03=A: apps/web/src/content/media.ts drops image/svg+xml from ACCEPTED_MIME and removes the looksLikeSvg sniff, so an <svg> sniffs to null and the upload refuses it like any unaccepted type (no sanitiser, no dependency); apps/web/src/content/media.test.ts flips to prove SVG is refused in every form (mutation-proved). make check green (192 closed, 6 blocked, deferrals all live) and check-app green (337 web tests; 11 media tests against PostgreSQL 17.11). Earlier this window: iters 73–74 closed CMS-002/T4 and T5 (completing CMS-002) plus the sign-in/acceptance rate limit, and a background review's HIGH stored-XSS in the paste parser was fixed at the write boundary (702c5f2).
NEXT: The four resolutions unblock the loop's next work, each its own focused iteration: SEC-002/T4 (record changed values under the SEC-DEC-01 allow-list — a cross-cutting change to recordAudit and its config, role-change and mail.send callers), CMS-005/T3+T4 (seed a locale from the source, then the admin review screen, per CMS-DEC-04=B), and CMS-003/T2 (raster re-encode through jimp, per CMS-DEC-03=A, which adds the jimp dependency). MAIL-001/T6 stays blocked on the SMTP transport (MAIL-001/T8, external). The six remaining open decisions all carry Blocks: — none —.

## 2026-09-12 · CMS-002/T5 (1 task) · iter 74
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-002/T5 (paste imports plain text plus recognised structure and nothing else) closed, completing CMS-002 (7 of 7). apps/web/src/content/paste.ts turns a paste into blocks: blocksFromText splits plain text into a paragraph per blank-line run; blocksFromHtml reads a pasted HTML fragment with the browser's DOMParser and keeps only headings, lists and links, so a foreign stylesheet, a script, a table and a coloured span all reduce to text (CMS-R04); pasteToBlocks reads the HTML over the text and isStructured tells structure worth importing from ordinary inline text. The editor wires it in apps/web/src/app/admin/content/[id]/edit/block-fields.tsx (the paragraph's onPaste) and editor.tsx (pasteBlocks replaces an empty paragraph or inserts after a full one), the model still the block array. apps/web/src/content/paste.test.ts (12 passed) pins the pure paths; the DOMParser path is browser-driven on the built server on PostgreSQL 17.11: a heading-paragraph-list-script-styled-paragraph fragment imported exactly four blocks in order (Heading, Paragraph, List, Paragraph) with the link a mark over 'full report', the script and the inline style gone; a two-run plain-text paste imported two paragraphs. make check green (191 closed, 104 outstanding) and check-app green (331 web tests). ⭐ A paste of a foreign inline style logs one style-src CSP block at parse (SEC-001); a no-style paste is console-clean, which confirms the note is the CSP working on the parsed fragment and not the editor — recorded as a CMS-002 section 6 trade-off.
NEXT: CMS-002 is complete. The next W2 surface is CMS-004 preview: T1 renders a draft through the reader's own block components and evaluates the audience rule as a chosen role. Its buildability turns on whether a shared block-renderer already exists or must be built, since the reader views (RPT-003, DECK-003, INV-001) are W4 and unbuilt; iter 75 reads the CMS-004 design to decide — build T1 and T2 if a block-renderer is the whole of it (foundational, the reader views reuse it), else descend to W3 (POST-001, the update composer) because W2's rest is decision- or room-blocked (CMS-003 on CMS-DEC-03, CMS-005/T4 on CMS-DEC-04, CMS-007 on INV-001).

## 2026-09-12 · SEC-001/T4 + CMS-002/T4 (2 tasks) · iter 73
STATUS: green · TIER: S · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-002/T4 (marks by selection) closed and SEC-001/T4 advanced to in-progress. apps/web/src/content/marks.ts is a pure module — applyMark unions a same-type span and clips a link to the newest on overlap, removeMark drops one, and remapMarks maintains a mark's offsets across a text edit by the edit's common prefix and suffix: a mark grows when text is typed inside it, shifts when typed before it, holds at its edge, shrinks when its own text is deleted, and drops when that text is gone. apps/web/src/app/admin/content/[id]/edit/block-fields.tsx gives the paragraph a mark toolbar over the textarea's selection and a marks list that names each span and removes it (a textarea cannot render emphasis inline, A11Y-R02), the editor's model still the block array and never the DOM. apps/web/src/app/api/auth/set-password/route.ts now rate-limits invitation and reset acceptance per token, keyed by the token's SHA-256, counted after the weak-password refusal and before the Argon2 hash, answering 429 with a Retry-After identically whatever the token (SEC-001 §3, SEC-R03). Verified on PostgreSQL 17.11: apps/web/src/content/marks.test.ts (26 passed, every boundary of applyMark and remapMarks) and apps/web/src/app/api/auth/set-password/route.test.ts (3 passed). Browser-driven on the built server: selecting the word "quarter" and pressing Bold marked it; typing "Last " before it kept the mark on "quarter" (offset 4..11 moved to 9..16); Save then reload persisted it; a signed-out request went to sign-in and an investor got 404. Console clean. make check green (190 closed, 105 outstanding) and check-app green (319 web tests). ⭐ The first browser drive left the Bold button disabled: a scripted setSelectionRange does not fire React's onSelect, so the driver now selects by real key events, as a user would — the verifier was the defect, not the product.
NEXT: SEC-001/T4's reset-request limit waits on the public reset-request page, which waits on mail delivery — AUTH-003/T3 and the SMTP adapter MAIL-001/T8 with its credential (external residue). So W1's remaining open work is all credential- or decision-blocked: that, SEC-002/T2 (the REVOKE at deploy) and SEC-002/T4 (on SEC-DEC-01). iter 74 takes CMS-002/T5 (paste imports plain text plus recognised structure, an HTML-to-blocks parser) on the browser-verified editor, then the W2 preview pair CMS-004/T1 and T2.

## 2026-09-12 · CMS-005/T6 (1 task) · iter 72
STATUS: green · TIER: Standard · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-005/T6 (the locale grid) closed, the first W2 descent after W1's clean frontier drained. A server-rendered admin page at apps/web/src/app/admin/content/[id]/locales/page.tsx shows twenty languages down and an item's revisions across, each cell one of four states — authored, not started, machine draft or reviewed — carried by a word as well as a colour (A11Y-R03). apps/web/src/content/locales.ts:localeGrid reads the revisions newest first and their locale rows in two queries rather than a join, so a revision with no rows still appears as its own empty column — the property CMS-005 §3 makes visible: a new revision starts with one authored language and nineteen not-started cells, nothing carried forward. The console is English (ADMIN-002), so the grid's chrome is not translated, the language names are the platform's own English display names (Simplified Chinese, Traditional Chinese, Vietnamese and the rest) and the dates are UTC. Verified against PostgreSQL 17.11 by apps/web/src/content/locales.test.ts (10 passed, the four new ones pinning the grid's states and its newest-first ordering); browser-driven on the built server (vendored Playwright, a seeded admin session, an item with a published revision carrying a reviewed French and a machine German plus a newer empty draft): the grid returned 200 with twenty rows, two revision columns, and exactly two authored, one reviewed, one machine-draft and thirty-six not-started cells; a signed-out request redirected to sign-in and an investor was answered 404. Console clean. make check and check-app green (189 closed, 106 outstanding). ⭐ A verify assertion expected the name Chinese (Traditional); Intl.DisplayNames renders it Traditional Chinese, so the assertion was the defect, not the product.
NEXT: CMS-005/T4 (the review screen) stays entangled with the decision-blocked T3 — no locale rows exist to review until the drafting carrier CMS-DEC-04 is chosen — so it waits. W2's next clean surfaces are the CMS-002 editor pair: T4 (marks by selection, a selection-to-offsets model with the block array as the source of truth and offsets maintained under edits) and T5 (paste imports plain text plus recognised structure), both on the existing browser-verified editor. CMS-007 T5 and T6 (the search empty state and the labelled field) wait on the room INV-001 giving them a surface. iter 73 takes the CMS-002 editor pair, giving the offset maintenance the focused iteration it needs.

## 2026-09-12 · AUTH-004/T2 (1 task) · iter 71
STATUS: green · TIER: Standard · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: AUTH-004/T2 (the session list) closed — a server-rendered page at apps/web/src/app/account/sessions/page.tsx listing the reader's live sessions, the current one marked, with the control that ends them all. apps/web/src/auth/page-guard.ts:requireInvestorPage gates it (an investor or an admin in, a signed-out reader to the sign-in form); apps/web/src/auth/session.ts:currentSessionId marks the row the request's cookie names without the surface holding the token, beside the existing liveSessionsForAccount; each session's began, last-active and expires times format in the reader's locale and in UTC (apps/web/src/i18n/request.ts fixes the zone so a server render is deterministic). The end-everywhere control is a plain form to the route AUTH-004/T3 mounts, so the page carries no client script. An eight-string sessions namespace in twenty locales (en authored, the nineteen others anchored on the gateway's reviewed strings, native prose read owed under I18N-001). The sign-out runbook's stale line calling T2 blocked on I18N-DEC-02 is refreshed now that T2 ships. Verified: apps/web/src/auth/page-guard.test.ts pins the requireInvestorPage translation, and browser-driven on the built server (vendored Playwright, live PostgreSQL 17.11, an account seeded with two sessions): a signed-out request redirected to the sign-in form, the page listed both sessions with the current one alone marked as this device, the began, last-active and expires labels and the end-everywhere control present with an accessible name, the English, Vietnamese and Arabic (right to left) renders, and the control posted, redirected to the root, emptied the account's sessions and cleared the cookie. Console clean. make check and check-app green (188 closed, 107 outstanding).
NEXT: AUTH-004 is complete. W1's remaining open work is not a clean close — AUTH-003/T3 (the invitation mail) is gated on MAIL-001's Mailer port and has no invitee-locale source, and SEC-001/T4 (rate limits on sign-in, reset and invitation) has its sign-in half done but no reset-request page to rate-limit yet. So W1's AI-buildable frontier is drained, and iter 72 descends to W2: the CMS-002 editor pair (T4 marks by selection, T5 paste) and CMS-004/T1 (preview) are browser-verifiable surfaces on the framework, and CMS-005 T4 and T6 are the locale-variant screens. The reading views (RPT-003, DECK-003) and the room shell (INV-001) in the later waves sit on the i18n framework too.

## 2026-09-12 · AUTH-003/T4 + AUTH-004/T4 (2 tasks) · iter 70
STATUS: green · TIER: Standard · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: AUTH-003/T4 (the set-password form) and AUTH-004/T4 (the no-store back button) closed, both on the i18n framework iter 69 built. AUTH-003/T4: the invitee-and-reset set-password surface — the form at apps/web/src/app/invite/[token]/page.tsx and apps/web/src/app/reset/[token]/page.tsx (one shared view and client form under apps/web/src/app/set-password/), posting to apps/web/src/app/api/auth/set-password/route.ts, which checks apps/web/src/auth/password-policy.ts:checkPassword (at least twelve characters, at most two hundred, and the common list in apps/web/src/auth/common-passwords.ts) before apps/web/src/auth/invitation.ts:setPasswordWithToken consumes the token and sets the password in one transaction (invited becomes active, an active reset stays active, a suspended account refused); apps/web/src/auth/invitation.ts:tokenIsLive picks the form or the expired panel without consuming. Twenty-locale setPassword strings added (en authored, the nineteen others anchored on the gateway's reviewed strings, the native prose read owed under I18N-001). AUTH-004/T4: closed by browser-verifying the no-store back button on the admin console — an existing gated surface, so it did not wait for INV-002 — and docs/runbooks/auth-004-sign-out.md now records the Chromium drive with a WebKit spot-check noted. Verified against PostgreSQL 17.11 by apps/web/src/auth/invitation.test.ts (47) and apps/web/src/auth/password-policy.test.ts (6); browser-driven on the built server (vendored Playwright): English, Vietnamese and Arabic (RTL) render, accessible names on every field, keyboard order, a too-short and a too-common password rejected, a mismatch, a strong password activating the account and signing in, the consumed token's expired panel, and the admin back button landing on sign-in. make check and check-app green (187 closed, 108 outstanding). ⭐ The translation subagent died on a session rate limit mid-build (2026-09-11); the nineteen locales were authored in the main thread instead, and the iteration closed after the quota reset (2026-09-12) — the build and browser verification had completed before the limit hit, so nothing was re-run.
NEXT: AUTH-003 is complete but for T3 (the invitation mail, waiting on MAIL-001's Mailer port and with no invitee-locale source, so its localization has no caller yet). AUTH-004/T2 (the session list) is the next clean front-end surface on the framework — apps/web/src/auth/session.ts:liveSessionsForAccount already reads the rows; the sign-out runbook still calls T2 blocked on I18N-DEC-02, which has resolved, so it is buildable and that stale line is worth fixing when T2 lands. W1 then holds SEC-001/T4 (reset and invitation rate limits) and SEC-002 (external REVOKE plus pending SEC-DEC-01). iter 71 takes AUTH-004/T2, building and browser-verifying as here.

## 2026-09-11 · AUTH-001/T4 (1 task) · iter 69
STATUS: green · TIER: Standard · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: AUTH-001/T4 — the sign-in form in twenty locales, keyboard-reachable, an accessible name on every field — closed by building the application's i18n framework on next-intl (the owner's I18N-DEC-02, without URL routing) and the sign-in surface on top of it, then browser-verifying the built artifact. The framework: apps/web/src/i18n/locales.ts carries the twenty locales, the Arabic-and-Urdu right-to-left pair, the BCP-47 tags and an Accept-Language negotiation mirroring the gateway's match(), and apps/web/src/i18n/locales.test.ts pins that negotiation (13 passed); apps/web/src/i18n/request.ts is next-intl's per-request config, resolving the locale from the NEXT_LOCALE cookie or Accept-Language with no middleware beside the security proxy; apps/web/next.config.mjs wires createNextIntlPlugin; apps/web/src/app/layout.tsx wraps the tree in NextIntlClientProvider and sets the document lang and dir from the locale. The surface: apps/web/src/app/sign-in/page.tsx renders the heading server-side, apps/web/src/app/sign-in/sign-in-form.tsx:SignInForm is the client form (labels not placeholders, posting to the sign-in route; 204 navigates to the root, 401 shows the one failure message, 429 the wait message), styled by a CSS module because the CSP forbids inline style. Twenty message files in apps/web/src/messages: en authored here, the nineteen others anchored on the gateway's reviewed strings (password reuses inv.pw exactly), the per-locale native prose read still owed under I18N-001. next-intl@4.14.3 was added (the install needs node 24 under engine-strict; the app still builds and runs on node 20). Browser-verified on the built server (port 3131, vendored Playwright, live PostgreSQL 17.11): 200 with an accessible name on every field, the email autofocused and Tab walking to the password then the submit, a wrong password showing the single failure message and staying on the page, the correct password setting the valotech cookie and redirecting to the root, a rate-limited attempt showing the wait message, the Vietnamese render by Accept-Language and the Arabic render by cookie laid out right-to-left. Console clean but for the expected 401 and 429 statuses of the verification's own posts. make check and check-app both green (185 closed, 110 outstanding). ⭐ The first verify run saw two role=alert nodes because Next injects its own route announcer; the selector was the defect, not the product, and scoping it to the form's paragraph passed.
NEXT: The i18n framework is now the foundation every localized surface sits on, so the front-end can advance past the decision gate. The next auth surfaces on it are AUTH-004/T2 (the session list) and AUTH-003/T4 (the set-password form); the reading views (RPT-003, DECK-003), the investor room shell INV-001 and the gateway-served-by-app SITE-005 all consume it too. CMS-002/T4 (marks by selection) and T5 (paste) remain editor-interaction features, browser-verifiable on the now-free lane. iter 70 takes the next front-end surface on the framework — AUTH-004/T2 or the CMS editor pair — building and browser-verifying as here. The backend frontier stays gated on the remaining owner decisions (CMS-DEC-03, CMS-DEC-04, MAIL-DEC-02), external credentials and OPS-001 infra.

## 2026-09-11 · CMS-002/T1,T2,T6 (3 tasks) · iter 68
STATUS: green · TIER: Standard · OUTCOME: CLOSED
REASON: —
WHAT CHANGED: CMS-002/T1, T2 and T6 — the keyboard block list, the seven block types' fields, and explicit save with the unsaved line and local-copy restore — closed by browser verification, now that the owner freed port 3100. The mechanisms were already built (apps/web/src/app/admin/content/[id]/edit/editor.tsx and its block-fields); what was owed was a live browser drive, blocked until this session by the peer's server on 3100. Built the app (next build), served it on 3123, seeded an admin session and a content item carrying all seven block types, and drove vendored Chromium (VALOCompliance's Playwright). Measured: the editor opened under the admin gate (200); seven rows rendered, labelled Heading through Divider, 1 through 7 of 7, the add-a-block menu offering seven types and the image's alt field present (T2); focusing the first row and pressing ArrowDown moved focus to the second, and Enter inserted a block, seven rows to eight (T1); the standing line read saved before any edit, unsaved after editing the heading, saved again after Save draft as the POST succeeded, and an unsaved edit after a reload was offered back by the local-copy banner (T6). Console clean. No product code changed. make check green (184 closed, 111 outstanding). ⭐ The statusInitial that first read not-saved was my driver dirtying the editor with an Enter before the check — read before the edit it is saved; the product was right, the script was wrong.
NEXT: CMS-002 has T1, T2, T3, T6 and T7 closed; T4 (marks by selection) and T5 (paste) remain — editor-interaction features also browser-verifiable on the free lane. The bigger front-end foundation is AUTH-001/T4 (the sign-in form) on the next-intl framework the owner chose (I18N-DEC-02): next-intl is not yet in node_modules and the integration has no design, so it needs installing plus a small design, then it unblocks the localized surfaces — the auth forms, the reading views (RPT-003, DECK-003), the room shell INV-001, the gateway served by the app. iter 69 builds the next-intl framework plus the sign-in form, or closes CMS-002/T4 and T5 on the free browser lane.
