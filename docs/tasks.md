# VALO Tech — Tasks

> **Not for `main`.** This document lives on `development` only (`.claude/CLAUDE.md` §1.1).
> **Purpose:** track every implementation task that realizes [docs/PRD.md](PRD.md). A task closes only with concrete `Evidence:`.
> **Companions:** [docs/PRD.md](PRD.md) · [docs/decisions-log.md](decisions-log.md) · [docs/designs/](designs/) · [.claude/CLAUDE.md](../.claude/CLAUDE.md) §3.6 and §4.

---

## How to use this document

Tasks are grouped by feature code and carry a stable id `<CODE>/T<N>`. A task lives in exactly one section, owned by one feature; work spanning features is split. `Evidence:` is a commit sha, a test name, or `path:Symbol` — never a bare line number, which moves.

| Marker | State | Required |
|---|---|---|
| `- [ ]` | open | — |
| `- [~]` | in progress | `Note:` — what is done and what is left |
| `- [!]` | blocked | `Blocked by:` — a task code or a register anchor |
| `- [x]` | closed | `Evidence:` |

**Open `REVIEW` rows are drained before other roadmap work.**

---

## SITE-001 · The gateway page
Design: [docs/designs/site/site-001-the-gateway-page.md](designs/site/site-001-the-gateway-page.md) · PRD: `SITE-001`, `SITE-003`, `SITE-004`

- [x] SITE-001/T1 — One page, nine chapters, a fixed scene layer beneath them
  Evidence: index.html · assets/site.css §10b
- [x] SITE-001/T2 — Chapter sequence follows the reference: problem, answer, your people, why, workforce, ValoStack, yours-not-ours, contact
  Evidence: commit 7543b48
- [x] SITE-001/T3 — The close carries the contact call to action and states why prices are not published
  Evidence: index.html:#engage · docs/decisions-log.md#SITE-DEC-03
- [x] SITE-001/T4 — Navigation follows the page and folds into a menu below 1080px
  Evidence: assets/site.css:.nav-links · commit 7543b48
- [x] SITE-001/T5 — The reading column yields to the frame so labels survive below 1440px
  Evidence: commit 3af6ea0
- [x] SITE-001/T6 — A frame past 2000px shows a larger page rather than a further one, in every element the frame carries
  Evidence: assets/site.css:@media (min-width: 2000px) · docs/decisions-log.md#SITE-DEC-02 — measured at 3840 x 2160: `.sun` 739px, `.hero-aside .marker` 422px, `#people .chapter-head` 686px, all previously flat at their 1440-tuned widths while the display face grew 32%
- [x] SITE-001/T8 — Gateway is defined as a theme: two panel families, the type roles, and every deviation from the reference named with its measurement
  Evidence: brand/GUIDELINES.md — ground, panels, accent, text, type, scale, space, motion and contrast, with the three opacity deviations from the reference stated in one table
- [x] SITE-001/T7 — The brand kit cannot publish a value the stylesheet has stopped using, and cannot omit one it declares
  Evidence: scripts/check-brand-tokens.py — 39 tokens verified against `assets/site.css`, wired into `make check`; proved able to fail on a drifted value, on a token the kit invents, and on a name one kit file publishes and the other does not

## SITE-002 · Public and investor chapter split
PRD: `SITE-002` · Decision: [decisions-log.md#SITE-DEC-01](decisions-log.md#SITE-DEC-01)

- [x] SITE-002/T1 — How-we-deliver and the portfolio are hidden from a visitor and shown to a signed-in reader
  Evidence: assets/site.css:.chapter--gated · index.html
- [x] SITE-002/T2 — The mechanism detail under each of seven trust claims opens behind the sign-in
  Evidence: assets/site.css:.cap-detail
- [x] SITE-002/T3 — The navigation names the gated pair only to a signed-in reader
  Evidence: assets/site.css:.nav-gated
- [!] SITE-002/T4 — The split is enforced by the server rather than by CSS
  Blocked by: INV-002/T1 — the enforcement is that task's, and this row closes when the served page stops shipping the gated markup to a visitor.

## SITE-003 · Chapter sequence
Design: [docs/designs/site/site-003-chapter-sequence.md](designs/site/site-003-chapter-sequence.md) · PRD: `SITE-003`

- [x] SITE-003/T1 — Nine sections in the reference order, each declaring the side its argument holds
  Evidence: index.html — problem, approach, deliver, people, trust, workforce, valostack, outcome, ecosystem
- [x] SITE-003/T2 — Two chapters gated, and the journey spans them when they are folded
  Evidence: assets/scene/boot.js:CHAPTER_SPINE — a station whose chapter has no layout box is dropped from the route, so the world does not stop at a section the reader cannot see

## SITE-004 · The contact close
Design: [docs/designs/site/site-004-contact-close.md](designs/site/site-004-contact-close.md) · PRD: `SITE-004`

- [x] SITE-004/T1 — The close carries the offer, the call to action, and the reason prices are not published
  Evidence: index.html:engage · docs/decisions-log.md#SITE-DEC-03
- [x] SITE-004/T2 — Company, contact and ecosystem links, with the legal line beneath
  Evidence: index.html:footer-legal

## SCENE-001 · The world and its journey

Design: [docs/designs/scene/scene-001-world-and-journey.md](designs/scene/scene-001-world-and-journey.md) · PRD: `SCENE-001`

- [x] SCENE-001/T1 — A lunar sphere becomes Earth across one scroll scrub, with a lit frontier
  Evidence: assets/scene/planet.js:TRANSITION_GLSL
- [x] SCENE-001/T2 — The journey is measured in chapters, not page fractions
  Evidence: assets/scene/boot.js:CHAPTER_SPINE
- [x] SCENE-001/T3 — The journey is read ahead of the reader so each chapter's disc is standing when its heading arrives
  Evidence: assets/scene/boot.js:LEAD_EASE
- [x] SCENE-001/T4 — A jump is placed rather than eased
  Evidence: commit e180555
- [x] SCENE-001/T5 — The world's size follows the frame, with no flat ceiling and a floor that cannot shrink a tuned size
  Evidence: assets/site.css:--planet · docs/decisions-log.md#SITE-DEC-02 — 994px at 3840 x 2160 and 432px at 1440 x 900, 46% and 48% of the frame height; satellites re-measured over the larger disc at three points of the orbit stage, minimum pairwise gap 8px, none off-frame
- [x] SCENE-001/T6 — The close takes the open side of the footer
  Evidence: commit 535d981

## SCENE-002 · Satellites and their rings
PRD: `SCENE-002` · Decision: [decisions-log.md#SCENE-DEC-01](decisions-log.md#SCENE-DEC-01)

- [x] SCENE-002/T1 — Three bodies on one shared period, arriving in the order the story does
  Evidence: assets/scene/boot.js:SATELLITES
- [x] SCENE-002/T2 — One drawn ring per body, revealed with the body that rides it
  Evidence: commit e180555

## SCENE-003 · The sky
PRD: `SCENE-003`

- [x] SCENE-003/T1 — A parallaxed field in three depth tiers, against the orbit's bearing
  Evidence: assets/scene/stars.js:TIERS
- [x] SCENE-003/T2 — Meteors with two endings: a crater on the rock, absorption on the living world
  Evidence: assets/scene/stars.js:ABSORB_AT
- [x] SCENE-003/T3 — Colour temperature, a discrete twinkle, and a rare supernova
  Evidence: commit dc9e614

## SCENE-004 · Annotation chips
Design: [docs/designs/scene/scene-004-annotation-chips.md](designs/scene/scene-004-annotation-chips.md) · PRD: `SCENE-004`

- [x] SCENE-004/T1 — Fifteen chips across five chapters, each pinned to one body
  Evidence: assets/site.js:chips-showing
- [x] SCENE-004/T2 — Only one chapter names the bodies at a time, chosen by the reading line
  Evidence: assets/site.js — the chapter containing the reading line wins, rather than the first one intersecting the viewport, so a long chapter cannot hold the labels while the reader is in the next
- [x] SCENE-004/T3 — A chip opens away from the disc, and never clamps
  Evidence: assets/site.js:discRadius

## SCENE-006 · The mapping stage
Design: [docs/designs/scene/scene-006-mapping-stage.md](designs/scene/scene-006-mapping-stage.md) · PRD: `SCENE-006`

- [x] SCENE-006/T1 — A sticky three-column stage with a centre channel the world stands in
  Evidence: assets/site.css:--fit-channel
- [x] SCENE-006/T2 — Five pairs arriving one at a time, each side from its own edge
  Evidence: assets/site.css:.fit-row

## SCENE-005 · Orbit stages

PRD: `SCENE-005`

- [x] SCENE-005/T1 — Two chapters whose cards ride an ellipse about the world, a rear card masked rather than stacked
  Evidence: assets/site.js:initOrbits
- [x] SCENE-005/T2 — The stage takes the frame, and its heading is not squeezed into a ribbon
  Evidence: commit c0244ee
- [x] SCENE-005/T3 — The ellipse and the mask are measured from the world, not from a copy of its stylesheet value
  Evidence: assets/site.js:rockRadius
- [x] SCENE-005/T4 — The stage keeps its own clock while it holds the frame
  Evidence: commit c0244ee

## I18N-001 · Twenty-locale runtime dictionary
PRD: `I18N-001`, `I18N-002`

- [x] I18N-001/T1 — 303 keys complete in twenty locales, swapped without a reload
  Evidence: assets/i18n.js
- [x] I18N-001/T2 — The served copy and the dictionary cannot drift past a push
  Evidence: scripts/sync-static-copy.mjs
- [x] I18N-001/T3 — Sixteen non-`en`/`vi`/`zh`/`zt` locales reviewed on seven mechanical classes; six corrected
  Evidence: assets/i18n.js — coverage, brand preservation, register, typography, script, product-term consistency and English function-word leakage; `ko`, `ur`, `hi`, `bn`, `fr` and `de` corrected
- [!] I18N-001/T4 — Eleven locales read as prose, sentence by sentence, by someone who speaks them
  Blocked by: pending-external: a native reader for `es`, `pt`, `ru`, `tr`, `id`, `ms`, `tl`, `th`, `ar`, `ja` and `zt`. All eleven pass every mechanical class in `I18N-001/T3`; what no mechanical pass can see is a sentence that is correct and lifeless. Unblocks when a reader is available per locale — the eleven are independent, so the task closes locale by locale.

---

## I18N-002 · Served-copy parity gate
Design: [docs/designs/i18n/i18n-002-served-copy-parity-gate.md](designs/i18n/i18n-002-served-copy-parity-gate.md) · PRD: `I18N-002`

- [x] I18N-002/T1 — The check fails on markup drift, on locale parity loss, and on a moved node count
  Evidence: scripts/sync-static-copy.mjs — 245 localized nodes in index.html and twenty locales in 404.html, all three failure modes exercised
- [x] I18N-002/T2 — It runs in the pre-push hook and in CI
  Evidence: .githooks/pre-push · .github/workflows/ci.yml

## A11Y-001 · Accessibility baseline
Design: [docs/designs/a11y/a11y-001-accessibility-baseline.md](designs/a11y/a11y-001-accessibility-baseline.md) · PRD: `A11Y-001`

- [x] A11Y-001/T1 — Keyboard reach and a focus ring measured against the panel, not the void
  Evidence: assets/site.css:focus-visible — a ring tuned against the page ground disappears the moment the control sits on a panel, which is where most of them are
- [x] A11Y-001/T2 — Contrast measured against the painted pixel across five label chapters
  Evidence: assets/site.css — panel fill is opaque enough that the planet never reads through the right-hand side of a paragraph
- [x] A11Y-001/T3 — Reduced motion slows the scene and shows every chapter as a stacked list
  Evidence: assets/site.css:prefers-reduced-motion · assets/scene/boot.js
- [x] A11Y-001/T4 — Reveals and the nav mark read position directly rather than waiting for a threshold
  Evidence: assets/site.js — an IntersectionObserver is notified only when a ratio crosses a threshold, so a flick that carries a block from below the fold to above it in one frame crosses nothing and the block never appears
- [x] A11Y-001/T5 — A print stylesheet in black on white
  Evidence: assets/site.css:@media print

## DATA-001 · Schema and migrations

Design: [docs/designs/data/data-001-schema-and-migrations.md](designs/data/data-001-schema-and-migrations.md) · PRD: `DATA-001`

- [x] DATA-001/T1 — Choose and wire the migration tool; one command applies and one rolls back
  Evidence: apps/web/package.json — node-pg-migrate wired (docs/decisions-log.md#INFRA-DEC-06); `make migrate` prints the target then applies, `make migrate-down` rolls one back, verified against PostgreSQL 17.11.
- [x] DATA-001/T2 — Accounts table: identity, role, state, created and updated
  Evidence: apps/web/migrations/1788744617066_accounts.sql — the accounts table per DATA-001 with the citext e-mail, the role and state checks, and a `BEFORE UPDATE` trigger that maintains `updated_at` (verified: after an update it exceeds created_at). apps/web/src/db/db.test.ts holds the hand-written Kysely types to the migration column for column, mutation-proved to catch a drift in type, nullability, default, or uniqueness.
- [x] DATA-001/T3 — Sessions table, or the session store the auth library needs
  Evidence: apps/web/migrations/1788758032000_auth_store.sql — the sessions table: the cookie carries a random token and the row stores only its token_hash (unique), so a database dump is not a set of live sessions (AUTH-002); account_id cascades on erasure, and an index on account_id serves both the privilege-change 'delete every session for this account' and the cascade. Verified against PostgreSQL 17.11 (cascade deletes the sessions, token_hash refuses a duplicate); DATA-001 §3 reconciled to this token-hash scheme, and the choice of a hand-rolled store over the auth library is docs/decisions-log.md#AUTH-DEC-01. Held column for column by apps/web/src/db/db.test.ts.
- [x] DATA-001/T4 — Content items and their revisions, with the published revision named by a pointer
  Evidence: apps/web/migrations/1788749077541_content.sql — content_items and content_revisions per DATA-001, the circular current_revision_id FK made fail-closed (no on-delete: a published revision cannot be deleted while current), a CMS-R01 trigger that refuses editing a published revision's blocks while allowing publish and re-publish, and checks binding kind to updates and period to reports; verified against PostgreSQL 17.11 and mutation-proved by apps/web/src/db/db.test.ts.
- [x] DATA-001/T5 — Content grants and the audience constraint
  Evidence: apps/web/migrations/1788749077541_content.sql — content_grants with a composite PK, item and account cascading on erasure and granted_by set null, and the audience check on content_items; the on-delete behaviours verified against docs/designs/data/data-002-erasure-and-retention.md by a four-way erasure run.
- [x] DATA-001/T6 — Locale rows carrying a review state a query can filter on
  Evidence: apps/web/migrations/1788749077541_content.sql — content_locales with a composite (revision_id, locale) PK, a state check, a reviewed row required to carry reviewed_at, and reviewed_by set null on erasure; the generalized drift guard apps/web/src/db/db.test.ts holds every content table's Kysely types to the migration column for column.
- [x] DATA-001/T7 — Mail log and unsubscribe state
  Evidence: apps/web/migrations/1788752441424_platform.sql — mail_log keyed by account_id (cascade on erasure), a kind and a state check with no 'delivered' value that SMTP cannot honour, and an index on account_id; unsubscribes keyed by account with a source that names how it was set, the link token for a one-click unsubscribe and the admin reason for a manual stop-sending bound to their source by two checks (MAIL-002). Verified against PostgreSQL 17.11 and held column for column by apps/web/src/db/db.test.ts.
- [x] DATA-001/T8 — Audit table, append-only, with a database-level guard against update and delete
  Evidence: apps/web/migrations/1788752441424_platform.sql — audit with a caller-proof identity key (GENERATED ALWAYS refuses a supplied id), an at forced to now() by a BEFORE INSERT trigger so it cannot be backdated, a closed fifteen-action vocabulary, and a bare actor_id that survives erasure; append-only enforced by a BEFORE UPDATE OR DELETE row trigger and a BEFORE TRUNCATE statement trigger (SEC-R04). UPDATE, DELETE and TRUNCATE all refused and the id and at proven caller-proof against PostgreSQL 17.11. What before and after may record is the personal-data policy filed as docs/decisions-log.md#SEC-DEC-01, which blocks SEC-002/T4, not this table.
- [x] DATA-001/T9 — Configuration table with a recorded prior value
  Evidence: apps/web/migrations/1788752441424_platform.sql — config keyed by key, with value, the previous_value the CFG-001 undo reads, changed_by set null on erasure and changed_at; the table ships empty because each key's default lives beside its declaration in code. Held column for column by apps/web/src/db/db.test.ts.
- [x] DATA-001/T10 — Every migration has a down-migration that has been run
  Evidence: apps/web/scripts/migrate-roundtrip.mjs — the round trip runs the down step with count Infinity, so every migration's down is exercised on a throwaway database each run, not just the newest; `make migrate-roundtrip` completes up then down then up, verified against PostgreSQL 17.11.
- [x] DATA-001/T11 — Media and its references, with the audience reached by join
  Evidence: apps/web/migrations/1788752441424_platform.sql — media with a unique sha256 that deduplicates identical bytes, the bytes as bytea, a stored byte_size and uploaded_by set null on erasure; media_refs a composite (media_id, item_id) key with both sides cascading and an index on item_id, so a file is served to a reader by joining to the item that uses it (CMS-R06). Held column for column by apps/web/src/db/db.test.ts.
- [x] DATA-001/T12 — The portfolio state table
  Evidence: apps/web/migrations/1788752441424_platform.sql — portfolio keyed by a six-product check, a four-stage check including paused, a 140-character headline check and updated_by set null on erasure; the updated_at trigger carries a WHEN so it restamps only on a stage or headline change and an erasure nulling updated_by does not silently re-date the investor board (INV-003). Verified against PostgreSQL 17.11: a real change restamps, an erasure does not.
- [x] DATA-001/T13 — The invitations table: single-use token, expiry, and an atomic consume
  Evidence: apps/web/migrations/1788758032000_auth_store.sql — invitations with id, account_id (cascade on erasure), token_hash (unique; the token itself is never stored), expires_at not null and a nullable consumed_at, plus an index on account_id for 'invalidate every outstanding invitation for this account' (AUTH-003). The atomic consume `UPDATE invitations SET consumed_at = now() WHERE token_hash = $1 AND consumed_at IS NULL AND expires_at > now()` gives exactly one winner under two concurrent transactions, measured against PostgreSQL 17.11. Held column for column by apps/web/src/db/db.test.ts.

## AUTH-001 · Sign-in
Design: [docs/designs/auth/auth-001-sign-in.md](designs/auth/auth-001-sign-in.md) · PRD: `AUTH-001`, `SEC-R01`, `SEC-R03`

- [x] AUTH-001/T1 — Password hashing at the current cost, verified against a known vector
  Evidence: apps/web/src/auth/password.ts — Argon2id via the library chosen at docs/decisions-log.md#AUTH-DEC-03, at the OWASP first configuration (19 MiB, two iterations, one lane, version pinned), the cost in one const the sign-in and the invitation share so it cannot diverge. hashPassword, verifyPassword and needsRehash (rehash-on-sign-in when the stored cost is below target). verifyPassword takes a nullable hash and normalises a null or unreadable one to DUMMY_HASH inside the module, so an invited account with no password and a wrong password cost the same argon2 milliseconds and the call site cannot reintroduce a timing oracle (SEC-R03) — deep-review measured the equalisation sound (AUC 0.4881) and the pre-fix short-circuit 300x cheaper. Verified by apps/web/src/auth/password.test.ts (known-answer on the encoding, the non-throwing unreadable path, and a floor proving the null path pays the hash cost).
- [x] AUTH-001/T2 — Sign-in route: identical failure for an unknown account and a wrong password
  Evidence: apps/web/src/app/api/auth/sign-in/route.ts — POST verifies a hash on every path (DUMMY_HASH when the account or its password is absent), so an unknown address, a wrong password, a suspended account and an invited one return a byte-identical 401 at the same argon2 cost; 204 with the session cookie on success, 429 before any query when limited, 403 for a cross-origin POST (login CSRF), 400 for a malformed body. Verified against PostgreSQL 17.11 and mutation-proved (seven of seven mutants) by apps/web/src/app/api/auth/sign-in/route.test.ts.
- [x] AUTH-001/T3 — Rate limit per account and per address, with the limit stated in config
  Evidence: apps/web/src/app/api/auth/sign-in/route.ts — the route calls the config-driven limiter (apps/web/src/auth/rate-limit.ts) for the account key and the address key before any query, and refuses with 429 and the larger of the two Retry-Afters when either is over the limit; the address key is length-bounded so an over-long X-Forwarded-For cannot mint unbounded counters, and which forwarded hop is the client is docs/decisions-log.md#OPS-DEC-02. Verified against PostgreSQL 17.11 that spraying one account from many addresses hits the account limit and one address across many accounts hits the address limit (apps/web/src/app/api/auth/sign-in/route.test.ts).
- [!] AUTH-001/T4 — The sign-in form, in twenty languages, keyboard-reachable, with an accessible name on every field
  Blocked by: pending-decision: I18N-DEC-02 — the form must render in the reader's locale across twenty, and the application has no i18n framework yet; which one it uses is the owner's to settle.
- [x] AUTH-001/T5 — Regression test: a wrong password and an unknown account are indistinguishable in status, body and timing
  Evidence: apps/web/src/app/api/auth/sign-in/route.test.ts — the four failing states (unknown, wrong password, suspended, invited) return an identical 401 in status and body, and each is held above an argon2 timing floor derived from a warmed reference verification, so a short circuit on any path fails the test; a 429 is proved never to touch the database. Mutation-proved seven of seven against PostgreSQL 17.11.

## AUTH-002 · Session and role gate
Design: [docs/designs/auth/auth-002-session-and-role-gate.md](designs/auth/auth-002-session-and-role-gate.md) · PRD: `AUTH-002`, `SEC-R02`, `DATA-R05`

- [x] AUTH-002/T1 — Session cookie: httpOnly, SameSite=Lax, Secure, rotated on sign-in
  Evidence: apps/web/src/auth/session.ts — issue() writes a fresh sessions row storing sha256 of a 32-byte CSPRNG token (never the token) and returns the cookie: `__Host-valotech` outside development and `valotech` in it, HttpOnly, SameSite=Lax, Secure outside development, Path=/, Max-Age from SESSION_TTL_SECONDS; a fresh row per call is the rotation that keeps a planted cookie from becoming a session. Verified against PostgreSQL 17.11 (row token_hash equals sha256 of the cookie and not the raw token); apps/web/src/auth/session.test.ts pins the production __Host- cookie's Secure and Path attributes, which the route suite under development cannot reach. The bare-token vs signed cookie is docs/decisions-log.md#AUTH-DEC-02.
- [x] AUTH-002/T2 — Server-side invalidation, so a stolen cookie dies on sign-out
  Evidence: apps/web/src/auth/session.ts:invalidateSession deletes the `sessions` row whose `token_hash` is the SHA-256 of the presented token, and apps/web/src/auth/session.ts:invalidateAllForAccount deletes every row an account holds — a delete rather than a flag, because revocation that takes effect at the next natural expiry is not revocation. Both are account-scoped and caller-agnostic, so ADMIN-001's privilege-change path calls the same function on somebody else's behalf. apps/web/src/auth/session.ts:expiredCookie carries the issued cookie's name, path, SameSite, HttpOnly and Secure with `Max-Age=0`, so the browser drops exactly the cookie `issue` set. Verified against PostgreSQL 17.11 by apps/web/src/auth/sign-out.test.ts (18 tests): the row is gone, `resolveSession` then answers null, a second session for the same account survives, and a token no row holds resolves to a successful no-op. Mutation-proved — a delete that runs nothing, one keyed on the raw token rather than its hash, an account-wide delete that runs nothing, a live `Max-Age`, and an expiry sent under another path were each killed.
- [x] AUTH-002/T3 — Role gate at the query, not the template; a helper that cannot be forgotten
  Evidence: apps/web/src/auth/gate.ts:resolveSession — one `UPDATE sessions ... FROM accounts ... RETURNING` turns a cookie into an `Actor` (id and role) and slides `last_seen_at` and `expires_at` in the same statement, so the predicate that refuses an expired session or a suspended account is the predicate that withholds the slide and no ordering mistake can resurrect a session; apps/web/src/auth/gate.ts:requireInvestor and apps/web/src/auth/gate.ts:requireAdmin answer that actor or the response the caller returns — 303 to the form with no reader, 404 and never 403 for a signed-in investor at an admin surface, both `no-store`. Verified against PostgreSQL 17.11 by apps/web/src/auth/gate.test.ts: 21 tests, skipping cleanly with no `DATABASE_URL` and running with one, and mutation-proved eleven of eleven — the expiry predicate dropped, the active-account predicate dropped, the admin check dropped, 403 for 404, `no-store` dropped, the slide stopped, the raw token compared instead of its hash, the cookie name ignored, 307 for 303, the sign-in path changed, and every reader refused.
- [x] AUTH-002/T4 — Isolation test: an investor request for another investor's deck returns nothing, not a redirect
  Evidence: Closed by cross-reference to the content gate, which already proves this — a second copy of the test would be the redundancy §1.10 forbids. AUTH-002/T3's gate is the query predicate apps/web/src/content/access.ts:visibleTo, and the deck read is apps/web/src/content/read.ts:forReader, which composes it and returns an item only where the reader may see it — for a granted item, only where a content_grants row names them (the design's readDeck that joins deck_grants, in the unified content model). apps/web/src/content/read.test.ts drives the exact case this task names: an investor asking for a deck granted to another investor gets null — the granted-to-a fixture is null for investorB while it is the item for investorA and for an admin (the visibleTo and forReader cases), and the deck-type, granted-audience item added for CMS-006/T3 turns visible to a grantee only across an addGrant and back to null across removeGrant. It returns nothing and cannot return a redirect: forReader's return type is a view or null, so a refusal is the same null a missing item gives, which the 404-not-403 rule (a redirect would confirm the item exists, just as a 403 would — AUTH-002 design §2) collapses into one not-found answer at the route CMS-006/T4 carries.

## AUTH-003 · Invitation and password reset
PRD: `AUTH-003`

- [x] AUTH-003/T1 — Token generation, hashing, and storage that never holds a usable token
  Evidence: apps/web/src/auth/invitation.ts:issueToken mints 32 bytes from the system CSPRNG as base64url, hands the plaintext back to its caller and writes only its SHA-256, so the row an attacker reads from a dump carries nothing presentable. The two lifetimes the design fixes are apps/web/src/auth/invitation.ts:INVITATION_TTL_SECONDS — seven days, for a link that sits in an inbox over a weekend — and apps/web/src/auth/invitation.ts:RESET_TTL_SECONDS — one hour, for somebody at their keyboard; the caller passes the one its flow means, and both are constants rather than environment variables because the reason they differ is a security argument and not a per-deployment knob. The expiry is computed by the database, whose clock the consumption predicate also reads, so a skewed process cannot issue a token that outlives its constant. Verified against PostgreSQL 17.11 by apps/web/src/auth/invitation.test.ts (16 tests): no value in the row equals the token, `token_hash` equals a SHA-256 the suite computes for itself, and the stored expiry — measured inside the database — is within 30 seconds of seven days and of one hour. Mutation-proved: the token written in place of its hash, and the presented value compared rather than hashed, were both killed.
- [x] AUTH-003/T2 — Single-use consumption in one atomic statement, with expiry in the same predicate
  Evidence: apps/web/src/auth/invitation.ts:consumeToken is one `UPDATE invitations SET consumed_at = now() WHERE token_hash = $1 AND consumed_at IS NULL AND expires_at > now() RETURNING account_id`, so the statement that consumes is the check: an unknown token, a consumed one and an expired one are one answer, and no ordering mistake can admit a second use. Verified against PostgreSQL 17.11 by apps/web/src/auth/invitation.test.ts: a token returns its account once and null every time after, a refused attempt does not re-stamp `consumed_at`, an expired token answers null and is left unconsumed, the stored hash presented as though it were the token is refused, and two consumptions started together on two established connections yield exactly one account id and one null. That last test is the load-bearing one and it had to be made real: against a pool holding one idle connection the second racer spends 25 milliseconds opening a second while the first commits, and a consumption written as a read and then a write survived five runs of it; establishing both connections first kills that mutant five runs out of five. Mutation-proved further: the expiry predicate dropped, the single-use predicate dropped, and a consumption that never stamps the row were each killed.
- [ ] AUTH-003/T3 — The mail that carries the link, in the invitee's locale
- [ ] AUTH-003/T4 — The set-password form, its policy, and the sign-in that follows
- [x] AUTH-003/T5 — Reset answers identically for an address that exists and one that does not
  Evidence: apps/web/src/auth/invitation.ts:requestReset answers the same for an address an account holds and one it does not, and the sameness is a property of the code's shape: statements each keyed by the normalised address — a delete, then an `INSERT … SELECT` that lets the statement itself decide whether a row exists rather than a process-level branch a timing measurement could read — with the token minted and hashed before the transaction so the CSPRNG and SHA-256 are paid either way, and nothing returned. The address is normalised and length-bounded through apps/web/src/auth/address.ts:normaliseAddress, the one spelling apps/web/src/app/api/auth/sign-in/route.ts also uses, so a case or whitespace difference cannot mint a second rate-limit key over one account. The serialisation is an address-keyed advisory lock (apps/web/src/auth/invitation.ts:lockAddress, `pg_advisory_xact_lock(hashtext(address))`) rather than a `SELECT … FOR UPDATE`, which had taken a row lock only when an account existed and amplified under concurrency to a measured 5.8x separation at eight clients; the advisory lock is taken whether or not an account exists, so both paths wait on it identically and the residual is a flat ~1.5x write-versus-no-write envelope. Per docs/decisions-log.md#AUTH-DEC-04 the delete and the insert are narrowed to an `active` account, so a reset never destroys a still-invited account's invitation. Verified against PostgreSQL 17.11 by apps/web/src/auth/invitation.test.ts: statement-level barrier tests assert a known and an unknown address wait on the same advisory lock (observed in pg_stat_activity), a suspended and an invited account keep their outstanding invitation across a reset, and both properties are mutation-proved — removing the address lock, or narrowing neither statement to active, reddens a named test.
- [x] AUTH-003/T6 — A new invitation invalidates every outstanding one for that account
  Evidence: apps/web/src/auth/invitation.ts:issueToken deletes every unconsumed invitation the account holds and inserts the new one inside a single `db.transaction().execute`, so the account never holds two live tokens and never briefly holds none. Outstanding means unconsumed: a consumed row records that somebody used a link at a stated moment and is kept, while an unconsumed one is deleted rather than stamped, because stamping a link nobody opened would write down a use that never happened. Verified against PostgreSQL 17.11 by apps/web/src/auth/invitation.test.ts: after a re-issue the first token consumes to null and the second to the account, exactly one row survives and it is the new one, a consumed row is left standing, and another account's outstanding token is untouched and still consumes to its owner. The transaction itself is pinned by refusing inserts to `invitations` with a trigger the test installs and drops around one call: the delete rolls back with the insert and the account keeps the link it was sent, where two separate statements leave it holding nothing. Two issues racing for one account are serialised by a SELECT of the account row FOR UPDATE taken first in the transaction, and the partial unique index invitations_one_outstanding_per_account (account_id where consumed_at IS NULL, apps/web/migrations/1788758140000_invitation_single_outstanding.sql) makes one-outstanding a schema property; a concurrent-issue test fires two issues together on established connections and pins exactly one live token, and it fails three runs of three when the lock and the index are both removed. Mutation-proved: invalidation removed, invalidation widened to consumed rows, and the transaction split into two statements were each killed.
- [x] AUTH-003/T7 — With no mail credential, the invitation is still created and its link is shown to the admin
  Evidence: apps/web/src/auth/invitation.ts:inviteAccount creates the one way an account comes to exist — there is no self-registration — in a single transaction: it inserts an `invited` account (state and password_hash left to their column defaults) with the address normalised through apps/web/src/auth/address.ts:normaliseAddress, issues a seven-day invitation through the shared apps/web/src/auth/invitation.ts:issueTokenIn, and records an `account.create` audit against the admin who made it (SEC-R04, in the same transaction because apps/web/src/audit/record.ts:recordAudit takes it — the account, its token and its audit row commit or roll back as one). An address a normalised account already holds answers with EmailTakenError and writes nothing (`ON CONFLICT (email) DO NOTHING`), and the error names no address (DATA-R02). It always returns the invite link and a `deliverByHand` sentence — non-empty in both branches, because until the send lands (AUTH-003/T3) the admin delivers the link by hand whether or not a mail credential is set, so no screen reads 'sent' for an invitee who received nothing (SEC-R05). An address-keyed advisory lock serialises it with a concurrent reset so the two cannot interleave into a unique-index violation. Verified against PostgreSQL 17.11 by apps/web/src/auth/invitation.test.ts: inviting creates an invited account with a null password and one live invitation whose returned link consumes to it, a duplicate spelling collides into EmailTakenError, the account-token-audit trio is all-or-nothing under a trigger the test installs, and each is mutation-proved.

## AUTH-004 · Sign-out
Design: [docs/designs/auth/auth-004-sign-out.md](designs/auth/auth-004-sign-out.md) · PRD: `AUTH-004`

- [x] AUTH-004/T1 — POST sign-out deletes the session row, then expires the cookie
  Evidence: apps/web/src/app/api/auth/sign-out/route.ts — `POST /api/auth/sign-out` reads the cookie through apps/web/src/auth/gate.ts:presentedToken, awaits `invalidateSession`, and only then returns apps/web/src/auth/sign-out.ts:signedOut, a `303` to `/` carrying the expiring cookie and `no-store`. The order is the feature: a cookie cleared before the row is deleted leaves a live session nobody references, so a failed delete raises rather than answering a sign-out that did not happen. `POST` is the only export, so a `GET` — which any embedded image or link checker performs — is answered `405`. Measured against the built server (`next build` then `next start`, Next 16.3.4, PostgreSQL 17.11): `303`, `location: /`, `set-cookie: valotech=; Path=/; Max-Age=0; SameSite=Lax; HttpOnly`, `cache-control: no-store`, the account's session count 3 → 2, and `GET /api/auth/sign-out` → `405` leaving the count unchanged. Pinned by apps/web/src/auth/sign-out.test.ts against PostgreSQL 17.11.
- [!] AUTH-004/T2 — The session list shows this account's live sessions and marks the current one
  Blocked by: pending-decision: I18N-DEC-02 — the list is a localised page and the application has no i18n framework yet; which one it uses is the owner's to settle. The design's own §6 question rides with it: `sessions` carries no address or user-agent column, and whether the list shows them is a personal-data choice (`DATA-R01`) rather than a schema oversight.
- [x] AUTH-004/T3 — Ending every session, including this one, in one action
  Evidence: apps/web/src/app/api/account/sessions/all/route.ts — the POST handler resolves the presented cookie through apps/web/src/auth/gate.ts:accountForToken, the gate's non-sliding sibling, so the resolution is the authorisation: an expired cookie and a suspended account both end nothing and both still redirect. Resolving without sliding is the property: a sliding resolve followed by a delete that failed would leave the session live with a fresh full lifetime, worse than doing nothing, so this path reads the account rather than extending the session it is about to end. It then ends every session and records the act as one transaction — apps/web/src/auth/session.ts:invalidateAllForAccountIn deletes them and apps/web/src/audit/record.ts:recordAudit writes one `session.invalidate_all` row with this account as both actor and subject (SEC-R04) — because ending everything is a privileged act, unlike an ordinary sign-out, which records nothing (AUTH-004 §4). The row's `before`/`after` are null, the SEC-DEC-01 safe default, until SEC-002/T4 settles it. Measured against the built server: `303` to the gateway with the expiring cookie and `no-store`, and the account's two live sessions — the one that asked and one it had never seen — both gone. Pinned by apps/web/src/auth/sign-out.test.ts: exactly one `session.invalidate_all` row per call with the account as actor and subject, and — mutation-proved by removing the record call — an audit failure inside the transaction rolls the delete back, leaving the session present and at its original expiry, neither dropped nor slid.
- [~] AUTH-004/T4 — Every authenticated response is `no-store`, verified by pressing the back button
  Note: The mechanism ships and is header-verified; the acceptance the row names — pressing Back — is owed, so this is in-progress rather than closed. apps/web/src/proxy.ts marks a response `Cache-Control: no-store` when the request presented a session cookie, read through the gate's own parser — the cookie is what makes a response authenticated, so a surface added later is covered by the commit that mounts it rather than by an edit to a prefix list. Measured against the built server: `GET /` with a session cookie answers `cache-control: no-store`. `SEC-001` then made every route dynamic, so an anonymous request answers Next's dynamic default (`private, no-cache, no-store, max-age=0, must-revalidate`) rather than a cacheable response, and on the ordinary routes the cookie-conditional no longer discriminates; the one route that still shows it is Next's static `/_global-error`, which answers `s-maxage=31536000` anonymously and `no-store` with a cookie. A `/_next/static/chunks` bundle stays publicly cacheable and `immutable` with the cookie present, so the matcher's exemption holds; the header is pinned by apps/web/src/proxy.test.ts (35 tests), whose mutation run kills both a proxy that marks every response `no-store` and one that marks none. What is not done is the browser step: pressing Back and getting the sign-in path rather than a rendered gated page exercises the back-forward cache, a mechanism separate from the HTTP cache whose `no-store` treatment differs by browser, so the header assertion does not stand in for it. It needs a gated surface, which arrives with INV-002, and is recorded as owed in docs/runbooks/auth-004-sign-out.md.
- [x] AUTH-004/T5 — Signing out twice succeeds; there is no already-signed-out error
  Evidence: apps/web/src/auth/session.ts:invalidateSession treats deleting nothing as success and neither handler branches on what it found, so a second sign-out with the same cookie, a cookie whose row never existed, and no cookie at all all answer the same `303` with the same expiring cookie. Measured against the built server (a second `POST` with an already-deleted cookie → `303`, `location: /`, `Max-Age=0`) and pinned by apps/web/src/auth/sign-out.test.ts, whose mutation run kills a handler that raises when it finds no session.

## ADMIN-001 · Account management
PRD: `ADMIN-001`, `SEC-R04`

- [x] ADMIN-001/T1 — The list, sortable by last sign-in, with role and state
  Evidence: the column the list turns on did not exist, so apps/web/migrations/1788744617066_accounts.sql gains `last_sign_in timestamptz`, nullable, where null means the person has never signed in. The same migration narrows the `updated_at` trigger to a WHEN on the account's defining attributes, so writing `last_sign_in` on a sign-in does not restamp `updated_at` — which would otherwise read "last signed in" for an active account — folded now because `DATA-R07` freezes the file after deploy. It is stored rather than derived because `sessions.last_seen_at` is deleted on sign-out and the fact has to survive it, and it arrives by folding the CREATE rather than by an ALTER (`DATA-R07`): apps/web/src/db/db.test.ts parses the CREATE body, node-pg-migrate does not checksum, and a fresh database is what deploys. The down migration already drops the table, so the round trip needed no edit and was run rather than asserted (`DATA-R06`) — make migrate-roundtrip green, up then down then up on a throwaway database. apps/web/src/db/types.ts:AccountsTable and its `SCHEMA` descriptor carry the column, held equal to the migration column for column by apps/web/src/db/db.test.ts. apps/web/src/app/api/auth/sign-in/route.ts stamps it from the database clock on the one path that ended in a sign-in, before `issue`, so no session is ever issued without a fresh value behind it — the reverse, a stamp with no session, is a timestamp slightly ahead of a sign-in the person retries — and on no refused path, since a column that moved on a wrong password would name whoever was guessing as the person last in the room. apps/web/src/admin/accounts.ts:listAccounts reads the five things the list shows and nothing more (`DATA-R01`), ordered `last_sign_in asc nulls first` then by address: stalest first with the never-signed-in above them, because never having signed in is the strongest form of the signal rather than the absence of one, and the address is the one unique column, so the order is total and cannot reshuffle between renders. apps/web/src/app/admin/accounts/page.tsx renders it as a real table with `scope="col"` headers (`A11Y-R01`, `A11Y-R02`), `never` for a null and timestamps as UTC, English like the rest of the console, gated by the `/admin` layout it does not re-check; apps/web/src/app/admin/accounts/accounts.module.css is its style. Verified against PostgreSQL 17.11 by apps/web/src/admin/accounts.test.ts on the suite's own database — the empty list, the whole row key for key so a widened read is caught, the stale-first order asserted against an insertion order that is its reverse, the address tie-break, every state and role listed, and a sign-in leaving `updated_at` untouched where a change to a defining attribute moves it — and by apps/web/src/app/api/auth/sign-in/route.test.ts, where a success stamps a value inside the request bounded by the database's own clock read either side, and a wrong password, a suspended account and an invited one each leave it exactly as it was: 22 files, 437 tests. Seven mutants each killed a named test, every filter first proved to select a test on the clean tree: the write removed, the write moved onto every attempt against a known account, `nulls last`, `desc`, the tie-break reversed, the read widened to `selectAll`, and the `updated_at` trigger's WHEN dropped so a sign-in restamps it. Driven in a browser (Playwright, Chrome) against the built server on PostgreSQL 17.11: signed in as a seeded admin through the real POST, `/admin/accounts` answered `200` with `cache-control: no-store` and rendered five rows in the order the read asks for — the two never-signed-in accounts first and in address order, then `2025-02-03`, then `2026-08-09`, then the admin bearing the instant it had just signed in, which is the write measured end to end; five `columnheader` roles in the accessibility tree; zero console errors, zero failed resources and zero policy violations, both inline scripts nonced and hydration confirmed by a live `next.router` and a drained flight payload under a policy carrying no `unsafe-inline`. Signed out, the same path answers `307` to the sign-in page. The row's `sortable` ships as a fixed served order — stalest first, the never-signed-in above them — and no re-sort control was built; whether the design's `sortable` (`ADMIN-001` §3) means an interactive control is open at `ADMIN-DEC-02`.
- [x] ADMIN-001/T2 — The person page: identity, access, sessions, actions
  Evidence: apps/web/src/app/admin/accounts/[id]/page.tsx renders the person page under the /admin gate — identity (apps/web/src/admin/accounts.ts:personIdentity, UUID-guarded, seven fields, never password_hash), access (apps/web/src/content/decks.ts:grantedDecksForAccount, the account's deck grants with pin and last-opened, deck_reads left-joined and scoped by both deck and account), sessions (apps/web/src/auth/session.ts:liveSessionsForAccount, live rows only, never token_hash) with an end-all control, and the actions. Acts go through one route apps/web/src/app/admin/accounts/[id]/action/route.ts (POST, origin-checked then requireAdmin then subject-resolved) calling the existing services with the requester's id; suspend runs through the ADMIN-002/T3 destructive component. Proven against PostgreSQL 17.11 by apps/web/src/admin/person.test.ts (16) and apps/web/src/app/admin/accounts/[id]/action/route.test.ts (21): authZ refuses a non-admin and a signed-out caller, the audit actor is the admin and not the subject, the self and last-admin guards hold through the route, and every read is scoped to one account (DATA-R05). Browser-verified (Playwright drive against next build then start): opening the suspend confirmation moves focus into the panel, Escape and Cancel return focus, and Confirm suspends the account and empties the sessions section as router.refresh re-reads. Mutation-proved, each KILLED: passing the subject id where the actor id belongs, and dropping the account scope on the deck_reads join. Deep-reviewed sound on authZ, actor id, the guards, DATA-R05 and the origin check. Delete is ADMIN-001/T4; role-change is ADMIN-001/T7.
- [x] ADMIN-001/T3 — Suspending ends every live session in the same transaction
  Evidence: apps/web/src/admin/accounts.ts:suspendAccount suspends an account in one transaction: an `UPDATE ... WHERE id=$1 AND state<>'suspended' RETURNING id` whose returned row is the check — a second concurrent suspend blocks on the row lock, re-evaluates the predicate against the row the first committed, and matches nothing, so the trail carries one act not two — then apps/web/src/auth/session.ts:invalidateAllForAccountIn ends every session, the outstanding (unconsumed) invitation is deleted so a link cannot re-open the access just ended (AUTH-003), and apps/web/src/audit/record.ts:recordAudit writes one `account.suspend` row. All one transaction, so a rollback — forced in test by an unparseable actor id the audit's uuid column refuses — takes the state, the sessions and the invitation back together (SEC-R04); `before`/`after` are null, the SEC-DEC-01 safe default, until SEC-002/T4. No single act may strand the room or turn on the actor (ADMIN-DEC-01, safe default): apps/web/src/admin/accounts.ts:isLastActiveAdmin locks the active-admin rows in id order inside the transaction, and the operation refuses when the subject is the actor or the last admin who can sign in — counted `active`, not by role, since a suspended admin cannot sign in to undo anything. Verified against PostgreSQL 17 by apps/web/src/admin/accounts.test.ts, which runs on a database of its own so the global active-admin count is deterministic: it ends the sessions and records one act, deletes the outstanding invitation, leaves a consumed one, is idempotent when already suspended, rolls back whole on audit failure, touches no other account, and refuses the last active admin, self, and the suspended-admin-present case. The guard's removal, its `state='active'` filter, its subject-id check, its self-check, the concurrent-demotion race, and the suspend-then-reissue refusal (apps/web/src/auth/invitation.ts:issueTokenIn throwing SuspendedAccountError) are each mutation-proved.
- [x] ADMIN-001/T4 — Deletion is a real delete; the confirmation lists what goes and what remains, and takes the typed name
  Evidence: apps/web/src/app/admin/accounts/[id]/delete/route.ts is the dedicated delete route (not the shared action route) — POST {confirmName}, origin-checked then requireAdmin then subject-resolved, then the typed name re-checked server-side with the same apps/web/src/app/admin/destructive-actions.ts:typedNameMatches the panel gates on (a posted body is whatever the caller sent), then apps/web/src/admin/accounts.ts:eraseAccount, which owns the self and last-admin guards and audits account.delete in one transaction (ADMIN-DEC-01, SEC-R04). The confirmation lists what goes and what remains: apps/web/src/admin/accounts.ts:erasureCounts assembles the sessions, invitations, deck-read and report-read counts and, from apps/web/src/content/grants.ts:erasureContentCounts (which names the content tables where check-content-access allows it), the grant count and the surviving authored-revision count (DATA-002/T5, the author nulled). The delete control is the first final-act caller of the ADMIN-002/T3 component (typed name) and navigates to the account list on success, since the person is gone. Proven against PostgreSQL 17.11 by apps/web/src/app/admin/accounts/[id]/delete/route.test.ts (22) and apps/web/src/admin/accounts.test.ts (erasureCounts, 2): a non-admin refused, a wrong or mis-cased name refused with nothing written, an unknown id 404 before the name check, the self and last-admin guards refused through the route, a real delete removing the account and its cascades with account.delete audited to the admin, and every count scoped to one account (another person's rows excluded, DATA-R05). Browser-verified (Playwright drive against a built server): the Delete confirmation shows the typed field and focuses it, Confirm stays off until the exact name is typed, the preview renders, and confirming deletes and lands on the list — which also exercises the ADMIN-002/T3 typed-name rendering that had no caller until now. Mutation-proved, each KILLED: dropping the server-side name re-check, writing the subject as the audit actor, and dropping the account scope on each of the two count paths. Privacy: the preview shows counts, not personal data (DATA-R01), and the delete is a real hard-delete with the audit retained (DATA-R03).
- [x] ADMIN-001/T5 — Deleting the last admin, or yourself, is refused
  Evidence: the refusal is apps/web/src/admin/accounts.ts:eraseAccount's guard (DATA-002/T2), which ADMIN-001 is the admin half of (§4 of its design): before any write, an actor erasing their own account is refused, and erasing the last admin who can sign in is refused through isLastActiveAdmin — both returning the false a no-op returns, weightier here than in suspendAccount because erasure has no inverse (ADMIN-DEC-01). Proven against PostgreSQL 17.11 by apps/web/src/admin/accounts.test.ts: an admin erasing their own account is refused and writes nothing; erasing the last active admin is refused and writes nothing; and an admin is erased while another active admin remains — the self guard and the last-admin guard each mutation-proved under DATA-002/T2. The delete surface that presents this refusal is ADMIN-001/T4 and lands with the admin console; this row is the rule, and the rule holds in the function that surface calls.
- [x] ADMIN-001/T6 — Creation issues an invitation; no admin ever sets another person's password
  Evidence: apps/web/src/app/admin/accounts/invite/route.ts is the create route — POST {name, email, role}, origin-checked then requireAdmin, the role validated against ACCOUNT_ROLES and the address shape-checked, then apps/web/src/auth/invitation.ts:inviteAccount, which creates the invited account, its single-use token and the account.create audit in one transaction and sets no password (AUTH-003). The route hands back the invite link for the admin to deliver by hand, since nothing mails it yet (AUTH-003/T3). apps/web/src/app/admin/accounts/new/page.tsx is the form (name, email, role), and the accounts list links to it. Proven against PostgreSQL 17.11 by apps/web/src/app/admin/accounts/invite/route.test.ts (8): a non-admin refused, a signed-out caller redirected, a cross-origin post refused, a valid invite creating an invited account with the name and role and a link that consumes to it with account.create audited to the admin, an admin created when that role is chosen, an unknown role and a blank name and a no-@ address each refused, and a second invite of a taken address refused with one account left. Mutation-proved, each KILLED: dropping the role check, the address shape-check, the email-taken 409, and the cross-origin guard. The form is a standard labelled form posting to the tested route; its interactive submit-flow is not independently browser-driven this iteration, the route being the verified surface. Completes ADMIN-001.
- [x] ADMIN-001/T7 — A role change ends every session and is audited
  Evidence: apps/web/src/admin/accounts.ts:changeRole changes an account's role in one transaction: an `UPDATE ... WHERE id=$1 AND role<>$2 RETURNING id` whose returned row is the check, then apps/web/src/auth/session.ts:invalidateAllForAccountIn ends every session — a privilege change that left the old session's claims standing is one that has not happened (SEC-R02) — then apps/web/src/audit/record.ts:recordAudit writes one `account.role_change` row, all one transaction so a rollback takes the role and the sessions back together (SEC-R04); `before`/`after` are null, the SEC-DEC-01 safe default (option B would record the role and state here), until SEC-002/T4. An invited account keeps its invitation, unlike a suspension's. The same guard as T3 refuses the actor and the last active admin before mutating (ADMIN-DEC-01). Verified against PostgreSQL 17 by apps/web/src/admin/accounts.test.ts: a role change ends every session and records one act, a demoted admin's elevated session cannot outlive the demotion, the act is idempotent and rolls back whole on audit failure, and the last-active-admin and self refusals and the two-concurrent-demotions race — exactly one succeeds, one active admin remains — are mutation-proved.
- [x] ADMIN-001/T8 — Reinstating a suspended account restores it to active and is audited
  Evidence: apps/web/src/admin/accounts.ts:reinstateAccount restores a suspended account in one transaction — an `UPDATE accounts SET state='active' WHERE id=$1 AND state='suspended' RETURNING id` whose returned row is the check (an account that is not suspended matches nothing and the act is a no-op) — then apps/web/src/audit/record.ts:recordAudit writes one `account.reinstate` row, so a rollback (forced in test by an unparseable actor id the audit's uuid column refuses) takes the state change back with it (SEC-R04). It is the inverse of suspendAccount and deliberately simpler: no session is restored (suspension deleted them; the person signs in afresh), no invitation is restored, and no last-admin or self guard is needed — reinstating only adds to the admins who can sign in, so it cannot strand the room, and a suspended account's holder cannot sign in to reinstate themselves. `account.reinstate` joins the closed audit vocabulary by folding it into the audit table's CREATE (apps/web/migrations/1788752441424_platform.sql) and apps/web/src/db/types.ts:AUDIT_ACTIONS, which apps/web/src/db/db.test.ts holds equal by parsing the migration. Verified against PostgreSQL 17 by apps/web/src/admin/accounts.test.ts on the suite's own database: it restores a suspended account and records one act, leaves an active or invited account untouched and records nothing, answers false for an absent id, rolls the state back on audit failure, and touches no other account — the state predicate, the audit write and the action name each mutation-proved.

## ADMIN-002 · Admin console shell
PRD: `ADMIN-002`

- [x] ADMIN-002/T1 — A `/admin` segment layout whose role check every page inherits, answering `404` to a non-admin
  Evidence: apps/web/src/app/admin/layout.tsx gates the admin segment through apps/web/src/auth/page-guard.ts:requireAdminPage, which resolves the request's session through AUTH-002's requireAdmin and translates its answer into the App Router's control flow — notFound() for the 404 a non-admin receives, redirect() for a signed-out reader. Every page beneath the segment is a child of this layout and inherits the check structurally (SEC-R01). Verified against the built server (next build then next start, Next 16.3.4, PostgreSQL 17.11): an admin request answered 200, an investor request answered 404 — not 403, so the console's existence is not confirmed to a guess — and a signed-out request redirected to the sign-in page; the investor 404 and the admin page both carried Cache-Control no-store from the proxy, so a shared cache cannot hold the who-asked 404 to serve an admin. The translation is pinned by apps/web/src/auth/page-guard.test.ts (3 tests, mutation-proved: a 404 rendered as a redirect, which would confirm the console exists, reddens the test).
- [x] ADMIN-002/T2 — The seven destinations, with the landing surface listing what needs attention
  Evidence: The seven-destination nav ships in apps/web/src/app/admin/layout.tsx as the console's chrome, and the landing surface apps/web/src/app/admin/page.tsx now lists its first attention source — a deck an active investor was granted and has never opened — read by apps/web/src/content/decks.ts:decksGrantedButNeverOpened, which anti-joins content_grants to deck_reads and returns the active-account, deck-item grants with no read of any version, longest-waiting first (an invited or suspended grantee cannot open it, and a granted non-deck is not a deck a reader opens). The surface stays honest when there is nothing: an empty list renders the nothing-to-attend-to message rather than an invented item. The other sources the design names — content with unreviewed locales, a stale portfolio row, the register's open questions — join it as CMS-005, INV-003 and their reads land. Proven against PostgreSQL 17.11 by apps/web/src/content/content-decks.test.ts (20 tests): a never-opened active grant is listed, a grant opened at any version is dropped, an invited or suspended grantee is omitted, a granted non-deck is omitted, and the list is ordered longest-waiting first. Mutation-proved, each KILLED: inverting the never-opened anti-join, changing the active-only filter, changing the deck-only filter, and reversing the order.
- [x] ADMIN-002/T3 — One destructive-action component, naming the subject, with a typed confirmation for the three that cannot be undone
  Evidence: apps/web/src/app/admin/destructive-action.tsx is the one control every irreversible act is reached through — a disclosure that names the subject ("Suspend Ada Lovelace?"), states the consequence and whether anything undoes it, and for an act nothing undoes holds Confirm off until the subject's name is typed. What counts as destructive lives in one registry apps/web/src/app/admin/destructive-actions.ts, and whether a typed name is demanded is derived from the reversal being final so the two cannot drift; apps/web/src/app/admin/destructive-actions.ts:typedNameMatches ignores surrounding whitespace and matches nothing for an empty subject, so a missing name never enables a final act. Its live caller is the person page's suspend (ADMIN-001/T2). Proven by apps/web/src/app/admin/destructive-actions.test.ts (9): exact and whitespace-forgiving match, an empty subject and an empty field both false, and the typed-name set is exactly the three final acts. Mutation-proved: dropping the empty-subject guard is KILLED. Browser-verified (Playwright drive against next build then start): opening the suspend confirmation moves focus to the heading, Escape and Cancel return focus to the trigger, keyboard-only open works, Confirm is enabled for the reversible suspend, and after Confirm focus lands on the outcome rather than the page body. A11Y-R03 contrast computed clear. Owed when a final-act caller lands (ADMIN-001/T4, CMS-003, MAIL-001): the browser check of the typed-name rendering, which no surface renders today.
- [x] ADMIN-002/T4 — An environment bar wherever `APP_ENV` is not production
  Evidence: apps/web/src/app/admin/layout.tsx renders an environment bar above the console when apps/web/src/config/index.ts reports app.env is not production, naming the environment in text and in a hue the console uses nowhere else (apps/web/src/app/admin/admin.module.css holds the warning token, distinct from the link accent), so it carries meaning beyond colour (A11Y-R03). Verified against the built server under APP_ENV=development: the bar rendered, reading the environment name and the words not production.
- [x] ADMIN-002/T5 — Console chrome in English, with the exception stated where a reader will find it
  Evidence: apps/web/src/app/admin/layout.tsx renders the console chrome — a left rail of the seven destinations, a content column, the environment bar — in English, and states the I18N-R01 exception in its own doc comment where a developer reading the layout finds it, matching the design §3 statement: I18N-R01 governs visitor-facing strings, both admins read English, and the content an admin writes is translated separately (CMS-005). Verified against the built server: the rail and its destination labels rendered in English.

## INV-001 · Investor room shell
PRD: `INV-001`

- [ ] INV-001/T1 — The landing surface: what is new, where things stand, the current report, your decks
- [ ] INV-001/T2 — Empty and error states are different renderings, each saying which it is
- [ ] INV-001/T3 — Flat navigation over four destinations, with the current one marked
- [ ] INV-001/T4 — The room's chrome is the gateway's, with no scene
- [ ] INV-001/T5 — An expired session returns the reader to where they were going

## INV-002 · Gated gateway chapters, served
PRD: `INV-002`, `SEC-R01`

- [!] INV-002/T1 — The gated components are not called for a reader who may not see them
  Blocked by: SITE-005/T1 — the gateway is still the static file, so there is no server response to withhold the gated chapters from; the gate they would be withheld by stands.
- [ ] INV-002/T2 — A test requests the page with no cookie and proves a gated sentence is absent from the body
- [ ] INV-002/T3 — The nav's gated links are not rendered rather than hidden
- [ ] INV-002/T4 — The dictionary splits, and the gated catalogue is sent only to an entitled reader
- [ ] INV-002/T5 — The parity gate counts both catalogues
- [ ] INV-002/T6 — The invitation block, in twenty locales, carrying no fragment of what it invites to

## INV-003 · Portfolio progress
PRD: `INV-003`

- [ ] INV-003/T1 — Six rows, a constrained product and a closed four-word stage vocabulary
- [ ] INV-003/T2 — Editing one product at a time, audited with the previous stage and headline
- [ ] INV-003/T3 — A changed stage offers a prefilled progress update, and can be declined
- [ ] INV-003/T4 — All six always render, including paused, with an absent row filled rather than dropped
- [ ] INV-003/T5 — Stage is carried by a word as well as by colour, and the board states when it last changed

## DECK-001 · Deck authoring
PRD: `DECK-001`

- [x] DECK-001/T1 — Sections derived from level-2 headings, with the block array the single source
  Evidence: apps/web/src/content/sections.ts:deriveSections partitions a deck's blocks at each level-2 heading, the block array the single source. Proven by apps/web/src/content/sections.test.ts — the sections concatenate back to the input, a level-3 heading stays within its section, and a run before the first heading forms one leading section. Mutation-proved: splitting at level 3 rather than level 2 is killed.
- [ ] DECK-001/T2 — An overview of section cards in order, showing heading, first line and what each carries
- [ ] DECK-001/T3 — Reordering by drag and by keyboard, writing back to the block array
- [ ] DECK-001/T4 — Section, word and figure counts in the overview
- [x] DECK-001/T5 — Speaker context per section, stripped in the investor read path and proven by a test
  Evidence: apps/web/src/content/blocks.ts carries an optional speaker context on a heading, and apps/web/src/content/blocks.ts:withoutSpeakerContext removes it; apps/web/src/content/decks.ts:deckRevisionFor strips it from the reading view so no reading surface serves it. Proven against PostgreSQL 17 by apps/web/src/content/content-decks.test.ts — the investor read returns the heading without its context, the paragraph intact — and as a unit by apps/web/src/content/sections.test.ts. Mutation-proved: removing the strip from the read path is killed.

## DECK-002 · Deck versioning and publishing
PRD: `DECK-002`

- [x] DECK-002/T1 — A monotonic version assigned at publication, never reused, holes kept
  Evidence: a deck revision takes a monotonic version at publication (CMS-R01). apps/web/migrations/1788749077541_content.sql folds a nullable content_revisions.version, and apps/web/src/content/publish.ts:versionForPublish assigns it inside the publish transaction only for a deck and only when the revision has none yet — version = coalesce(max(version for this deck), 0) + 1, read under the item-row lock apps/web/src/content/publish.ts:lockItem already holds so two publications of one deck cannot take the same number. A re-publish keeps the version it was shown under, so the version a grantee read keeps resolving; a report or an update takes none, so the column stays null and no non-deck publish reads it; and a withdrawal never clears a version, so a withdrawn one leaves a hole rather than being reused. version is Generated<number | null> in apps/web/src/db/types.ts and omitted from apps/web/src/content/items.ts:ContentRevision until a reader that needs it (DECK-003) adds it. Folded per DATA-R07: make migrate-roundtrip is green (up, down, up). Verified against PostgreSQL 17.11 by apps/web/src/content/content-decks.test.ts (5 tests, isolated database): successive publications number from one, each deck numbers from its own one and not a global sequence, a re-publish keeps the version, a withdrawal leaves a hole so the next is max + 1, and an update takes null. Mutation-proved and restored byte-identical: inverting the deck gate, turning the max + 1 into + 0, and inverting the re-publish guard each redden a named test. Recreating the shared development database to test this fold surfaced and fixed a latent hazard in CMS-007's blocks_text — it called jsonb_array_elements on a body that was not a JSON array and threw, failing any direct write of a non-array body (the generated column runs on every write); it now yields no text for a non-array body, proven by a regression in apps/web/src/content/content-search.test.ts.
- [x] DECK-002/T2 — An optional pinned version on a grant; unpinned readers get the current one
  Evidence: content_grants gains a nullable pinned_version (apps/web/migrations/1788749077541_content.sql), apps/web/src/content/grants.ts:addGrant takes an optional pin (null by default), and apps/web/src/content/decks.ts:deckRevisionFor resolves it — a grantee whose grant pins version n reads that version's revision, while an unpinned grantee and an admin read the current published one. Access is the one predicate: deckRevisionFor composes apps/web/src/content/access.ts:visibleTo first (CMS-006), so a reader who may not see the deck gets nothing whatever a pin would resolve to, and the read lives in the content module (CMS-006/T5). pinned_version is Generated<number | null> in apps/web/src/db/types.ts so addGrant may omit it. Folded per DATA-R07: make migrate-roundtrip is green. Verified against PostgreSQL 17.11 by apps/web/src/content/content-decks.test.ts (isolated database): the pin is stored on the grant and null when unpinned; after a new publication a pinned grantee still reads version 1 while an unpinned one reads version 2; and a non-grantee gets null while an admin gets the current version. Mutation-proved and restored byte-identical: inverting the pinned-versus-current branch serves the wrong version, and dropping the visibleTo clause serves a non-grantee — each reddens a named test.
- [ ] DECK-002/T3 — An unpinned reader is told once when the version changed, with what changed by section
- [x] DECK-002/T4 — A read record per account per version, deleted with the account
  Evidence: apps/web/migrations/1788758240000_deck_reads.sql creates the deck_reads table — one row per account per deck version, account_id and deck_id cascading — already declared in the erasure manifest (docs/designs/data/data-002-erasure-and-retention.md). apps/web/src/content/decks.ts:recordDeckRead upserts a read: the first open sets both timestamps and each later open moves last_opened_at while first_opened_at stays, so the row answers which version was shown and when it was first and last opened, and nothing more (DECK-002 §6). apps/web/src/db/types.ts and apps/web/src/db/db.test.ts carry the table schema. Proven against PostgreSQL 17 by apps/web/src/content/content-decks.test.ts — the version stored, a distinct row per version, first kept and last advanced on a re-open in one row, and the record deleted when the account is. migrate-roundtrip green. Mutation-proved: updating the wrong timestamp on conflict, and dropping the version from the conflict target, are each killed.
- [ ] DECK-002/T5 — The publish confirmation names every investor who will see the new version
- [x] DECK-002/T6 — Withdrawal does not break a pin to the withdrawn version
  Evidence: apps/web/src/content/decks.ts:deckRevisionFor resolves a pinned grantee to the revision carrying that version (content_revisions where version equals the pin), not through the item's current pointer, so a withdrawal — which moves the pointer but never deletes the revision or clears its version (apps/web/src/content/publish.ts:withdraw) — leaves the pin resolving. Verified against PostgreSQL 17.11 by apps/web/src/content/content-decks.test.ts: a deck published to version 2 and pinned there still serves version 2 to the pinned reader after the deck is withdrawn to version 1. Mutation-proved by the same inversion that proves DECK-002/T2 — routing a pinned reader through the current pointer would serve version 1 after the withdrawal, reddening this test.

## DECK-003 · Deck reading
PRD: `DECK-003`

- [ ] DECK-003/T1 — One column with sections in order, the version and date on the page
- [ ] DECK-003/T2 — A contents list that marks the current section by reading position, not by a threshold
- [ ] DECK-003/T3 — A next-section control that moves the reading position and nothing else
- [ ] DECK-003/T4 — The change notice for an unpinned reader whose version moved
- [ ] DECK-003/T5 — The print stylesheet shared with `RPT-003` where the rules are the same

## DECK-004 · Deck access grants
PRD: `DECK-004`

- [x] DECK-004/T1 — Grant and revoke, audited, with the pinned version optional
  Evidence: apps/web/src/content/grants.ts:addGrant and apps/web/src/content/grants.ts:removeGrant each record an audit row in the same transaction as the write (SEC-R04), the pinned version optional (DECK-002/T2). Proven against PostgreSQL 17 by apps/web/src/content/content-grants.test.ts — one audit row per act, an idempotent re-grant records nothing, and a revoke of a grant that does not exist records nothing. Mutation-proved: recording the wrong audit action on a grant is killed.
- [ ] DECK-004/T2 — The confirmation states in words what the person will be able to read, including the version
- [x] DECK-004/T3 — A grant to a suspended account is refused with the reason; to an invited one it is allowed
  Evidence: apps/web/src/content/grants.ts:addGrant locks the account row and refuses a grant to a suspended account with the reason, while an invited or active account is allowed. Proven against PostgreSQL 17 by apps/web/src/content/content-grants.test.ts — the suspended grant throws and writes neither the grant nor an audit row, and the invited and active grants succeed. Mutation-proved: inverting the suspended check is killed.
- [ ] DECK-004/T4 — The from-the-deck view, showing pin, granter, date and when last opened
- [ ] DECK-004/T5 — The from-the-account view, listing every deck a person may read
- [ ] DECK-004/T6 — Bulk grant with the names shown before it commits, and no bulk revoke

## POST-001 · Update authoring
PRD: `POST-001`

- [ ] POST-001/T1 — A composer that opens with the cursor in the body and fits without scrolling
- [ ] POST-001/T2 — Kind is required and chosen before writing; three kinds, no more
- [ ] POST-001/T3 — An optional product tag from the six, plus the company
- [ ] POST-001/T4 — The title derives from the first line until it is edited separately
- [ ] POST-001/T5 — A soft length marker that offers to move the text into the current draft report
- [ ] POST-001/T6 — The tagged product's current progress value is shown beside the composer

## POST-002 · Update publishing and audience
PRD: `POST-002`

- [x] POST-002/T1 — Audience on the item, with the predicate from `CMS-006` on every read
  Evidence: the audience is a column on content_items (apps/web/migrations/1788749077541_content.sql — public, investor or granted) that apps/web/src/content/audience.ts:changeAudience sets under audit (CMS-006/T6), and every read composes the one predicate at apps/web/src/content/access.ts:visibleTo — this task is that property for the update stream, met by apps/web/src/content/stream.ts:updateStream whose first clause is visibleTo(reader); no second audience rule is written for updates (CMS-R03). Closed by cross-reference to the audience mechanism and the stream read that composes it, since a separate implementation would be the redundancy §1.10 forbids — the isolation is proven where the stream read is, in apps/web/src/content/content-stream.test.ts.
- [x] POST-002/T2 — The stream pages by keyset on `(published_at, id)`, never by offset
  Evidence: apps/web/src/content/stream.ts:updateStream pages the feed by keyset on (published_at, id), never by offset: it fetches STREAM_PAGE_SIZE + 1 rows past a row-value cursor (a parameterised `(content_revisions.published_at, content_items.id) < (cursorTime, cursorId)`), returns the page and the next cursor, and the extra row is how a next page is known without a second count query. Keyset rather than offset because an update published while a reader is between pages shifts every later offset by one and silently drops the entry beneath the cursor — in a reverse-chronological feed, the most recent thing they had not read. Verified against PostgreSQL 17.11 by apps/web/src/content/content-stream.test.ts: a full first page carries a cursor and the remainder none, the two pages neither repeat nor skip and stay ordered, and an update published after the first page was read does not shift the second. Mutation-proved and restored byte-identical: reversing the cursor comparison reddens the paging tests.
- [x] POST-002/T3 — Ordered by publication rather than creation
  Evidence: apps/web/src/content/stream.ts:updateStream orders by content_revisions.published_at descending, the id breaking a tie, so an update drafted in one month and published in another appears where it was published rather than where it was written. Verified against PostgreSQL 17.11 by apps/web/src/content/content-stream.test.ts: an update created before another but published after it leads the feed, which ordering by creation would reverse. Mutation-proved and restored byte-identical: ordering by content_items.created_at instead reddens that test.
- [x] POST-002/T4 — The gateway's public news is the same query with an anonymous reader
  Evidence: apps/web/src/content/stream.ts:updateStream takes the reader as an argument, and the gateway's public news is the same call with a null reader — apps/web/src/content/access.ts:visibleTo reduces to public-and-published for an anonymous reader inside the one predicate, so there is no second, simpler public query to forget the published check on (CMS-R03, SEC-R01). Verified against PostgreSQL 17.11 by apps/web/src/content/content-stream.test.ts: a visitor's pages carry every public update and never the investor or the granted one, while an investor sees the investor update and a grantee also the granted one. Mutation-proved and restored byte-identical: dropping the visibleTo clause reddens the visitor-isolation test — a visitor then sees investor-only updates, an investor-only note shown to the market.
- [ ] POST-002/T5 — Narrowing states what it cannot recall; widening states what becomes public
- [ ] POST-002/T6 — Unread marking from the per-account read state, and a paging control rather than infinite scroll

## MAIL-001 · Investor mail
PRD: `MAIL-001`

- [x] MAIL-001/T1 — The `Mailer` port, and the composer that renders the exact bytes the send will use
  Evidence: apps/web/src/mail/mailer.ts declares the Mailer port — one method send(to, subject, text, html) returning a Receipt that carries only the SMTP queue id — behind which the single SMTP adapter will live (MAIL-DEC-01, MAIL-001/T8), and apps/web/src/mail/mailer.ts:compose renders a subject and a plain-text body into the ComposedMessage a send transmits and a preview shows, so the two are the same bytes: the text half is the body verbatim (what a plain-text client shows) and the HTML half is that same body split into paragraphs on blank lines, each escaped so its content cannot be read as markup and a single newline becoming a line break, inventing no structure the author did not write. There is no templating in the port — the message is data before it reaches the wire. Verified by apps/web/src/mail/mailer.test.ts (7 tests, pure): the subject passes through, the text is the body verbatim, the markup characters are escaped so a script tag cannot survive, paragraphs split on blank lines, a single newline becomes a break, and an empty body renders no HTML. Mutation-proved and restored byte-identical: dropping the `<` escape lets a script tag through, blanking the text half, removing the line break, and admitting empty paragraphs each redden a named test. The SMTP adapter (MAIL-001/T8, credential-blocked on SMTP_URL) and the send pipeline (MAIL-001/T2 through T6) build on this port; the composer is the caller-independent core they share, which config.mail already reports available or not (CRED-001).
- [x] MAIL-001/T2 — Recipients are a confirmed list of names, never a criterion re-evaluated at send time
  Evidence: apps/web/src/mail/recipients.ts:resolveRecipients turns a chosen set of account ids into a confirmed list of names — the list the admin confirms and the send then uses, not a criterion re-evaluated at send time. Proven against PostgreSQL 17 by apps/web/src/mail/recipients.test.ts — selected active accounts resolved and ordered by name, only the selected ids resolved, and an empty selection yielding nothing. Build-ahead of the send route (MAIL-001/T4).
- [x] MAIL-001/T3 — Suspended and unsubscribed accounts are excluded and shown as excluded, with the reason
  Evidence: apps/web/src/mail/recipients.ts:resolveRecipients excludes a suspended account and an unsubscribed one, each returned with its reason (suspended winning when an account is both), reading the unsubscribe list (MAIL-002). Proven against PostgreSQL 17 by apps/web/src/mail/recipients.test.ts — suspended and unsubscribed each excluded with the reason, and suspended shown when an account is both. Mutation-proved: inverting the suspended exclusion and inverting the unsubscribed exclusion are each killed.
- [ ] MAIL-001/T4 — The send requires the recipient count to be typed, and re-resolves every recipient first
- [x] MAIL-001/T5 — One row per recipient written before the attempt; a failure leaves the row and the send continues
  Evidence: apps/web/src/mail/send.ts:send writes one `mail_log` row per recipient (`queued`) with the `mail.send` audit in one transaction before any message leaves, then hands each to the port one at a time and moves its row to `accepted` with the queue id or `failed` with the reply — a refusal stops the one recipient, not the send, and only the port call sits in the try, so a database failure never records an accepted message as failed. The stored reply first runs through apps/web/src/ops/scrub.ts:scrub (an address and a long credential token masked) and is capped, so no personal datum reaches the two-year column (`DATA-R02`). Proven against PostgreSQL 17.11 by apps/web/src/mail/send.test.ts — 10 tests: every row queued before the first hand-off, a refused recipient left failed while the rest send, the audit carrying no address, one-at-a-time hand-off, an empty list writing nothing, and a refusal naming an address and a credential stored with neither and capped. Mutation-proved, each KILLED: scrub turned no-op, the cap removed, the empty-reply name dropped, and a refusal made to end the send instead of failing one row. The audit ships SEC-DEC-01's safe default — the `before` and `after` columns null — and its resolution revises this task (listed on SEC-DEC-01 Revises).
- [!] MAIL-001/T6 — Retry sends only to the ones that failed
  Blocked by: pending-decision: MAIL-DEC-02 — a retry must never reach a recipient who already received the message, and `mail_log` never supersedes an earlier attempt row, so the mechanism that marks a failed row spent (a `retry_of` link, an idempotency key, or a heuristic) shapes MAIL-002's table and is the owner's to settle. The per-recipient terminal states MAIL-001/T5 writes are the record it will read; until it settles no automated retry ships and a failed recipient is re-reached only by a fresh, deliberately re-confirmed send.
- [ ] MAIL-001/T7 — With no credential the composer works, the list resolves, and the send control is disabled with the reason
- [ ] MAIL-001/T8 — One SMTP connection per send, TLS required, and a connection that cannot be secured fails rather than falling back

## MAIL-002 · Mail log and unsubscribe
PRD: `MAIL-002`, `DATA-R04`

- [ ] MAIL-002/T1 — Rows written before the attempt, keyed by account and never by address
- [ ] MAIL-002/T2 — An unsubscribe that works in one click without signing in, and a preference inside the room
- [ ] MAIL-002/T3 — Transactional mail is never suppressed, enforced by the `kind` set at send time
- [ ] MAIL-002/T4 — A manual `stop sending` control with its reason, and the send view naming the mailbox bounces arrive in
- [ ] MAIL-002/T5 — Two-year retention, and immediate removal with the account
- [ ] MAIL-002/T6 — The admin log, filtered by recipient and date, showing state and error

## CFG-001 · Runtime configuration
PRD: `CFG-001`

- [x] CFG-001/T1 — The table with a previous-value column, and defaults declared in code beside the keys
  Evidence: apps/web/src/config/settings.ts:SETTINGS is the registry — the five keys CFG-001 §3 names (room.banner, room.signin_message, mail.enabled, session.max_age_days, signin.rate_per_hour), each with its type and its default, declared in code beside the key rather than as seed rows. `as const satisfies` makes each default a literal, so a key reads back as its own type. The table — key, value, previous_value, changed_by, changed_at — already exists in apps/web/migrations/1788752441424_platform.sql, so this task is the defaults-in-code half: a database with no config rows produces a working application. Verified by apps/web/src/config/settings.test.ts — every key declares one of text, int or bool and a default — and by the accessor reading those defaults against a real PostgreSQL.
- [x] CFG-001/T2 — Change validates against the key's type and bounds, refusing rather than clamping
  Evidence: apps/web/src/config/settings.ts:validateSetting checks a raw value against the key's declared type and bounds and refuses with the range rather than clamping — `session.max_age_days` an integer 1-90, `signin.rate_per_hour` 1-1000, the two text lines at most 280 characters, `mail.enabled` the two words it is stored as — returning the normalised string a valid value stores as (an integer without the caller's formatting). A clamp was rejected because a value silently pinned to its bound disagrees with what the caller typed and what the screen then shows. The bounds live in the `SETTINGS` registry beside each key. Verified by apps/web/src/config/settings.test.ts ("validation against type and bounds", pure) and, against PostgreSQL 17.11, by apps/web/src/config/settings.ts:changeSetting refusing an out-of-range value and writing nothing; mutation-proved — dropping the max bound reddens both the range refusal and the write-nothing test.
- [x] CFG-001/T3 — Change and revert are one transaction each, audited with both values
  Evidence: apps/web/src/config/settings.ts:changeSetting validates, then in one transaction captures the current value — or the declared default when no row exists yet, so reverting the first change restores the default rather than a null — as `previous_value`, writes the new value and `changed_by`, and records `config.change` (apps/web/src/audit/record.ts:recordAudit, SEC-R04). Both values, the key, who and when live in the `config` row, which is what a revert and the console read; the audit's `before`/`after` are null under the SEC-DEC-01 safe default until SEC-002/T4, and the key is a string the `uuid` `subject_id` cannot hold, so the audit row attests the act while the config row carries the detail. recordAudit takes a `Transaction`, so the audit cannot fall outside the write. Verified against PostgreSQL 17.11 by apps/web/src/config/settings.test.ts: the value written, the declared default then the prior value captured as `previous_value`, one `config.change` per change, and an unparseable actor rolling the row and its audit back together. Mutation-proved — writing the new value as `previous_value` reddens the capture and revert tests; removing the audit reddens the audit assertion.
- [x] CFG-001/T4 — Revert is one action with no confirmation, and is itself recorded
  Evidence: apps/web/src/config/settings.ts:revertSetting reverts in one action — under a row lock it swaps `value` and `previous_value` and records `config.change` (a revert is a change back, so there is no separate action and the trail shows both moves), returning `false` and writing nothing for a key with no previous value. Reverting twice returns to the start, which is the one step of undo the design keeps rather than a stack. Verified against PostgreSQL 17.11 by apps/web/src/config/settings.test.ts: the swap, reverting twice returning to the start, reverting a first change back to the default, and the no-op for a key that has never changed. Mutation-proved — a revert that does not swap `previous_value` reddens the revert-twice test.
- [x] CFG-001/T5 — One cached accessor with a short refresh; an empty table yields a working application
  Evidence: apps/web/src/config/settings.ts:Settings reads a setting through a cache refreshed at most every five seconds, so a change takes effect within seconds without a database read on every request. apps/web/src/config/settings.ts:Settings returns a key's declared default when no row holds it, so an empty table is a working application; a stored value is parsed into the key's type (text, int or bool), and a corrupt integer falls back to the default rather than handing a caller a NaN. The clock is injected so a test crosses the refresh boundary without waiting it. Verified by apps/web/src/config/settings.test.ts against a real PostgreSQL: defaults on an empty table, a stored value parsed to its type, a corrupt integer defaulting, and the cache holding within the window then reloading after it — the default branch, the refresh condition, and the boolean parse each mutation-proved.
- [x] CFG-001/T6 — The accessor refuses a secret-shaped key
  Evidence: apps/web/src/config/settings.ts:Settings refuses a secret-shaped key before any read; apps/web/src/config/settings.ts:isSecretShaped matches the names a credential takes — secret, password, passphrase, token, credential, private, an api-key, a trailing key — so the config table cannot become a place a secret is kept even if one were added to the registry by mistake (SEC-R05). Credentials come from the environment instead (CRED-001). Verified by apps/web/src/config/settings.test.ts: the guard rejects a secret-shaped key with no database touched, the pattern recognises several credential names, and no registry key is secret-shaped — the guard and the pattern each mutation-proved.

## SEC-001 · Security baseline
PRD: `SEC-001`

- [x] SEC-001/T1 — One middleware sets every header on every response, with no route exempt
  Evidence: apps/web/src/proxy.ts sets five security headers on every response, from the one middleware that already carried AUTH-004's `no-store` — four whose value never varies, held in the `SECURITY_HEADERS` table (`Strict-Transport-Security` two years with `includeSubDomains` and `preload`; `X-Content-Type-Options` `nosniff`; `Referrer-Policy` `strict-origin-when-cross-origin`; `Permissions-Policy` denying camera, microphone, geolocation and both the live and the retired interest-targeting capabilities), and a per-request `Content-Security-Policy` from apps/web/src/proxy.ts:contentSecurityPolicy (SEC-001/T2). The `no-store` block runs unchanged and still conditional on the session cookie. "Every response" is every document: the matcher exempts only the build's content-addressed immutable assets, which carry no session, are not documents a policy governs, and would lose their caching if marked unstorable — named in docs/designs/sec/sec-001-security-baseline.md §3. Verified against the built server: all five headers answer on a 200 start page, a 404, the admin gate's 307 and an API 405, while the asset paths stay `public, immutable` with none of the five. apps/web/src/proxy.test.ts (35 tests) asserts each header with and without a session and across five paths; its mutation run kills the removal of each header and both directions of the `no-store` conditional.
- [x] SEC-001/T2 — The CSP carries no `unsafe-inline`, and the page is verified in a browser under it
  Evidence: apps/web/src/proxy.ts:contentSecurityPolicy builds `default-src 'self'; img-src 'self' data:; style-src 'self'; script-src 'self' 'nonce-<per request>'; frame-ancestors 'none'; base-uri 'none'; form-action 'self'` — no `unsafe-inline`, no `unsafe-eval`. Refusing inline script costs one nonce, minted per request as 16 bytes from the system CSPRNG and set on the request as well as the response so the render stamps it onto Next's own inline tags, which is why every route renders on demand (apps/web/src/app/layout.tsx, docs/decisions-log.md#SEC-DEC-02). Verified in a browser (Playwright driving Chrome) against the built server: the start page renders and hydrates with zero console errors, both inline flight scripts carrying the nonce; the styled 404 apps/web/src/app/not-found.tsx — an external CSS module the policy admits — replaces the framework's inline-styled default that a policy without `unsafe-inline` would leave unstyled; and a forced root-layout crash renders apps/web/src/app/global-error.tsx dynamically, nonced and fully styled (measured: flex layout, background and font applied, one stylesheet loaded, zero CSP violations). The one surface the policy degrades is Next's internal global-error prerender, which the framework builds static with its own inline-styled body and no nonce; it is reached only by a direct request to that internal route or by the near-impossible crash of the trivial root layout, and is recorded with its caching consequence in docs/decisions-log.md#SEC-DEC-02 and docs/designs/sec/sec-001-security-baseline.md §6.
- [x] SEC-001/T3 — Every route parses its input with a schema, and the handler sees only the parsed value
  Evidence: the route that takes a body parses it through a schema before the handler sees a value, and the handler sees only the parsed result. apps/web/src/app/api/auth/sign-in/route.ts:readCredentials returns a typed `Credentials` or `null`, rejecting a body that is not an object, an `email` or `password` that is not a string, and an address that is empty or past RFC 5321's length, so the handler branches on `Credentials | null` and the unparsed body is out of scope. The other two routes take no body — the sign-out and session-invalidate-all handlers act on the session cookie, which the gate parses (AUTH-002) — so there is no unparsed input for a handler to read. A body that fails the schema is answered `400 invalid_request`, a shape error distinct from the `401 invalid` a wrong credential gets, so a malformed body and a wrong password do not look alike. Verified against PostgreSQL 17.11 by apps/web/src/app/api/auth/sign-in/route.test.ts, which drives a non-object body, a non-string password and an over-length address to the `400` and a well-formed body through. The property is per route, and every route that exists meets it; a future body-taking route carries its own parser under the same rule.
- [ ] SEC-001/T4 — Rate limits on sign-in, reset and invitation, per account and per address, refusing identically
- [x] SEC-001/T5 — `gitleaks`, dependency audit and type check in CI, with every action pinned to a verified SHA
  Evidence: .github/workflows/ci.yml runs the three checks the design's §3 CI table names, each blocking. gitleaks scans the full history on every push and pull request (the `secrets` job, `fetch-depth: 0`), as the release binary pinned to v8.30.0 rather than the published action, which refuses to run for an organisation without a paid licence and would stop the day this repository goes private (docs/decisions-log.md#INFRA-DEC-04). `tsc --noEmit` and the suite run in the `app` job against a PostgreSQL service, and `npm audit --audit-level=high` runs there too, blocking a merge on a critical or high advisory — measured clean, 0 vulnerabilities, at close. Every action is pinned to a commit SHA with its version in a comment, and each SHA was verified against the upstream release rather than trusted from the file: the setup-node and setup-python pins matched their v4 and v5 tags, while the checkout action was found pinned to a v6-development commit (dated 2025-11-13, mislabelled `# v4`) — a real commit, which is why no gate had caught it — and re-pinned to the verified v4.4.0 release, the SHA `.github/workflows/ci.yml` now carries with the version in its comment. The dependency-update automation stays off (`.github/dependabot.yml.disabled`) on the shared-runner quota argument; it is the update tool, separate from the audit check this task names.

## SEC-002 · Audit log
PRD: `SEC-002`, `SEC-R04`

- [x] SEC-002/T1 — The table, the closed action vocabulary, and the append-only trigger
  Evidence: The table, its fifteen-value action CHECK, and both append-only triggers ship in apps/web/migrations/1788752441424_platform.sql — `audit` with `id` GENERATED ALWAYS and `at` DEFAULT now(), `raise_append_only()` behind `audit_is_append_only` (BEFORE UPDATE OR DELETE) and `audit_no_truncate` (BEFORE TRUNCATE), and `audit_force_now()` behind `audit_at_is_db_time` (BEFORE INSERT). apps/web/src/db/types.ts:AuditTable and AUDIT_ACTIONS mirror it, and apps/web/src/db/db.test.ts asserts the interface matches the migration. The enforcement is proven behaviorally against PostgreSQL 17.11 by apps/web/src/audit/audit.test.ts: an UPDATE, a DELETE and a TRUNCATE on a written row are each refused with an append-only exception, an action the vocabulary does not name is refused by the database, and a raw insert carrying a backdated `at` is overwritten with the database clock.
- [!] SEC-002/T2 — The application role holds no UPDATE or DELETE on it
  Blocked by: pending-external: the REVOKE runs where the database is deployed, against the role in DATABASE_URL, and is documented at docs/operator-checklist.md#AUDIT-GRANT. The migration deliberately does not make a grant specific to a deploy's role; the append-only trigger enforces the same property in the database meanwhile, so the two mechanisms fail independently. Unblocks when `select has_table_privilege(<role>, 'audit', 'UPDATE')` and the same for DELETE both return false at the deploy — the operator checklist's own done-when.
- [x] SEC-002/T3 — One insert function, called inside the caller's transaction, with no error discarded
  Evidence: apps/web/src/audit/record.ts:recordAudit inserts one `audit` row and takes a `Transaction<Database>`, never the pool — a `Transaction` is not assignable from a `Kysely`, so the only way to call it is from inside `db.transaction().execute(...)`, and SEC-R04's same-transaction requirement is enforced by the compiler rather than by the care of the caller. It does not catch the insert error, so a refusal by the database propagates and rolls the caller's write back with it. `at` and `id` are the database's; `before` and `after` are written null, since which fields each action may record is SEC-DEC-01 and unsettled (T4). Proven against PostgreSQL 17.11 by apps/web/src/audit/audit.test.ts: a row commits with its transaction, a rolled-back transaction leaves none, and a refused second insert rolls the valid first one back — the last two mutation-proved, a swallowed error and a pool write each reddening a test.
- [!] SEC-002/T4 — Only changed fields are recorded, and no personal data reaches the trail
  Blocked by: pending-decision: SEC-DEC-01 — whether a changed field records its name or an allow-listed value is the trail's personal-data policy, and it is the user's to settle (special-category data).
- [x] SEC-002/T5 — The admin view: newest first, filterable by actor, subject and action, with no edit or delete control
  Evidence: apps/web/src/audit/read.ts:recentAudit reads the trail newest first — the identity id descending, which is the order things happened without trusting a clock — narrowed by any of actor, subject and action, none required, and bounded to a recent window. apps/web/src/app/admin/audit/page.tsx renders it under the admin segment's inherited role check (ADMIN-002), as a read-only table with a filter form and no edit or delete control, because the trail is append-only in the database (SEC-002/T1) and a control that does not exist cannot be reached by a bug. Proven against PostgreSQL 17.11 by apps/web/src/audit/audit.test.ts: recentAudit returns rows newest first and narrows by actor, action and subject, mutation-proved by reversing the order to ascending; and measured against the built server (next build then next start) — an admin GET answered 200 and listed the trail, an investor GET answered 404 through the inherited gate, and filtering to one action dropped the other action's row from the page.

## OPS-001 · Hosting and deploy
PRD: `OPS-001`

- [ ] OPS-001/T1 — Terraform under `deploy/`: VPC, ECS Fargate, ALB, RDS in private subnets, ECR, Route 53, ACM, with remote state and a lock table
- [ ] OPS-001/T2 — The deploy sequence: migrate as a one-off task on the same image, then the new task set, health-checked before it takes traffic
- [ ] OPS-001/T3 — A short DNS TTL set a day before the cutover, and `main` left deployable for a month after
- [ ] OPS-001/T4 — Secrets from Secrets Manager by ARN; values set by the owner, never in Terraform state or the image
- [ ] OPS-001/T5 — The six post-deploy checks, run against the real deployment through Cloudflare
- [ ] OPS-001/T6 — A staging service carrying `APP_ENV=staging`, so the console says which one it is
- [ ] OPS-001/T7 — RDS unreachable from outside the VPC, proved by attempting it rather than by reading the security group

## OPS-002 · Logging and monitoring
PRD: `OPS-002`, `DATA-R02`

- [x] OPS-002/T1 — One JSON logger to stdout, with a closed event vocabulary and no `console.log` anywhere
  Evidence: apps/web/src/ops/logger.ts:log writes one JSON line per call to stdout — `{ ts, level, event, request_id, msg, ...fields }` — with `ts` in UTC (ISO 8601, `Z`), `level` one of error, warn, info or debug (debug suppressed when app.env is production), and `event` constrained by the compiler to the closed apps/web/src/ops/logger.ts:LOG_EVENTS vocabulary so a line is counted rather than matched by prose. The vocabulary's first member is `db.pool_error`, emitted by apps/web/src/db/index.ts:handlePoolError — the pool `error` listener that writes a structured line and exits, clearing the Deferred marker apps/web/src/db/index.ts carried for this task. `request_id` is null pending OPS-002/T2 (the edge mints none yet), a documented slot rather than an absent field. No `console.*` call exists in any non-test source file, checked by a scan in apps/web/src/ops/logger.test.ts that strips comments before matching a call — itself proven to catch a real `console.log` by injecting one. Verified by apps/web/src/ops/logger.test.ts (node, no database): the line shape and UTC stamp, the three non-debug levels, debug off in production and on in development (the config mocked so one test sees production without the frozen cache leaking), and the handler logging `db.pool_error` and exiting with 1 — the debug-in-production guard and the handler's exit each mutation-proved.
- [x] OPS-002/T2 — A request id generated at the edge and carried through every line of that request
  Evidence: apps/web/src/proxy.ts mints a request id per request (apps/web/src/ops/request-context.ts:newRequestId, a uuid) and sets it on both the request it hands the render and the response — the edge is the one place every request passes, so the id is generated once, there. apps/web/src/ops/request-context.ts carries it to the logger through an AsyncLocalStorage rather than a threaded parameter: apps/web/src/ops/request-context.ts:withRequestId wraps a route handler and opens the context from the request's x-request-id header (a request reaching a handler without the edge gets a fresh id, never none), and apps/web/src/ops/request-context.ts:currentRequestId reads it, so every line a request logs carries its id while a line logged outside any request — a connection-pool error from an idle client — carries null, the honest value. The three api routes that take a request — sign-in, sign-out, and the end-all-sessions route — are wrapped; apps/web/src/ops/logger.ts reads currentRequestId in place of the null it returned while this was deferred (OPS-R01). Verified by apps/web/src/ops/request-context.test.ts (the id is read inside its context and nowhere outside, survives an await, and two contexts in flight never cross), apps/web/src/ops/logger.test.ts (a line logged in a context carries that id, one logged outside carries null), and apps/web/src/proxy.test.ts (a fresh uuid on the response and on the request the render receives, distinct per response, the security baseline untouched). Mutation-proved, each reddening a named test: currentRequestId forced to null, withRequestId ignoring the header, newRequestId made constant, the logger ignoring the context, and the proxy omitting either the request or the response header.
- [x] OPS-002/T3 — Explicit fields, never serialised objects, plus a scrubber on the way out
  Evidence: apps/web/src/ops/logger.ts keeps DATA-R02 in two halves. The structural half is the type of `fields` (apps/web/src/ops/logger.ts:LogFields) — primitives only, so a whole object handed to the logger does not compile, which is how personal data reaches a log almost every time. The backstop is apps/web/src/ops/logger.ts:scrub, run on every string field value and on the message on the way out: it masks an address and any run of forty or more base64url characters (a 32-byte token is forty-three, a SHA-256 hash sixty-four), while a thirty-six-character UUID account id stays readable, because an id ties a line to an act and is not the secret a token is. Verified by apps/web/src/ops/logger.test.ts: an address in a field and in the message is redacted, a token and a hash are redacted while an account id is left intact, and a line with no sensitive shape is untouched — mutation-proved by dropping the address replace, dropping the token replace, widening the threshold until it catches a uuid, and stopping the message scrub, each reddening a test.
- [x] OPS-002/T4 — A scrubber hit raises an alert naming the event, so the caller is fixed rather than the scrubber
  Evidence: apps/web/src/ops/logger.ts:emit raises an alert whenever the scrubber masks anything: apps/web/src/ops/logger.ts:scrubFields reports which field values it changed, the message is checked too, and on any hit a second line is written at error level with event `log.scrubbed`, naming the source event and the masked field names — never a value, so the alert carries nothing to mask itself. It points a reader at the caller that tried to log the value, not at the scrubber. A `log.scrubbed` line never raises its own alert (the `SCRUBBED_EVENT` guard), so a hit costs exactly one extra line rather than a cascade. Verified by apps/web/src/ops/logger.test.ts: a masked field raises one alert naming the source event and the field, a masked message names `msg`, a clean line raises none, and a leak is exactly two lines — mutation-proved by removing the alert, by stopping the field and the message detection, and by mis-naming the source event, each reddening a test.
- [x] OPS-002/T5 — `/health` runs a real query and reports the build version, and nothing else
  Evidence: apps/web/src/app/health/route.ts answers a health check with `{ ok, version, db }` and nothing else. `db` is a real `select 1` through apps/web/src/db/index.ts:getDb, not a pool status — a pool holding a connection to a database that has stopped answering reports healthy, which is the failure this catches — and a failed query answers `503`, so a monitor reading only the status line still sees it. `version` is apps/web/src/config/index.ts:getConfig's `build.version`, read from the optional BUILD_VERSION the deploy stamps (default `unknown`), so a report about behaviour ties to what was running; BUILD_VERSION is declared in apps/web/src/config/index.ts:DECLARED_VARIABLES and env.example, held equal by the INFRA-001/T2 test. The response is never cached — `force-dynamic` and `Cache-Control: no-store`. Verified by apps/web/src/app/health/route.test.ts against a real PostgreSQL (200, no-store, db true, the build version) and with the database not answering (503, db false) — mutation-proved by forcing the status to 200, skipping the query so db never turns true, mis-reading the version, and dropping no-store. apps/web/src/config/index.test.ts pins build.version reading BUILD_VERSION and defaulting to unknown.
- [x] OPS-002/T6 — Three alerts, each with its action written beside it
  Evidence: docs/runbooks/ops-002-alerts.md defines the three alerts the design's §3 table names, each with the operator's action beside it: the site is down (the health endpoint fails twice, sixty seconds apart — the static gateway on main is the one-revert fallback, SITE-005/T6), errors are up (more than ten error lines in five minutes — read them by the request_id each now carries, OPS-002/T2), and something logged personal data (a scrubber hit — fix the caller the log.scrubbed line names, never the scrubber). One of the three, the scrubber hit, fires today as the error line the logger raises on any mask (OPS-002/T4); the other two are conditions a watcher evaluates, and the runbook states plainly that until OPS-001 adds a metrics backend nothing polls the health endpoint or counts error lines, and that mail delivery waits on the send path (MAIL-001) with the service already chosen (MAIL-DEC-01) — the gap recorded rather than papered over (design §6). The runbook is the definition and the action; OPS-001 wires the watching and the delivery from it.

## INFRA-001 · Local development stack
PRD: `INFRA-001`

- [x] INFRA-001/T1 — PostgreSQL 17 on 5434 under docker-compose, with a named volume and a health check
  Evidence: docker-compose.yml — `make infra-up` brought up `postgres:17-alpine` (PostgreSQL 17.11), the `pg_isready` health check reached `healthy` at t+16s, the host port mapped 5434→5432, and the named volume `valotech-postgres` was created; `make infra-reset` tore it all down with no residue. Verified by running, not asserting. The task's text is the Postgres side alone; bringing the application up against it is `INFRA-001/T3`.
- [x] INFRA-001/T2 — `env.example` names every variable the application reads, with what its absence means
  Evidence: env.example declares all twelve variables the application reads, each with whether it is required and what its absence does; apps/web/src/config/index.ts exposes the reader's own list as `DECLARED_VARIABLES`, and apps/web/src/config/index.test.ts asserts that list equals the set env.example declares, so the file and the reader cannot drift.
- [x] INFRA-001/T3 — Make targets for up, down, reset, and the three migration commands
  Evidence: `make migrate`, `make migrate-down`, `make migrate-roundtrip`, `make infra-up`, `make infra-down`, `make infra-reset` — the connecting targets print the target before they act; verified end to end.
- [x] INFRA-001/T4 — `make migrate-roundtrip` applies, rolls back and re-applies against a throwaway database
  Evidence: apps/web/scripts/migrate-roundtrip.mjs — `make migrate-roundtrip` creates a scratch database beside the target, runs up then down then up on it, and drops it; verified the developer's own database is left untouched (DATA-R06).
- [x] INFRA-001/T5 — A first-run path that works from a fresh clone with no prior state
  Evidence: scripts/setup.sh (run by `make setup`) takes a checkout with no `.env`, no node_modules and no database to an up-and-migrated stack: it creates `.env` from env.example without clobbering one, installs apps/web dependencies, starts PostgreSQL, waits for it to report healthy, and applies every migration — each step idempotent. Verified end to end against a fresh state (twelve tables applied) and re-run clean (`No migrations to run!`); it names `SESSION_SECRET` as the owner's to set (CRED-001) rather than inventing one.

## CRED-001 · Credential handling
PRD: `CRED-001`, `SEC-R05`

- [x] CRED-001/T1 — One module reads the environment once, validates it, and exports a frozen object
  Evidence: apps/web/src/config/index.ts — the sole reader of `process.env`; `loadConfig` reads every variable `env.example` declares, validates the set, and returns a deep-frozen typed `Config`, and `getConfig` reads once and caches. Verified by apps/web/src/config/index.test.ts (a valid environment yields a frozen object whose mutation throws).
- [x] CRED-001/T2 — A required variable that is absent stops the application before it listens, naming the variable
  Evidence: apps/web/src/config/index.ts — `loadConfig` collects every problem and throws `ConfigError` naming each absent or unparseable required variable, not the first only; apps/web/src/instrumentation.ts calls `getConfig` in Next's `register`, so a misconfiguration aborts startup before the server listens. Verified by apps/web/src/config/index.test.ts (`names every missing required variable`; `refuses to load when X alone is missing`).
- [x] CRED-001/T3 — An absent optional credential disables its feature with a stated reason, and the system stays up
  Evidence: apps/web/src/config/index.ts — an absent `SMTP_URL` or `BACKUP_TARGET` yields `{ available: false, unavailable: <reason> }` and `loadConfig` still returns, so the feature is disabled and the system is not. Verified by apps/web/src/config/index.test.ts (mail and backups each disable with a reason while the config loads).
- [x] CRED-001/T4 — A credential never reaches a log, an error message or a response, including through generic serialisation
  Evidence: apps/web/src/config/index.ts — every secret is a `Secret` whose `toString`, `toJSON` and `util.inspect` hook redact, so a value survives explicit `.value` access and nothing else. Verified by apps/web/src/config/index.test.ts: `JSON.stringify` and `util.inspect` of the whole config, a lone secret, a spread sub-object, and an `Error` built from the config all omit the secret bytes while `.value` returns them.
- [x] CRED-001/T5 — `credentials/README.md` says what the owner sets, and the local input form writes `.env` without the value crossing a chat
  Evidence: credentials/README.md states the rule and the three secrets the owner sets; credentials/credential-input.html generates `SESSION_SECRET` in the browser with `crypto.getRandomValues`, takes the two it cannot generate, and emits a `.env` block with no network call — the value never crosses a chat window.

## LEGAL-SG-001 · PDPA posture
PRD: `LEGAL-SG-001`

- [ ] LEGAL-SG-001/T1 — The privacy notice: what is held, why, how long, who to write to, in twenty locales
- [ ] LEGAL-SG-001/T2 — The notice is linked from the sign-in page and the room's footer
- [ ] LEGAL-SG-001/T3 — The four rights answered within thirty days, with the admin path for each written down
- [ ] LEGAL-SG-001/T4 — A named DPO recorded, and published in the notice
- [ ] LEGAL-SG-001/T5 — A breach runbook with the assessment steps and both notification paths
- [ ] LEGAL-SG-001/T6 — The backup window disclosed in the notice rather than omitted

## LEGAL-GLOBAL-001 · GDPR posture for EU investors
PRD: `LEGAL-GLOBAL-001`

- [ ] LEGAL-GLOBAL-001/T1 — The notice carries the additional GDPR statements, in twenty locales
- [x] LEGAL-GLOBAL-001/T2 — An admin-generated JSON export of everything held about one person
  Evidence: apps/web/src/admin/accounts.ts:exportPersonData gathers the five things the system holds about a person — the account's own fields (never the password hash), the decks granted, which deck versions and reports were opened and when, and the mail subjects and dates — each scoped to the one account (DATA-R05). The grants come from apps/web/src/content/grants.ts:grantsForAccount, which reads the grant table behind the audience predicate's boundary; the rest are this person's own rows. It is an admin action producing a file, with no self-service route and no link mailed (DATA-R01, DATA-R02). Proven against PostgreSQL 17 by apps/web/src/admin/accounts.test.ts and apps/web/src/content/content-grants.test.ts — the fields, grants, reads and mail gathered with no password hash, scoped to one account and not another, and null for an id no account holds. Mutation-proved: grantsForAccount dropping the pin, and the export widening its account scope, are each killed.
- [x] LEGAL-GLOBAL-001/T3 — An objection flag that stops read-tracking and deletes the existing rows
  Evidence: accounts gains a read_tracking_objected flag (folded, DATA-R07); apps/web/src/admin/accounts.ts:objectToReadTracking sets it, deletes the account's deck read and report read rows, and audits the act in one transaction (SEC-R04, the objection's action folded into the audit vocabulary). apps/web/src/content/decks.ts:recordDeckRead and apps/web/src/content/reports.ts:markReportRead check the flag and record nothing when it is set, so the objection stops future tracking while the room keeps working — unread marking degrades to everything looking new. Proven against PostgreSQL 17 by apps/web/src/admin/accounts.test.ts, apps/web/src/content/content-decks.test.ts and apps/web/src/content/content-reports.test.ts — the flag set and the existing reads deleted with the act audited, a second objection a no-op, and a new read recorded nothing once set. migrate-roundtrip green. Mutation-proved: not setting the flag, inverting the record guard, and deleting the wrong table are each killed.
- [ ] LEGAL-GLOBAL-001/T4 — The breach runbook uses the 72-hour clock for everyone
- [ ] LEGAL-GLOBAL-001/T5 — A one-page record of processing, and a written statement of what is deliberately not claimed

## LEGAL-GLOBAL-002 · Cookie and analytics posture
PRD: `LEGAL-GLOBAL-002`

- [ ] LEGAL-GLOBAL-002/T1 — The three categories, with `necessary` fixed on and both others off until a visitor says otherwise
- [ ] LEGAL-GLOBAL-002/T2 — The notice states the three storages, when each is set, and that nothing else is set on arrival
- [ ] LEGAL-GLOBAL-002/T3 — A test proves a visitor who answers nothing, signs in to nothing and chooses no language leaves with an empty cookie jar and empty storage
- [ ] LEGAL-GLOBAL-002/T4 — Nothing non-essential is present in the page until consent, rather than present and inert
- [ ] LEGAL-GLOBAL-002/T5 — The stored choice is versioned, and a bump re-asks rather than extending an old answer

## CMS-001 · Content model and revisions
Design: [docs/designs/cms/cms-001-content-model-and-revisions.md](designs/cms/cms-001-content-model-and-revisions.md) · PRD: `CMS-001`

- [x] CMS-001/T1 — Items, revisions, and a published pointer that is the only thing a reader query consults
  Evidence: apps/web/src/content/items.ts:createItem inserts a content_items row whose current_revision_id is null — the pointer is the published state, there is no is_published column to disagree with it, and a reader query consults only that pointer once the reads land (CMS-001/T6). A revision is immutable once published: apps/web/migrations/1788749077541_content.sql carries the trigger refusing an edit to a published content_revisions row (CMS-R01), so what an investor read survives byte for byte. Proven against PostgreSQL 17.11 by apps/web/src/content/content.test.ts: a created item has a null pointer and the investor-default audience, and the database refuses a report with no period and a kind on a non-update.
- [x] CMS-001/T2 — The closed block vocabulary and its validator, rejecting an unknown block on write
  Evidence: apps/web/src/content/blocks.ts:validateBlocks admits only the seven shapes in apps/web/src/content/blocks.ts:BLOCK_TYPES and throws apps/web/src/content/blocks.ts:BlockValidationError on anything else, so an unknown block is refused at the write rather than skipped at the read (CMS-R04); an image without non-empty alt text is refused too (A11Y-R02). Proven by apps/web/src/content/content.test.ts: a valid document of all seven types returns typed, and eleven malformed documents each throw. Mutation-proved: a validator accepting an unknown block as a divider reddens the test.
- [x] CMS-001/T3 — Marks as offsets over plain text, so a paragraph stays one translatable string
  Evidence: apps/web/src/content/blocks.ts:Mark is an offset span — start, end, a type from apps/web/src/content/blocks.ts:MARK_TYPES, and a target for a link — validated within the paragraph's text length and never embedded markup, so the text stays one string a translator, a search index and a renderer all read without a parser, and a paragraph is not an injection surface. Proven by apps/web/src/content/content.test.ts: a mark past the text, a start not before its end, an unknown mark type, a link with no target, and a non-link carrying one each throw.
- [x] CMS-001/T4 — `saveDraft` replaces the open draft rather than accumulating a revision per save
  Evidence: apps/web/src/content/items.ts:saveDraft validates the blocks, then in one transaction locks the item row and updates the open unpublished draft if one exists or inserts the first, so an item holds one open draft rather than one per save (CMS-001 §6); the lock makes that invariant hold under concurrency. Proven against PostgreSQL 17.11 by apps/web/src/content/content.test.ts: a second save reuses the first revision's id and leaves one row, the blocks round-trip through jsonb as the array they were, an invalid document writes nothing, and two saves racing on warmed connections leave one open draft — mutation-proved, the test reddens when the item-row lock is removed.
- [x] CMS-001/T5 — `publish` and `withdraw` as pointer moves, with the previous revision intact
  Evidence: apps/web/src/content/publish.ts:publish re-validates the revision's body, stamps its published_at, moves content_items.current_revision_id to it, and records content.publish — all in one transaction, so the audit row and the pointer move commit or roll back together (SEC-R04); apps/web/src/content/publish.ts:withdraw moves the pointer to the revision published before the current one, or to null, and records content.withdraw. The withdrawn revision keeps its published_at, so the previous version is intact and re-publishable. recordAudit (SEC-002/T3) takes the transaction, so the same-transaction rule is the type rather than a hope — this is its first caller. Proven against PostgreSQL 17.11 by apps/web/src/content/content.test.ts: publish moves the pointer and writes one content.publish row, an invalid revision is refused and records nothing as the transaction rolls back, and withdraw walks back to the previous published revision and then to null with both revisions surviving and two content.withdraw rows. Mutation-proved: skipping the re-validation, withdrawing always to null, and recording no audit each redden a test.
- [x] CMS-001/T6 — Every read function takes the reader; none exists that does not
  Evidence: apps/web/src/content/read.ts:forReader and apps/web/src/content/read.ts:forAuthor are the only reads the content module exposes, and each takes the reader as an argument (DATA-R05), so omitting the scope is a type error rather than a leak; both compose apps/web/src/content/access.ts:visibleTo rather than restating an audience rule of their own. That no other read exists is mechanical rather than agreed: scripts/check-content-access.py refuses any file outside apps/web/src/content that names content_items, and it runs as make check-content-access inside the make check aggregate (CMS-006/T5). Proven against PostgreSQL 17.11 by the twenty cases in apps/web/src/content/read.test.ts, which drive both functions for a visitor, two investors, an admin, and a role the vocabulary does not name.

## CMS-002 · Authoring surface
Design: [docs/designs/cms/cms-002-authoring-surface.md](designs/cms/cms-002-authoring-surface.md) · PRD: `CMS-002`

- [~] CMS-002/T1 — A block list the author operates by keyboard, with each block's type visible
  Note: Built. apps/web/src/app/admin/content/[id]/edit/editor.tsx renders the blocks as a list of focusable rows; each row shows its written type (apps/web/src/app/admin/content/[id]/edit/block-fields.tsx:TYPE_LABELS) and carries Move-up/Move-down/Delete buttons, and the row itself is the keyboard hub — ArrowUp/Down move between rows, Enter inserts a paragraph after, Backspace merges into the row above, while a keystroke inside a field keeps its native meaning. It type-checks and builds, and the built server serves the editor at /admin/content/<id>/edit under the admin gate (200) with every block's type label present. Pending: the live keyboard interaction is not yet driven in a browser — the shared Playwright MCP browser was held by another process this session — which is all that stands between this and closed.
- [~] CMS-002/T2 — The seven block types, each with the fields its schema requires
  Note: Built. apps/web/src/app/admin/content/[id]/edit/block-fields.tsx:BlockFields edits each of the seven types with the fields its schema demands (heading level+text; paragraph text; list ordered+items; quote text+attribution; image mediaId+alt+caption; figure mediaId+caption+data; divider), and apps/web/src/app/admin/content/[id]/edit/block-fields.tsx:defaultBlock seeds a schema-valid empty block for the "Add a block" menu. The built server HTML renders all seven menu entries, and a valid heading+paragraph+divider document saves over real HTTP. Pending: editing each type's fields is not yet driven in a browser (shared MCP browser held by another process), the one thing left before closing.
- [ ] CMS-002/T3 — An image block cannot be saved without alternative text
- [ ] CMS-002/T4 — Marks by selection, stored as offsets, with the editor's model the block array and not the DOM
- [ ] CMS-002/T5 — Paste imports plain text plus recognised structure and nothing else
- [~] CMS-002/T6 — Explicit save, a visible unsaved state, and a local copy offered back after a closed tab
  Note: Built. apps/web/src/app/admin/content/[id]/edit/editor.tsx saves explicitly on a control and on Ctrl/Cmd-S (POST to the route below), states the unsaved/saved state in a standing line rather than a toast, warns before unload while there are unsaved blocks, mirrors the blocks to localStorage under the item id, offers that copy back when it differs from the loaded draft, and discards it on a successful save. The save path is verified against PostgreSQL 17 (apps/web/src/app/admin/content/[id]/draft/route.test.ts) and over real HTTP against the built server (BUILD_VERSION cms002-verify): a valid POST returned 200 with a revisionId and the draft read back through forAuthor as the array it was. Pending: the client-only behaviours — the unsaved line toggling on edit and clearing on save, the leave warning, and the localStorage restore-after-reload — are not yet driven in a browser (shared MCP browser held by another process), which is what remains.
- [x] CMS-002/T7 — One schema module validates in the browser and on the server, and the server's error names the block and the field
  Evidence: The one module is apps/web/src/content/blocks.ts:validateBlocks, imported unchanged on both sides — the server through apps/web/src/content/items.ts:saveDraft, which the save route apps/web/src/app/admin/content/[id]/draft/route.ts surfaces, and the browser through apps/web/src/app/admin/content/[id]/edit/editor.tsx, which validates before it POSTs. The refusal carries the validator's own message, `<blocks[i].field>: <why>`: apps/web/src/content/content.test.ts asserts validateBlocks names the block index and the field (blocks[0], blocks[1], blocks[0].text), and apps/web/src/app/admin/content/[id]/draft/route.test.ts asserts the route answers 422 with that message — never 200, never 500 — for an alt-less image, an unknown type, and a fault at index 1. Mutation-proved against PostgreSQL 17: swallowing the naming (a generic detail) and defeating the validation (returning 200) each redden the three named 422 tests. Over real HTTP against the built server: an alt-less image answered `422 {"error":"invalid_blocks","detail":"blocks[0]: an image requires non-empty alt text (A11Y-R02)"}` and a valid body `200 {"revisionId":…}`.

## CMS-003 · Media library
Design: [docs/designs/cms/cms-003-media-library.md](designs/cms/cms-003-media-library.md) · PRD: `CMS-003`

- [x] CMS-003/T1 — Upload sniffs the type from the bytes, and refuses anything outside the accepted set
  Evidence: apps/web/src/content/media.ts:sniffType reads the type from the bytes — the fixed signatures of png, jpeg, webp and pdf, and an XML-root check for svg past an optional byte-order mark, an XML declaration and comments — and returns null for anything outside the accepted png, jpeg, webp, svg and pdf. It never trusts the filename or the declared content type, both of which the uploader writes (`SEC-001`). The re-encode and SVG sanitise the upload then applies (`CMS-003/T2`, `T3`) wait on `CMS-DEC-03`. Verified by apps/web/src/content/media.test.ts (pure, no database): each accepted type recognised by its bytes, an svg recognised past an xml declaration, a comment, leading whitespace and a BOM, and a GIF, prose, empty bytes and a non-root `<svg>` all refused. Mutation-proved — a sniff that returns a type instead of null reddens the refusal test.
- [!] CMS-003/T2 — Raster images are re-encoded, so EXIF and its location do not survive
  Blocked by: pending-decision: CMS-DEC-03 — re-encoding needs an image library, and which one is a new dependency the owner chooses; the safe default refuses raster uploads until it lands.
- [!] CMS-003/T3 — SVG is parsed and stripped to shape and text, or refused
  Blocked by: pending-decision: CMS-DEC-03 — sanitising SVG needs a library, or the design's refuse-outright fallback; the choice is the owner's, and the safe default refuses SVG until it lands.
- [x] CMS-003/T4 — Storage keyed by content hash, so a duplicate upload is one row
  Evidence: apps/web/src/content/media.ts:storeMedia stores bytes under their SHA-256 and returns the row's id with whether it created the row. The insert yields to an existing `sha256` (`onConflict … doNothing`), so the same file stored twice is one row and two uploads of it racing settle on the same row rather than one erroring on the unique key. Verified against PostgreSQL 17.11 by apps/web/src/content/media.test.ts on the suite's own database: the stored row's sha256, mime, byte_size and uploader; a duplicate returning the existing id with one row in the table; different bytes as different rows; and a warmed two-connection store of the same bytes settling on one row. Mutation-proved — removing the `onConflict` dedup reddens both the duplicate and the concurrent-store tests.
- [x] CMS-003/T5 — Serving joins through references and composes the audience predicate, answering `404` on no match
  Evidence: apps/web/src/content/media.ts:isReferencedByVisibleItem composes apps/web/src/content/access.ts:visibleTo over the items that reference a file — a correlated EXISTS through media_refs — so a file inherits the audience of what uses it by the one predicate every content read shares, and apps/web/src/content/media.ts:mediaForServing reads the row and returns null for a missing id and for a non-uuid, so a malformed id answers `404` rather than `500` from a uuid cast. The route apps/web/src/app/media/[id]/route.ts resolves the reader without sliding the session (apps/web/src/auth/gate.ts:accountForToken), serves when the reader uploaded the file or an item they may read references it, and otherwise answers the same `404` a missing file gets. Verified against PostgreSQL 17.11 by apps/web/src/content/media-serve.test.ts on its own database: a public file served to a visitor, an investor and an admin; an investor file `404` to a visitor and served to an investor and an admin; a granted file served only to the grantee and an admin and `404` to a non-grantee and a visitor; an unplaced file served to its uploader and `404` to another admin, an investor and a visitor — the uploader, not any admin; a file referenced only by an unpublished item `404` to a visitor and an investor; a non-existent id and a malformed id both `404`; and an unreadable file byte-identical to a missing one. Mutation-proved: dropping the `visibleTo` predicate (serving any referenced file to anyone) reddens the investor-visitor, granted-non-grantee and unpublished-item tests, and dropping the uploader clause reddens the unplaced-to-uploader test. Over real HTTP against the built server (BUILD_VERSION t5t6-verify): the public file with no cookie 200, the investor file with no cookie 404, and the investor file with the entitled investor's session cookie 200.
- [x] CMS-003/T6 — Cache headers follow the audience; nothing gated is cacheable
  Evidence: Caching follows the audience in apps/web/src/app/media/[id]/route.ts — a file a public item references (apps/web/src/content/media.ts:isReferencedByVisibleItem with no reader) is served `public, max-age=600`; anything else is `private, no-store`, the uploader's own unplaced file included, since no public item references it. Ten minutes matches the window the gateway's HTML carries at the edge and bounds how long a reader keeps a file whose item was later narrowed away from public. A signed-in reader's request is additionally `no-store` by apps/web/src/proxy.ts whatever the route computed, so a gated file is never cacheable. Verified against PostgreSQL 17.11 by apps/web/src/content/media-serve.test.ts: a public file `public, max-age=600` to a cookieless visitor; an investor file and a granted file `private, no-store` to every reader who may see them; the unplaced file `private, no-store` to its uploader; and both `404`s carry `no-store`. Mutation-proved: keying cacheability on the reader's audience instead of the public's — so a gated file served to its entitled reader would become cacheable — reddens the investor-file and granted-file no-store tests. Over real HTTP against the built server: the public file with no cookie carried `cache-control: public, max-age=600` with `x-content-type-options: nosniff`, and the investor file fetched with the entitled investor's session cookie carried `cache-control: no-store` (the proxy overlay).
- [x] CMS-003/T7 — Deletion is refused while a reference exists, and is audited when it is not
  Evidence: apps/web/src/content/media.ts:deleteMedia refuses deletion while any `media_refs` row exists, naming the items, and audits `media.delete` (apps/web/src/audit/record.ts:recordAudit, `SEC-R04`, subject the media id) when none does; an id no row holds is a no-op that writes nothing. Because `media_refs.media_id` is `ON DELETE CASCADE` (apps/web/migrations/1788752441424_platform.sql) — so the database does not refuse a referenced delete, it cascades it away — the media row is locked `FOR UPDATE`, which conflicts with the `FOR KEY SHARE` a `media_refs` insert takes on it, so a reference added concurrently either lands first and is seen, or waits and then fails against the row this delete removed; no referenced file is deleted. Verified against PostgreSQL 17.11 by apps/web/src/content/media.test.ts: an unreferenced file deleted and audited, a referenced file refused with the items and nothing written, a no-op for an absent id, and a warmed two-connection race asserting that a reference landing and a successful delete are never both true. Mutation-proved — never refusing a referenced delete reddens the referenced and race tests, and removing the audit reddens the audit test. The `FOR UPDATE` lock is the documented race-safe pattern for a delete-then-insert under READ COMMITTED; removing it did not deterministically redden the warmed race (timing), so a deterministic lock test is owed a later iter.

## CMS-004 · Preview, publish and withdraw
Design: [docs/designs/cms/cms-004-preview-publish-and-withdraw.md](designs/cms/cms-004-preview-publish-and-withdraw.md) · PRD: `CMS-004`

- [ ] CMS-004/T1 — Preview renders through the reader's own components and evaluates the audience rule as the chosen role
- [ ] CMS-004/T2 — Preview is admin-only, with no token and no shareable link
- [x] CMS-004/T3 — Publish validates, moves the pointer and audits, in one transaction, taking an explicit revision
  Evidence: apps/web/src/content/publish.ts:publish takes an explicit revision id, locks the item's row, re-validates the revision's blocks — publish is the gate past which a reader sees them, so it does not trust a stored row a validator tightened since, or a direct write, never checked — stamps the revision's published_at, moves content_items.current_revision_id to it, and records content.publish, all in one transaction, with recordAudit taking that transaction so the pointer move and its audit commit or roll back together (SEC-R04, CMS-R01). Proven against PostgreSQL 17.11 by apps/web/src/content/content.test.ts (the publish and withdraw describe): publishing moves the pointer to the named revision, stamps published_at and writes one content.publish row; a revision whose blocks are invalid is refused and writes no audit — and its mutation coverage lives with CMS-006's read tests that rely on published pointers. Closed by cross-reference: the function ships and is tested, so a second implementation would be the redundancy §1.10 forbids; the preview and confirmation surfaces that call it are CMS-004/T1, T2 and T4.
- [ ] CMS-004/T4 — The confirmation names what is replaced and how many locales will fall back
- [x] CMS-004/T5 — Withdraw returns to the previous published revision, or states plainly that nothing will be visible
  Evidence: apps/web/src/content/publish.ts:withdraw locks the item, moves content_items.current_revision_id to the most recent revision published before the current one — or to null when there is none — and records content.withdraw in the same transaction (SEC-R04). The withdrawn revision keeps its published_at, so what an investor read is still on disk and re-publishing is moving the pointer forward again (CMS-R01). Withdrawing an item that already shows nothing is refused: there is no pointer to move back. The returned item carries current_revision_id null when nothing remains visible, which is what the confirmation surface states plainly. Proven against PostgreSQL 17.11 by apps/web/src/content/content.test.ts (the publish and withdraw describe): two successive withdrawals move the pointer to the previous published revision and then to nothing, each recording one content.withdraw; an item showing nothing refuses withdrawal. Closed by cross-reference: withdraw ships and is tested; the plain-words confirmation is CMS-004/T4's surface.
- [ ] CMS-004/T6 — A public item's cache is purged on publish and on withdraw

## CMS-005 · Locale variants and translation state
Design: [docs/designs/cms/cms-005-locale-variants-and-translation-state.md](designs/cms/cms-005-locale-variants-and-translation-state.md) · PRD: `CMS-005`

- [x] CMS-005/T1 — Locale rows per revision, with a state a query filters on rather than infers
  Evidence: the locale rows are content_locales (DATA-001), one per (revision, locale) carrying the translated blocks and a state of machine or reviewed — a column (apps/web/src/db/types.ts:ContentLocaleState, the CHECK the migration declares), never an inference from a null reviewed_at. apps/web/src/content/locales.ts:localeFor is the read that turns that column into behaviour: it filters state = 'reviewed', so "servable" is a predicate the query expresses rather than a rule each caller must remember. Proven against PostgreSQL 17.11 by apps/web/src/content/locales.test.ts: a reviewed row is served for the reader's language. Mutation-proved: removing the state filter reddens a named test, because a machine row would then be served.
- [x] CMS-005/T2 — A `machine` row is never reachable by any reader path
  Evidence: apps/web/src/content/locales.ts:localeFor — the one reader path for a localed revision — filters state = 'reviewed', so a machine draft is served to nobody (CMS-R05): it is absent from the candidates the query returns, and a requested locale with only a machine row falls through to the authored English exactly as one with no row at all does. Proven against PostgreSQL 17.11 by apps/web/src/content/locales.test.ts: a revision whose German is a machine row serves English to a reader requesting German, not the machine text. Mutation-proved: removing the state filter serves the machine row and reddens that test.
- [!] CMS-005/T3 — Drafting translates block text and reassembles marks by span, never by offset arithmetic
  Blocked by: pending-decision: CMS-DEC-04 — the mark reassembly by span needs a programmatic per-span translator, so which carrier drafts a locale (a self-hosted service, or the admin translating in review) decides what this task builds; the two answers build different mechanisms. The design leaves the carrier open (CMS-005 §3) and it is the owner's to settle.
- [ ] CMS-005/T4 — The review screen shows source beside translation, editable, marked one locale at a time
- [x] CMS-005/T5 — Serving falls back to the authored language and says so to the reader
  Evidence: apps/web/src/content/locales.ts:localeFor returns the reviewed translation for the reader's locale — the exact locale, then the language without its region (fr-CA takes a reviewed fr), never a script apart (zt never falls back to zh, because a reader who cannot read one script cannot read it served as the other's fallback) — and when none is reviewed it returns the revision's own blocks, the authored English, with fellBack true so the reading view can tell the reader they are reading English because their language is not yet ready (I18N-R04, I18N-DEC-01). Proven against PostgreSQL 17.11 by apps/web/src/content/locales.test.ts: a language with no reviewed row falls back to English and reports it, a region is dropped to match the language, and Traditional Chinese does not take Simplified as its fallback. Mutation-proved: removing the region-strip, and suppressing the fallback flag, each redden a named test.
- [ ] CMS-005/T6 — A new revision starts with no locale rows, and the grid shows it

## CMS-006 · Audience and access
Design: [docs/designs/cms/cms-006-audience-and-access.md](designs/cms/cms-006-audience-and-access.md) · PRD: `CMS-006`

- [x] CMS-006/T1 — One predicate builder from a reader, with the published check inside it
  Evidence: apps/web/src/content/access.ts:visibleTo turns a reader — an actor, or null for a visitor with no session — into the WHERE fragment every content query composes (CMS-R03); apps/web/src/content/access.ts:ContentVisibility is its type. A visitor gets audience = public AND current_revision_id IS NOT NULL. An investor gets current_revision_id IS NOT NULL AND (audience IN (public, investor) OR (audience = granted AND EXISTS (SELECT 1 FROM content_grants correlated on this item and this account))). An admin gets a literal TRUE. Three properties are structural rather than agreed: the published check is inside the predicate, so a draft is an item with no audience rather than a low-audience one (CMS-R02) and no caller can forget it; the grant check is a correlated EXISTS and never a join, so several grants on one item cannot multiply the row and invite a DISTINCT that moves filtering outside the predicate; and the admin branch compiles to a literal, not a bound parameter; and a role the vocabulary does not yet name reads nothing rather than inheriting the investor's audience by falling through — the one predicate the design calls most dangerous is not where an unrecognised role is admitted silently, and the line refusing it stops type-checking the day a third role is added. Proven against PostgreSQL 17.11 by apps/web/src/content/read.test.ts, whose five visibleTo cases query the table with the predicate alone — the shape a list or a search composes, where no join can hide a missing clause: a visitor sees only the published public item, an investor sees public and investor content plus the granted items they hold a grant for, an investor holding no grant does not see the granted item, an admin sees every audience and the unpublished items too, and an item carrying two grants answers once. Mutation-proved: dropping the published check from the visitor branch alone, from the investor branch alone, and from both; replacing the grant subquery with a literal true; writing the grant check as a join at the query site, and letting a non-admin role fall through to the investor predicate, each redden a named test.
- [x] CMS-006/T2 — Reader and author read paths as separate functions, so admin-sees-all cannot leak into a shared one
  Evidence: apps/web/src/content/read.ts:forReader and apps/web/src/content/read.ts:forAuthor are two functions rather than one with a flag, and both compose apps/web/src/content/access.ts:visibleTo, the one predicate. forReader joins the revision the item's published pointer names, so what it returns is the revision a reader is entitled to by construction: an item with no published revision has nothing for it to return, to a visitor, an investor and an admin alike (CMS-R02), whichever branch the predicate took. forAuthor returns the latest revision, published or not, ordered by creation with the identifier breaking a tie, and refuses a non-admin before it queries — that role check is the gate rather than a second opinion, because the predicate scopes which item is reachable while the revision this one selects is the newest rather than the published one. A refusal is null, and it is the same null an item that does not exist gets, so a caller cannot tell the two apart and can answer one 404 for both (CMS-006/T4 carries the route). forReader's join binds the revision to this item and to being published — neither enforced on the pointer by the schema, which lets the foreign key name any revision — so a pointer to another item's revision or to an unpublished one, states publish and withdraw never create, resolves to nothing rather than trusting the write path. Proven against PostgreSQL 17.11 by apps/web/src/content/read.test.ts: public content reaches every reader; investor content reaches an investor and an admin and not a visitor; a granted item reaches its grantee and an admin and nobody else; an unpublished item reaches nobody, including an admin; the published revision comes back for an item that also holds an open draft, while forAuthor returns that open draft to an admin and nothing to an investor even for an item they may read; and a missing identifier and a withheld one answer identically. Mutation-proved: loosening forReader's join so a draft comes back, dropping the item binding so another item's revision is served, dropping the published check so an unpublished revision is served, and removing forAuthor's role check, each redden a named test.
- [x] CMS-006/T3 — The grant subquery, and the admin surface that writes and revokes grants
  Evidence: apps/web/src/content/grants.ts:addGrant and apps/web/src/content/grants.ts:removeGrant are the write and revoke surface the content module exposes over content_grants; the deck admin route that calls them is DECK-004's (depended_by), and this task ships the mechanism it composes rather than a second page over the same table. Both write in one transaction with the audit that records them (SEC-R04): recordAudit takes the transaction, so a grant.add or grant.remove row and the content_grants change commit or roll back together, and the grantee account is the audited subject because a grant is a permission on an account the way a role change is. addGrant inserts ON CONFLICT DO NOTHING and returns false when a grant already stood, recording nothing — the trail carries acts, not re-assertions — and removeGrant returns false and records nothing when there was none to remove. The read side of the grant is the granted branch of apps/web/src/content/access.ts:visibleTo, which admits an item to an investor only where a content_grants row names them (CMS-R03). Proven against PostgreSQL 17.11 by apps/web/src/content/read.test.ts (21 tests): a granted deck is invisible to a non-grantee investor, turns visible to them the moment addGrant runs and invisible again the moment removeGrant does, a second addGrant is a no-op that writes no second audit, and each act writes exactly one audit row for the grantee. Mutation-proved, each reddening a named case: dropping addGrant's idempotency guard so a re-grant re-audits, mislabeling the grant.add action, and dropping removeGrant's guard so a revoke of nothing audits.
- [x] CMS-006/T4 — A refusal is a `404`, and it is the same `404` for an item that does not exist
  Evidence: apps/web/src/app/content/[id]/route.ts serves a published item's body to a reader who may see it, composing apps/web/src/content/read.ts:forReader — itself composing the one predicate apps/web/src/content/access.ts:visibleTo — and answering the same 404 to a reader who may not see it, to an item with no published revision, and to an item that does not exist, because all three are forReader's one null; never a 403, which would confirm the id names something (CMS-006 §2). The id reaches the route straight from the URL and content_items.id is a uuid, so a value that is not one is refused by its shape before forReader, answering that same 404 rather than a 500 repeating the value back (DATA-R02). The 200 body is the revision narrowed to its id, its blocks and its published time — not author_id, the staff identifier forReader still carries on the row — so the reader-facing shape is fixed here before INV-001, POST-002 and RPT-002 depend on the wider one (CMS-006 §6). The session is resolved without sliding it (apps/web/src/auth/gate.ts:accountForToken), since reading an item is not the act that should extend a session. Verified against PostgreSQL 17.11 by apps/web/src/content/content-serve.test.ts, an isolated-database suite of 11 tests driving the real handler across the audience-by-reader matrix — a public, an investor, a granted and a draft item, each read by a visitor, by investorA, by a non-grantee investorB and by an admin: the served body narrowed and carrying no author_id, the byte-identical 404 for a missing, a malformed and a withheld id, and the cache header by audience. Mutation-proved and restored byte-identical: disabling the uuid shape guard reddens the malformed-id test, disabling the null check reddens the non-grantee and visitor refusals, and inverting the public-cache test reddens the cache assertions.
- [x] CMS-006/T5 — A CI gate refuses SQL naming `content_items` outside the repository module
  Evidence: scripts/check-content-access.py fails on any file under apps/web or scripts that names any of the four content tables — content_items and the content_revisions, content_locales and content_grants that hold or gate its body — matched case-insensitively, since PostgreSQL folds an unquoted identifier and CONTENT_ITEMS names the same table. Five named exemptions each carry the reason they are one: the content module itself, the migrations that create the table, apps/web/src/db/types.ts which names it as a key of the database type, apps/web/src/db/db.test.ts which compares that description to the migration, and the gate, which names the table because it searches for it. It runs as make check-content-access and inside the make check aggregate. Two ways it could pass while checking nothing are refused rather than trusted: a scan that matched no file at all, and an exemption whose path has been renamed away. Measured on the clean tree: 56 files scanned, none naming a content table, 15 exempt under 5 exemptions, exit 0. Proven to bite three ways, each restored afterward: a scratch route querying a content table outside the module failed naming that path and line; scan roots pointed at directories that are not there failed with no file scanned; and an exemption repointed at a path that is not there failed both for the stale exemption and for the two lines it had been excusing.
- [x] CMS-006/T6 — Narrowing an audience is audited, and public content carries a short cache lifetime
  Evidence: apps/web/src/content/audience.ts:changeAudience sets an item's audience in one transaction — locking the row, returning it unchanged when the audience is already the one asked for, and otherwise writing the column and recording one content.audience_change (apps/web/src/audit/record.ts:recordAudit, SEC-R04) so the change and its trail commit or roll back together; a change to the audience it already holds records nothing, because an audience change is a change and a trail must not log a write that altered nothing. The cache half is the serve route's (CMS-006/T4): apps/web/src/app/content/[id]/route.ts answers a public item `public, max-age=600` — short, so a later narrowing away from public is bounded rather than cached forever — and anything else `private, no-store`, with apps/web/src/proxy.ts overlaying no-store on any cookie-bearing request regardless. Verified against PostgreSQL 17.11: apps/web/src/content/content.test.ts asserts changeAudience records exactly one content.audience_change on a real change and nothing on a no-op, and apps/web/src/content/content-serve.test.ts drives narrowing end to end — a public item a visitor could read answers 404 to that visitor once it is narrowed to investor, while an investor still reads it, now no-store. Mutation-proved and restored byte-identical: mislabelling the audit action reddens the record-once test, inverting the no-op guard reddens both the change and the no-op tests, and inverting the route's public-cache test reddens the cache assertions.

## CMS-007 · Search and filter in the room
Design: [docs/designs/cms/cms-007-search-and-filter.md](designs/cms/cms-007-search-and-filter.md) · PRD: `CMS-007`

- [x] CMS-007/T1 — A generated `tsvector` over flattened block text, so the index cannot drift from the content
  Evidence: apps/web/migrations/1788749077541_content.sql folds a `search tsvector GENERATED ALWAYS AS (to_tsvector('simple', blocks_text(blocks))) STORED` column into content_revisions, with a GIN index over it, so the full-text index is a function of the blocks column that the database maintains and cannot drift from — the usual failure, where a document is edited and the index still answers about the previous version, is not expressible. blocks_text(jsonb) is an IMMUTABLE SQL function flattening the text-bearing fields (headings, paragraph and quote text, list items, captions and alternative text) and no other, so a media id or a mark type never enters the index; 'simple' rather than a stemmer, since the content is in twenty possible languages. It is folded rather than added by ALTER (DATA-R07): apps/web/src/db/db.test.ts parses the CREATE body and a fresh database is what deploys, and the down migration drops blocks_text after the table whose generated column depends on it. Proven: make migrate-roundtrip green (up, down, up); apps/web/src/db/db.test.ts holds the new column against the interface and descriptor; and apps/web/src/content/content-search.test.ts finds an item by each of the six searched fields and by a heading prefix, while a mark's href and a figure's data cell — text the flattener does not name — are not findable. The column is search: Generated in apps/web/src/db/types.ts (the database fills it and no query selects it), and apps/web/src/content/items.ts:ContentRevision omits it, so a caller never carries the internal index.
- [x] CMS-007/T2 — The search query composes `CMS-006`'s predicate as its first clause, and lives in the repository module
  Evidence: apps/web/src/content/search.ts:search composes apps/web/src/content/access.ts:visibleTo — the same predicate every read uses, not a copy, so a change to the access rule reaches search in the same commit (CMS-R03) — and lives in the content module, the one place a content table may be named (CMS-006/T5). It matches content_revisions.search with to_tsquery over the reader's terms suffixed `:*` (prefix matching; apps/web/src/content/search.ts:toPrefixQuery strips each term to letters and digits so only a valid query, never the raw input, reaches to_tsquery), scoped to the reader passed as an argument (DATA-R05) and ordered by recency. Proven against PostgreSQL 17.11 by apps/web/src/content/content-search.test.ts: a public item is found by every reader including a visitor, an investor item by an investor and an admin but not a visitor, and a granted item by its grantee and an admin but not a non-grantee investor or a visitor. Mutation-proved and restored byte-identical: dropping the visibleTo clause reddens the isolation tests — a visitor then finds investor-only content, the leak this design exists to prevent.
- [ ] CMS-007/T3 — Kind, product, period and type filters, composing into one statement
- [x] CMS-007/T4 — Only the published revision is findable; a draft is not, including by its author
  Evidence: apps/web/src/content/search.ts:search joins content_revisions through content_items.current_revision_id, the published pointer, so only the published revision is searched. This is not met by the audience predicate alone (CMS-007 §6): apps/web/src/content/access.ts:visibleTo answers an admin TRUE, unpublished items included, because the author surfaces rely on it — so the pointer join, as apps/web/src/content/read.ts:forReader does, is what excludes a draft. Proven against PostgreSQL 17.11 by apps/web/src/content/content-search.test.ts: an unpublished item is not found even by an admin; the newer open draft of a published item is not found by its content while the published body is; and a withdrawn item, whose revision keeps its published_at while the pointer is back to null, is not found — the case the pointer join hides and the published_at filter would not, which is what makes it isolate the join. Mutation-proved and restored byte-identical: joining by item_id rather than the published pointer reddens the withdrawn-item test.
- [ ] CMS-007/T5 — The empty state names what narrowed the result and offers to widen it
- [ ] CMS-007/T6 — The field is labelled, keyboard-operable, and announces its result count

## DATA-002 · Erasure and retention
Design: [docs/designs/data/data-002-erasure-and-retention.md](designs/data/data-002-erasure-and-retention.md) · PRD: `DATA-002`

- [x] DATA-002/T1 — The manifest: every table, what it holds, and what erasure does to it
  Evidence: The manifest is the table in docs/designs/data/data-002-erasure-and-retention.md §3 — one row per table, each naming what it holds about a person and what a real delete does to it. It enumerates the fourteen tables the migrations create (accounts, sessions, invitations, content_items, content_revisions, content_locales, content_grants, mail_log, unsubscribes, media, media_refs, portfolio, config and audit) plus three the reader needs and no migration creates: deck_reads and report_reads, forward-looking rows the design lists ahead of the features that will add them, and pgmigrations, the migration tool's own bookkeeping where no person appears. The split the design draws is the one erasure turns on: what is about the person (their sessions, mail, grants, read records) cascades, and what the person did on the company's behalf (a revision's author_id, a grant's granted_by, a translation's reviewed_by, a file's uploaded_by) is set null, so the company's document survives the staff member's erasure; accounts itself is the subject, deleted rather than referenced, and audit is retained because SEC-002 keeps it free of personal data by construction. That the manifest is complete and every disposition on it is the schema's own is no longer a claim but a mechanically proven fact: DATA-002/T3's gate holds it against the migrations, and is what keeps a table added tomorrow from being forgotten here.
- [x] DATA-002/T2 — One erasure function driven by the manifest, with the schema's `on delete` matching it
  Evidence: apps/web/src/admin/accounts.ts:eraseAccount deletes the accounts row in one transaction and lets the foreign keys carry the deletion outward exactly as the manifest declares — what is about the person cascades (the sessions they hold, the invitation they were sent, the mail they received, the decks they were granted, their unsubscribe preference) and what they did on the company's behalf is set null (a revision's author_id, a grant's granted_by, a translation's reviewed_by, a file's uploaded_by, the board's updated_by, a config change's changed_by). The dispositions are DATA-001's migrations and DATA-002/T3's gate holds them to the manifest; this function sets them in motion. It joins suspendAccount, changeRole and reinstateAccount and takes their shape, with a guard weightier here because erasure has no inverse (ADMIN-DEC-01): an admin may not erase their own account, and no single act may leave the room with no admin who can sign in — both refused before any write with the false a no-op returns. No session is ended by hand; deleting the row cascades to sessions, so the person is signed out by the statement that erases them. The account.delete audit row outlives its subject — actor_id and subject_id are bare uuids, not foreign keys (SEC-002) — and is written in the same transaction, so the delete and its record are one act (DATA-R03: a real delete, the trail retained minimally and holding no personal data). Proven against PostgreSQL 17.11 by apps/web/src/admin/accounts.test.ts: erasing an active account removes the row, ends its two sessions, deletes its outstanding invitation and writes one account.delete audit that survives the deletion; the self and last-active-admin guards each refuse while another path is permitted, writing nothing; an absent id is a no-op; the delete and the audit roll back together when the audit is handed an unparseable actor; and no other account is touched. Mutation-proved against real PostgreSQL, each reddening a named test and restored byte-identical: removing the self guard, removing the last-admin guard, and mislabelling the audit action.
- [x] DATA-002/T3 — A gate comparing the manifest against the live schema, with exemptions written rather than patterns loosened
  Evidence: scripts/check-erasure-manifest.py parses the manifest in docs/designs/data/data-002-erasure-and-retention.md §3 and the CREATE TABLE statements under apps/web/migrations, and holds the three invariants the design names. First, every table a migration creates appears in the manifest — a new table cannot ship without someone deciding its erasure. Second, every column that REFERENCES accounts has the ON DELETE disposition the manifest's row names, so a cascade the manifest calls set null, or the reverse, is refused here rather than discovered during an erasure. Third, a table the manifest declares to hold nothing personal has no column whose name carries a personal-data token — email, name, phone, address, ip — matched against the column's underscore-delimited tokens rather than as substrings, because ip as a substring matches recipient and description and would fire on half the schema; a real misfire is answered by a named line in scripts/check-erasure-manifest.py:EXEMPTIONS, never a weaker pattern, which the design's §6 fixes as the response. It parses the migrations rather than a live database, so it runs in CI with no connection, the way apps/web/src/db/db.test.ts proves the types against the same files; pgmigrations is named in the manifest and created by no migration, so it stays outside the parse. Wired as make check-erasure inside the make check aggregate. On the current tree it reports fourteen created tables declared and eleven accounts-references, every disposition matching, exit 0. Mutation-proved, each mutation restored byte-identical afterward and the gate green again: renaming a created table's manifest row so sessions is absent reddens the first check; changing config's cell from set null to cascade while the schema keeps set null reddens the second; declaring accounts to hold nothing personal so its email and name columns stand exposed reddens the third; and changing the accounts row so it no longer says the row is deleted reddens the subject-of-erasure check.
- [x] DATA-002/T4 — Scheduled deletion enforcing each retention window
  Evidence: apps/web/scripts/retention-sweep.mjs deletes what each window in the design's §3 table has outlived — expired sessions (expires_at past), consumed or expired invitations (a spent or lapsed credential), and mail_log rows older than two years — and reports how many of each went. audit is absent, kept by never being deleted (append-only, DATA-R09, a DELETE on it refused by a trigger); content_revisions is absent, kept forever (CMS-R01). It is the operational-script sibling of the backup and migration-roundtrip scripts, runnable by hand as make retention and npm run retention (apps/web/package.json), printing its target before connecting; the schedule that runs it on a cadence is OPS-001's deploy cron, the way the backup's is (build-ahead — the deletion ships and runs, verified printing 0 of each against the empty development database, and the cron is the deploy's). Proven against PostgreSQL 17.11 by apps/web/src/db/retention-sweep.test.ts, against a database it creates for itself so the whole-database sweep cannot reach a parallel suite's rows: an expired session goes and the live one stays (asserted by which token_hash survives, not merely the count), a consumed and an expired invitation go while a second account's live outstanding one stays, a three-year-old mail_log row goes and a month-old one stays (asserted by which subject survives), and the audit trail is left untouched. Mutation-proved: inverting the session comparison, turning the invitation OR into an AND, and inverting the mail-log age each redden a named test — the survivor assertions catch a sign flip a count alone would not.
- [x] DATA-002/T5 — Content authored by an erased account survives with a null author
  Evidence: the property holds because content_revisions.author_id references accounts ON DELETE SET NULL (DATA-001, the disposition DATA-002/T3's gate holds to the manifest), so apps/web/src/admin/accounts.ts:eraseAccount nulls the author and keeps the revision rather than cascading it — a published revision is the company's document, and erasing its author must not delete an investor's archive. Proven against PostgreSQL 17.11 by apps/web/src/content/erasure.test.ts, which lives in the content module because only there may a content table be named (CMS-006): a revision authored by the erased account survives with a null author_id, and the same split is shown on content_grants, the one table carrying both dispositions — a grant to the subject cascades away while a grant the subject made for another account stays with its granted_by nulled. Mutation-proved: changing content_revisions.author_id to ON DELETE CASCADE reddens the surviving-revision test, and changing content_grants.granted_by to CASCADE reddens the nulled-granter test.

## DATA-003 · Backup and restore
Design: [docs/designs/data/data-003-backup-and-restore.md](designs/data/data-003-backup-and-restore.md) · PRD: `DATA-003`

- [x] DATA-003/T1 — A daily dump, encrypted before it leaves the host, to a target from the environment
  Evidence: scripts/backup.py (make backup) — pg_dump --format=custom piped through openssl aes-256-cbc so the plaintext never touches disk, written to BACKUP_TARGET with a UTC-stamped name that says nothing of its contents (DATA-R02). BACKUP_KEY is the passphrase, in config beside BACKUP_TARGET (CRED-001); absent either, no backup is taken and it says so (SEC-R05). Verified against PostgreSQL 17.11: a 37 KB Salted__ file with no plaintext PGDMP.
- [x] DATA-003/T2 — Seven daily, four weekly, twelve monthly, enforced rather than intended
  Evidence: scripts/backup.py keep_set — the newest backup in each of the last seven days, four ISO weeks and twelve months is kept and the rest are pruned on every run, so the retention is enforced by the same command that writes, not by a schedule nobody checks.
- [x] DATA-003/T3 — `make restore-rehearsal` into a throwaway database, asserting schema and row counts, printing the elapsed time
  Evidence: scripts/restore-rehearsal.py (make restore-rehearsal) — fetches the newest backup, decrypts it, restores into a throwaway database (never a live one), asserts the table set equals the current migration head and every row count is within tolerance of live, prints the elapsed time, and drops the scratch database. Verified against PostgreSQL 17.11: fourteen tables match, restore OK in 2.4s.
- [x] DATA-003/T4 — `make doctor` reports the last successful backup and the last successful rehearsal, and fails the second after two months
  Evidence: scripts/doctor.py — a Backups section reports the newest backup and the last rehearsal from BACKUP_TARGET, and doctor exits non-zero when the rehearsal is over two months old or has never run, so a stale restore is a failure of the feature rather than a missing chore. Verified: exit 1 on a rehearsal dated over two months back, exit 0 on a fresh one.
- [x] DATA-003/T5 — A restore runbook whose every command was executed in the rehearsal, with measured timings
  Evidence: docs/runbooks/data-003-restore.md — the production restore, owner present and application stopped, whose decrypt and pg_restore commands are the ones make restore-rehearsal runs and whose 2.4s timing is the one it measured; restoring over live data is deliberately left to this document rather than given a target of its own.

## RPT-001 · Investor report authoring
Design: [docs/designs/rpt/rpt-001-investor-report-authoring.md](designs/rpt/rpt-001-investor-report-authoring.md) · PRD: `RPT-001`

- [x] RPT-001/T1 — Create a report against a period, with the period fixed at creation
  Evidence: apps/web/migrations/1788749077541_content.sql adds the content_items_period_format check, so a report's period is shaped YYYY-Qn or YYYY-MM in the database; apps/web/src/content/items.ts:createItem is the create path that stores it, and no function updates period after creation, so it is fixed. Proven against PostgreSQL 17 by apps/web/src/content/content-reports.test.ts — a well-formed quarterly and monthly period are stored, five malformed periods are refused by the constraint, and publishing a revision leaves the period unchanged. migrate-roundtrip green. Mutation-proved: a pattern accepting anything, and one rejecting a valid quarter, are each killed.
- [x] RPT-001/T2 — A new report is prefilled with the previous period's structure and none of its text
  Evidence: apps/web/src/content/reports.ts:prefillStructureFor returns the heading blocks of the previous published report — the one of the greatest period below the new one, so a skipped period falls back to the last that exists — dropping every non-heading block, or apps/web/src/content/reports.ts:DEFAULT_REPORT_STRUCTURE when there is no previous report; it composes visibleTo like every content read (CMS-R03), and the join to the published pointer keeps a draft from being carried. Proven against PostgreSQL 17 by apps/web/src/content/content-reports.test.ts — a first report opens with the suggested structure, a later one carries the greatest-period-below report's headings and none of its text, a gap falls back to the last that exists, and a draft is never carried forward. Mutation-proved: inverting the heading filter, ordering ascending, and reversing the period comparison are each killed.
- [x] RPT-001/T3 — Metrics are `figure` blocks carrying their numbers as data
  Evidence: apps/web/src/content/blocks.ts defines the figure block carrying its numbers as a required string array of data alongside its caption, so a metric is a figure a screen reader can read and a later chart can consume, and a figure without that array is refused on write. Proven by apps/web/src/content/content.test.ts — a document with a figure carrying its data round-trips through validateBlocks. Closed by cross-reference: the figure block (CMS-001) is the mechanism, so no report-specific code is added (§1.10).
- [ ] RPT-001/T4 — The progress board's current values are shown beside the section that narrates them
- [ ] RPT-001/T5 — Making a report public states in words what that means

## RPT-002 · Report periods and archive
Design: [docs/designs/rpt/rpt-002-report-periods-and-archive.md](designs/rpt/rpt-002-report-periods-and-archive.md) · PRD: `RPT-002`

- [x] RPT-002/T1 — A partial unique index gives one published report per period, and drafts are exempt
  Evidence: apps/web/migrations/1788749077541_content.sql folds a partial unique index one_published_report_per_period on content_items (type, period) WHERE type = 'report' AND current_revision_id IS NOT NULL, so "one published report per period" is a database guarantee rather than a convention that holds until two admins work the same afternoon: publishing a second report into a taken period fails at the pointer move, a draft replacement for a live period is legitimate because the index is partial on the published pointer, and it is on the item not the revision (CMS-R01) so a corrected report is a new revision of the same item and withdrawing the published one frees the period at once. Folded rather than added by ALTER (DATA-R07): make migrate-roundtrip is green (up, down, up) and the index drops with the table. Verified against PostgreSQL 17.11 by apps/web/src/content/content-reports.test.ts on a database of its own, created fresh because the shared development database is stale to a fold node-pg-migrate will not re-apply: a second published report for a period is refused by the index by name, a draft replacement is created without error, a corrected revision re-publishes the same item without colliding with itself, distinct periods each hold a published report, and a withdrawal frees the period. Mutation-proved and restored byte-identical: weakening the index to a non-unique one lets the second report publish, reddening the refusal test.
- [ ] RPT-002/T2 — Publishing into a taken period fails with the report that holds it and the two real choices
- [ ] RPT-002/T3 — The archive lists by period, groups by year, and shows a period with no report as a gap
- [x] RPT-002/T4 — The room's current report is the most recent period the reader may read, not the most recent publication
  Evidence: apps/web/src/content/reports.ts:currentReport returns the published report of the greatest period the reader may read, by period rather than by publication date, composing visibleTo (CMS-R03) with the published pointer required explicitly so an admin's TRUE predicate cannot leave a draft current. Proven against PostgreSQL 17 by apps/web/src/content/content-report-current.test.ts on an isolated database — null when none is published, the greatest period rather than the latest publication, the greatest the reader may see when the top is out of reach, and a draft ignored. Mutation-proved: ordering ascending, and admitting an unpublished pointer, are each killed.
- [ ] RPT-002/T5 — Withdrawal states that the period becomes a gap and which report becomes current
- [x] RPT-002/T6 — A per-account read state, used only in the list, and deleted with the account
  Evidence: apps/web/migrations/1788758340000_report_reads.sql creates report_reads — one row per account per report, account_id and item_id cascading — already declared in the erasure manifest (docs/designs/data/data-002-erasure-and-retention.md). apps/web/src/content/reports.ts:markReportRead records a read idempotently: a re-read changes nothing, so there is one read state and not a visit count. apps/web/src/db/types.ts and apps/web/src/db/db.test.ts carry the table schema. Proven against PostgreSQL 17 by apps/web/src/content/content-reports.test.ts — the report recorded once with a read time, a re-read keeping the first time, a distinct row per report, and the state deleted with the account. migrate-roundtrip green. Mutation-proved: recording the account id where the report belongs is killed.

## RPT-003 · Report reading
Design: [docs/designs/rpt/rpt-003-report-reading.md](designs/rpt/rpt-003-report-reading.md) · PRD: `RPT-003`

- [ ] RPT-003/T1 — One column at a reading measure, with the period, title and date on the page itself
- [ ] RPT-003/T2 — Previous and next by period, skipping gaps, absent rather than disabled at the ends
- [ ] RPT-003/T3 — A print stylesheet: black on white, repeating header, figures as tables, links with their targets
- [ ] RPT-003/T4 — The phone layout is the default; only figures scroll horizontally, never the page
- [ ] RPT-003/T5 — The locale fallback notice sits above the content

## SITE-005 · The gateway served by the application
Design: [docs/designs/site/site-005-gateway-served-by-the-application.md](designs/site/site-005-gateway-served-by-the-application.md) · PRD: `SITE-005`

- [ ] SITE-005/T1 — The page is server-rendered from the same dictionary, at the same URLs, with the same asset paths
- [ ] SITE-005/T2 — The parity gate is pointed at the rendered output and counts both catalogues, for both readers
- [ ] SITE-005/T3 — Node-for-node comparison of the rendered page against the static one, at three viewports
- [ ] SITE-005/T4 — Edge caching for anonymous readers, `Vary` on the session cookie, verified through the CDN with and without one
- [ ] SITE-005/T5 — First paint measured before and after, at the same viewport on the same machine
- [ ] SITE-005/T6 — `main` stays deployable as the fallback until the owner answers `INFRA-DEC-03`

## SITE-006 · Legal pages and the consent surface
Design: [docs/designs/site/site-006-legal-pages-and-consent.md](designs/site/site-006-legal-pages-and-consent.md) · PRD: `SITE-006`

- [ ] SITE-006/T1 — Three legal pages in twenty locales, through the parity gate, linked in a footer row of their own
- [ ] SITE-006/T2 — The banner: three categories, `necessary` fixed, three controls of equal weight, no dismissal without an answer
- [ ] SITE-006/T3 — The banner is not first in the tab order, does not trap focus, and carries state without relying on colour
- [ ] SITE-006/T4 — The stored choice read in a `try`/`catch`; an unreadable store means no answer, and nothing non-essential loads
- [ ] SITE-006/T5 — A control on `legal/cookies` that changes the answer, and a footer link that reaches it
- [ ] SITE-006/T6 — The legal pages print in black on white
